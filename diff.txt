--- old_station.html	2026-06-29 16:15:51
+++ station-v121.html	2026-06-29 16:28:46
@@ -10,8 +10,11 @@
     <meta http-equiv="Expires" content="0" />
     <title>Aqua Station – ตู้บริการน้ำดื่ม</title>
     <!-- Firebase Compat SDKs (More resilient for WebViews) -->
-    <script src="https://www.gstatic.com/firebasejs/10.8.1/firebase-app-compat.js"></script>
-    <script src="https://www.gstatic.com/firebasejs/10.8.1/firebase-firestore-compat.js"></script>
+    <!-- Local Firebase SDKs for True Offline-First Boot -->
+    <script src="assets/js/firebase-app-compat.js"></script>
+    <script src="assets/js/firebase-firestore-compat.js"></script>
+
+
     <script>
         // 🚀 INITIALIZE FIREBASE COMPAT
         try {
@@ -23,33 +26,133 @@
                 messagingSenderId: "330527536801",
                 appId: "1:330527536801:web:c0132854940609dd3f62e",
             };
-            
+
             firebase.initializeApp(firebaseConfig);
             const db = firebase.firestore();
-            
+
             // 🚀 EXPORT TO WINDOW (Compat Style)
             window.db = db;
+            window.lastSuccessfulWriteTime = Date.now();
+
             window.doc = (db, coll, id) => db.collection(coll).doc(id);
-            window.getDoc = (docRef) => docRef.get();
-            window.setDoc = (docRef, data) => docRef.set(data, { merge: true });
-            window.updateDoc = (docRef, data) => docRef.update(data);
-            window.addDoc = (collRef, data) => collRef.add(data);
+            window.getDoc = (docRef, options = {}) => {
+                return docRef.get(options).then((res) => {
+                    window.lastSuccessfulWriteTime = Date.now();
+                    return res;
+                });
+            };
+            window.setDoc = (docRef, data) => {
+                const p = docRef.set(data, { merge: true }).then((res) => {
+                    window.lastSuccessfulWriteTime = Date.now();
+                    return res;
+                });
+                return Promise.race([p, new Promise(r => setTimeout(() => {
+                    console.warn("⚠️ [FIREBASE] setDoc timeout (offline/hanging), resolving early for UI.");
+                    r();
+                }, 2500))]);
+            };
+            window.updateDoc = (docRef, data) => {
+                const p = docRef.update(data).then((res) => {
+                    window.lastSuccessfulWriteTime = Date.now();
+                    return res;
+                });
+                return Promise.race([p, new Promise(r => setTimeout(() => {
+                    console.warn("⚠️ [FIREBASE] updateDoc timeout (offline/hanging), resolving early for UI.");
+                    r();
+                }, 2500))]);
+            };
+            window.addDoc = (collRef, data) => {
+                const p = collRef.add(data).then((res) => {
+                    window.lastSuccessfulWriteTime = Date.now();
+                    return res;
+                });
+                return Promise.race([p, new Promise((_, reject) => setTimeout(() => {
+                    reject(new Error("addDoc timeout"));
+                }, 3500))]);
+            };
             window.serverTimestamp = () => firebase.firestore.FieldValue.serverTimestamp();
             window.collection = (db, coll) => db.collection(coll);
             window.increment = (n) => firebase.firestore.FieldValue.increment(n);
+            window.deleteField = () => firebase.firestore.FieldValue.delete();
             window.onSnapshot = (ref, cb, errCb) => {
                 console.log("📡 [FIREBASE] Setting up listener on:", ref.path);
-                return ref.onSnapshot(cb, (err) => {
+                return ref.onSnapshot((snap) => {
+                    window.lastSuccessfulWriteTime = Date.now();
+                    cb(snap);
+                }, (err) => {
                     console.error("🛑 [FIREBASE] Listener Error:", err);
                     if (errCb) errCb(err);
                 });
             };
-            
+
             // Better Query helpers for Compat
-            window.query = (collRef) => collRef; // Compat uses chaining
-            window.where = (field, op, val) => { /* compat uses .where() directly */ };
-            window.getDocs = (q) => q.get();
+            // Emulate v9 query/where for v8 compat
+            window.where = (field, op, val) => { return { field, op, val }; };
+            window.query = (collRef, ...conditions) => {
+                let q = collRef;
+                for (const cond of conditions) {
+                    if (cond && cond.field) {
+                        q = q.where(cond.field, cond.op, cond.val);
+                    }
+                }
+                return q;
+            };
+            window.getDocs = (q, options = {}) => {
+                const p = q.get(options).then((res) => {
+                    window.lastSuccessfulWriteTime = Date.now();
+                    return res;
+                });
+                return Promise.race([p, new Promise((_, reject) => setTimeout(() => {
+                    console.warn("⚠️ [FIREBASE] getDocs timeout (offline/hanging), resolving early for UI.");
+                    reject(new Error("Network Timeout"));
+                }, 3500))]);
+            };
 
+            // 📝 Startup and Restart Logger
+            setTimeout(async () => {
+                try {
+                    let hwId = window.DEVICE_ID;
+                    if (!hwId && window.AndroidSerial && typeof window.AndroidSerial.getDeviceId === 'function') {
+                        hwId = window.AndroidSerial.getDeviceId().toLowerCase();
+                    }
+                    if (!hwId) return;
+
+                    const now = new Date();
+                    const hour = now.getHours();
+                    const min = now.getMinutes();
+
+                    let reason = localStorage.getItem('restart_reason') || 'normal_boot';
+                    localStorage.removeItem('restart_reason'); // clean up
+
+                    // If it is around 04:00 AM - 04:05 AM, it is most likely a daily maintenance boot
+                    if (hour === 4 && min <= 5) {
+                        reason = 'daily_maintenance';
+                    }
+
+                    let details = 'ตัวตู้ทำการเริ่มต้นระบบแอปพลิเคชันสำเร็จ (Application Startup / Booted Successfully)';
+                    if (reason === 'remote_reboot') {
+                        details = 'ตัวตู้ทำการรีสตาร์ทแอปพลิเคชันจากระยะไกลสำเร็จ (Remote Reboot Executed Successfully)';
+                    } else if (reason === 'daily_maintenance') {
+                        details = 'ระบบทำการรีสตาร์ทเพื่อบำรุงรักษาประจำวันสำเร็จ (04:00 AM Daily Maintenance Restart)';
+                    }
+
+                    const appVer = window.AndroidSerial && typeof window.AndroidSerial.getAppVersion === 'function'
+                        ? window.AndroidSerial.getAppVersion()
+                        : '1.38 (RESTORED)';
+
+                    await db.collection("machine_logs").add({
+                        machineId: hwId,
+                        machineName: hwId,
+                        status: 'app_start',
+                        timestamp: firebase.firestore.FieldValue.serverTimestamp(),
+                        details: `${details} [เวอร์ชัน ${appVer}]`
+                    });
+                    console.log("📝 [SYSTEM_LOG] Logged startup successfully with reason:", reason);
+                } catch (e) {
+                    console.error("🛑 [SYSTEM_LOG] Failed to log startup:", e);
+                }
+            }, 3000);
+
             console.log("🔥 [FIREBASE] Compat Initialized and Exported (V2.11)");
             if (window.AndroidSerial && window.AndroidSerial.jsLog) {
                 window.AndroidSerial.jsLog("🔥 [FIREBASE] Compat Initialized");
@@ -85,6 +188,19 @@
             }
         })();
 
+
+        window.getDocFromServerOrCache = async function (docRef, source) {
+            if (source === "cache") {
+                return await window.getDoc(docRef); // firebase getDoc tries cache/server automatically, but we can enforce it
+            }
+            // Strict timeout for slow server check
+            const serverPromise = window.getDoc(docRef);
+            const timeoutPromise = new Promise((_, reject) =>
+                setTimeout(() => reject(new Error("Timeout")), 4000)
+            );
+            return await Promise.race([serverPromise, timeoutPromise]).catch(() => window.getDoc(docRef));
+        };
+
         // 🛡️ PRE-DEFINE CORE FUNCTIONS TO PREVENT BUTTON CRASHES
         window.logToScreen = function (msg) {
             console.log(msg);
@@ -106,142 +222,71 @@
                 window.DEVICE_ID = "ERR-0000";
             }
         } // 🎯 FIX: Close the if (!window.DEVICE_ID) block here!
-        
+
         localStorage.setItem('DEVICE_ID', window.DEVICE_ID);
 
-        window.showScreen = function(name) {
-            try {
-                // 🎯 UPDATE HEADER: Always show Station ID
-                    const headerNames = document.querySelectorAll('.kiosk-brand-name');
-                    headerNames.forEach(el => {
-                        if (el.id === 'stationNameHeader') {
-                            el.textContent = (window.DEVICE_ID || 'AQUA-STATION').toUpperCase();
-                        }
-                    });
+        // 🛡️ HARD CAP: จ่ายน้ำได้ไม่เกิน 2.0 ลิตร/วัน/คน
+        const MAX_DAILY_QUOTA = 2.0; // 🛡️ Regular users strictly capped at 2.0L!
 
-                    const targetElement = document.getElementById('screen-' + name);
-                    if (targetElement) {
-                        document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
-                        targetElement.classList.add('active');
-                        window.logToScreen('✨ [SUCCESS] Screen active: screen-' + name);
+        window.isAppOnline = false; // 🛡️ Pessimistic boot: assume offline until network verify completes
 
-                    // 🚀 AGGRESSIVE CLEANUP ON EXIT
-                    if (name === 'standby' || name === 'choice' || name === 'thankyou' || name === 'choice') {
-                        console.log("🧹 [SYSTEM] Aggressive cleanup for screen:", name);
-                        window._isEnding = false; // 🔓 Reset guard for next session
-                        
-                        if (window._sessionUnsubscribe) {
-                            try { window._sessionUnsubscribe(); } catch(e){}
-                            window._sessionUnsubscribe = null;
-                        }
-                        
-                        window.currentToken = null;
-                        window.currentKioskToken = null;
-                        window.maxLiters = 0;
-                        window.currentUserName = '';
-                        
-                        // Reset Hardware Signals
-                        window.GLOBAL_STOP_SIGNAL = false;
-                        window.LOCKDOWN_MODE = false;
-                        
-                        if (window.dispenseSession) {
-                            window.dispenseSession.active = false;
-                            window.dispenseSession.paused = false;
-                            window.dispenseSession.targetReached = false;
-                            window.dispenseSession.accumulatedPulses = 0;
-                            window.dispenseSession.lastFlow = (window.telemetry && window.telemetry.flow) || 0;
-                        }
-
-                        if (typeof window.typePhone === 'function') {
-                            window.typePhone('clear');
-                            const disp = document.getElementById('phoneDisplay');
-                            if (disp) disp.textContent = "XXX-XXXX-X___"; 
-                        }
-                        
-                        // Hide Quota consistently
-                        const qEl = document.getElementById('dispense-quota-display');
-                        if (qEl) {
-                            qEl.style.display = 'none';
-                            qEl.textContent = '';
-                        }
-                    }
-
-                    // 🚀 POPULATE DISPENSE UI IMMEDIATELY
-                    if (name === 'dispense') {
-                        const nameEl = document.getElementById('dispense-user-name');
-                        const quotaEl = document.getElementById('dispense-quota-display');
-                        const leftEl = document.getElementById('dispenseLeft');
-                        const nowEl = document.getElementById('dispenseNow');
-                        const volEl = document.getElementById('dispenseLitersDisplay');
-                        const fillEl = document.getElementById('waterFill');
-                        
-                        if (nameEl) nameEl.textContent = window.currentUserName || 'สมาชิก';
-                        if (quotaEl) {
-                            quotaEl.style.display = 'none';
-                            quotaEl.textContent = '';
-                        }
-                        if (leftEl) leftEl.textContent = (window.maxLiters || 0).toFixed(2);
-                        
-                        // 🎯 RESET: Ensure values start from 0.00
-                        if (nowEl) nowEl.textContent = "0.00";
-                        if (volEl) volEl.textContent = "0.00";
-                        if (fillEl) fillEl.style.height = "0%";
-                        
-                        // Reset Global Animation States
-                        window._animLiters = 0;
-                        window._animLitersTarget = 0;
-                        window._animTargetVol = window.maxLiters || 0;
-                        window._isEnding = false; // 🔓 UNLOCK: Allow finishing this new session status x181
-                        if (window._animFrameId) {
-                            cancelAnimationFrame(window._animFrameId);
-                            window._animFrameId = null;
-                        }
-                        if (typeof dispenseSession !== 'undefined') {
-                            dispenseSession.accumulatedPulses = 0;
-                            dispenseSession.active = false;
-                        }
-                        const subEl = document.getElementById('dispense-status-sub');
-                        if (subEl) subEl.textContent = "กรุณากดเริ่มจ่ายน้ำเพื่อรับน้ำ";
-                    }
-
-                    // 🚀 RESET PHONE INPUT WHEN SHOWING PHONE SCREEN
-                    if (name === 'phone' && typeof window.typePhone === 'function') {
-                        window.typePhone('clear');
-                    }
-
-                    // 🚀 START FIREBASE SESSION IF GOING TO QR
-                    if (name === 'qrdisplay' && typeof window.createKioskSession === 'function') {
-                        window.createKioskSession();
-                    }
-                } else {
-                    window.logToScreen('🛑 [ERROR] Screen not found: screen-' + name);
+        // 🎵 ระบบสร้างเสียงปุ่มกด (ไม่ต้องโหลดไฟล์)
+        window._audioCtx = null;
+        window.KEYPAD_SOUND_TYPE = 'click'; // 🔴 เปลี่ยนเสียงได้: 'beep1' (เสียงบี๊บ), 'click' (เสียงคลิกมือถือ), 'drop' (เสียงหยดน้ำ)
+        window.playKeypadSound = function (type = window.KEYPAD_SOUND_TYPE) {
+            try {
+                if (!window._audioCtx) window._audioCtx = new (window.AudioContext || window.webkitAudioContext)();
+                if (window._audioCtx.state === 'suspended') window._audioCtx.resume();
+                const osc = window._audioCtx.createOscillator();
+                const gain = window._audioCtx.createGain();
+                osc.connect(gain);
+                gain.connect(window._audioCtx.destination);
+                if (type === 'beep1') {
+                    osc.type = 'sine'; osc.frequency.setValueAtTime(880, window._audioCtx.currentTime);
+                    gain.gain.setValueAtTime(0.3, window._audioCtx.currentTime); gain.gain.exponentialRampToValueAtTime(0.01, window._audioCtx.currentTime + 0.1);
+                    osc.start(); osc.stop(window._audioCtx.currentTime + 0.1);
+                } else if (type === 'click') {
+                    osc.type = 'square'; osc.frequency.setValueAtTime(150, window._audioCtx.currentTime);
+                    gain.gain.setValueAtTime(0.1, window._audioCtx.currentTime); gain.gain.exponentialRampToValueAtTime(0.01, window._audioCtx.currentTime + 0.05);
+                    osc.start(); osc.stop(window._audioCtx.currentTime + 0.05);
+                } else if (type === 'drop') {
+                    osc.type = 'sine'; osc.frequency.setValueAtTime(400, window._audioCtx.currentTime); osc.frequency.exponentialRampToValueAtTime(800, window._audioCtx.currentTime + 0.1);
+                    gain.gain.setValueAtTime(0.3, window._audioCtx.currentTime); gain.gain.exponentialRampToValueAtTime(0.01, window._audioCtx.currentTime + 0.15);
+                    osc.start(); osc.stop(window._audioCtx.currentTime + 0.15);
                 }
-            } catch (e) {
-                window.logToScreen('❌ [FATAL UI ERROR]: ' + e.message);
-                console.error(e);
-            }
+            } catch (e) { console.warn("AudioContext error:", e); }
         };
 
         // 📱 PHONE ENTRY LOGIC (GLOBAL SCOPE)
         window.currentPhone = "";
-        window.typePhone = function(val) {
+        window.typePhone = function (val) {
+            // 🔊 เล่นเสียงเวลากด (กรณี clear จะเล่นเสียงเมื่อมีการเคลียร์จริงๆ)
+            if (val !== 'clear' || window.currentPhone.length > 0) {
+                window.playKeypadSound();
+            }
+
             if (val === 'clear') window.currentPhone = "";
             else if (val === 'back') window.currentPhone = window.currentPhone.slice(0, -1);
             else if (window.currentPhone.length < 10) window.currentPhone += val;
 
-            let display = "";
-            for(let i=0; i<10; i++) {
-                if (i < window.currentPhone.length) {
-                    if (i < 7) display += "X";
-                    else display += window.currentPhone[i];
+            let display = window.currentPhone;
+            if (display.length > 3) display = display.slice(0, 3) + "-" + display.slice(3);
+            if (display.length > 8) display = display.slice(0, 8) + "-" + display.slice(8);
+            if (window.currentPhone === "") display = "กรุณากรอกเบอร์โทร";
+
+            const el = document.getElementById('phoneDisplay');
+            if (el) {
+                el.textContent = display;
+                if (window.currentPhone === "") {
+                    el.style.fontSize = '35px';
+                    el.style.color = '#94a3b8';
+                    el.style.letterSpacing = '1px';
                 } else {
-                    display += "_";
+                    el.style.fontSize = '50px';
+                    el.style.color = 'var(--blue-dark)';
+                    el.style.letterSpacing = '4px';
                 }
-                if (i === 2 || i === 6) display += "-";
             }
-            
-            const el = document.getElementById('phoneDisplay');
-            if (el) el.textContent = display;
 
             const btn = document.getElementById('phoneConfirmBtn');
             if (btn) {
@@ -255,8 +300,49 @@
             }
         };
 
-        window.confirmPhone = async function() {
+        // 🚀 AUTOMATIC USER QUOTA RESET FOR NEW DAY (Asia/Bangkok timezone)
+        window.ensureUserQuotaReset = async function (userDocId, userData) {
+            const todayStr = new Date().toLocaleDateString('en-US', { timeZone: 'Asia/Bangkok' });
+            if (userData.litersLeft !== undefined && userData.lastQuotaResetDate !== todayStr) {
+                logToScreen('🔄 วันใหม่แล้ว! เริ่มต้นการรีเซ็ตโควตารายวัน...');
+                let calculatedMax = 2.0;
+
+                if (userData.customQuota !== undefined && userData.customQuota !== null && userData.customQuota !== "") {
+                    calculatedMax = parseFloat(userData.customQuota);
+                } else {
+                    try {
+                        const configDoc = await window.getDoc(window.doc(window.db, "settings", "quota_config"));
+                        if (configDoc.exists) {
+                            const conf = configDoc.data();
+                            const isAreaMatch = (conf.inAreaSubdistricts || []).includes(userData.subdistrict) ||
+                                (conf.inAreaDistricts || []).includes(userData.district) ||
+                                (conf.inAreaProvinces || []).includes(userData.province);
+                            calculatedMax = isAreaMatch ? (conf.inAreaVol || 2.0) : (conf.outAreaVol || 2.0);
+                        }
+                    } catch (configErr) {
+                        console.error("Config fetch fail:", configErr);
+                    }
+                }
+
+                try {
+                    await window.updateDoc(window.doc(window.db, "users", userDocId), {
+                        litersLeft: calculatedMax,
+                        lastQuotaResetDate: todayStr
+                    });
+                    // ✅ No hard cap — use full litersLeft from DB
+                    userData.litersLeft = calculatedMax;
+                    userData.lastQuotaResetDate = todayStr;
+                    logToScreen('✅ รีเซ็ตโควตาของสมาชิกเรียบร้อย: ' + calculatedMax + ' ลิตร');
+                } catch (resetErr) {
+                    console.error("Failed to reset litersLeft:", resetErr);
+                }
+            }
+        };
+
+        window.confirmPhone = async function () {
+            if (window.preventSpam('phone-confirm', 1000)) return;
             logToScreen('🔍 ตรวจสอบเบอร์: ' + window.currentPhone);
+            showSearchLoading();
             const btn = document.getElementById('phoneConfirmBtn');
             if (btn) {
                 btn.textContent = "ค้นหา...";
@@ -264,8 +350,31 @@
             }
 
             try {
-                const snap = await window.collection(window.db, "users").where("phone", "==", window.currentPhone).get();
+                const snap = await (async () => {
+                    logToScreen("🌐 Querying server for latest quota (2.5s timeout)...");
+                    try {
+                        const q = window.query(window.collection(window.db, "users"), window.where("phone", "==", window.currentPhone));
+                        const serverPromise = window.getDocs(q); // v9 doesn't easily support source: "server" without getDocsFromServer
+
+                        const timeoutPromise = new Promise((_, reject) =>
+                            setTimeout(() => reject(new Error("Network Timeout")), 2500)
+                        );
+
+                        const serverSnap = await Promise.race([serverPromise, timeoutPromise]);
+                        if (!serverSnap.empty) {
+                            logToScreen("✅ User found on server with latest quota!");
+                            return serverSnap;
+                        }
+                    } catch (serverErr) {
+                        console.warn("Server lookup failed or timed out, falling back to local cache...", serverErr);
+                    }
+
+                    logToScreen("📡 Searching user in local offline cache...");
+                    const q = window.query(window.collection(window.db, "users"), window.where("phone", "==", window.currentPhone));
+                    return await window.getDocs(q, { source: 'cache' });
+                })();
                 if (snap.empty) {
+                    hideSearchLoading();
                     showCustomAlert("ไม่พบข้อมูลสมาชิก โปรดลงทะเบียนก่อน", "ไม่พบข้อมูล");
                     if (btn) { btn.textContent = "ยืนยัน ➔"; btn.style.pointerEvents = "auto"; }
                     return;
@@ -273,6 +382,9 @@
 
                 const userDoc = snap.docs[0];
                 const userData = userDoc.data();
+
+                // 🚀 Auto-reset user quota if it's a new day!
+                await window.ensureUserQuotaReset(userDoc.id, userData);
                 const userStatus = (userData.status || '').toLowerCase();
                 if (userStatus !== 'approved' && userStatus !== 'active') {
                     let displayStatus = userData.status || 'รอดำเนินการ';
@@ -281,47 +393,86 @@
                     } else if (displayStatus.toLowerCase() === 'blocked') {
                         displayStatus = 'ระงับการใช้งาน';
                     }
+                    hideSearchLoading();
                     showCustomAlert("สถานะสมาชิก: " + displayStatus, "รอการอนุมัติ");
                     if (btn) { btn.textContent = "ยืนยัน ➔"; btn.style.pointerEvents = "auto"; }
                     return;
                 }
 
-                // QUOTA CALC — คำนวณแบบหักโควตารายวันแบบไดนามิก (ป้องกันการเติมซ้ำเกิน 2L ใน 1 วัน)
-                let maxQuota = parseFloat(userData.litersLeft ?? userData.quota ?? userData.totalQuota ?? 2);
-                let isRealtimeBalance = (userData.litersLeft !== undefined && userData.litersLeft !== null && userData.litersLeft !== "");
+                // 🔒 SESSION CHECK: ตรวจสอบว่ามีค้างใช้งานตู้ไหนอยู่หรือไม่
+                if (userData.isDispensing) {
+                    const lockAge = Date.now() - (userData.lockTime || 0);
+                    const lockedBy = userData.lockedByMachine || 'Unknown';
+                    const currentDevice = typeof DEVICE_ID !== 'undefined' ? DEVICE_ID : 'Unknown_Current';
+                    if (lockAge < 3 * 60 * 1000 && lockedBy !== currentDevice) { // 3 นาที and NOT this machine
+                        hideSearchLoading();
+                        showCustomAlert('บัญชีนี้กำลังทำรายการอยู่ที่ตู้อื่น กรุณารอสักครู่', 'ไม่สามารถใช้งานได้');
+                        if (btn) { btn.textContent = "ยืนยัน ➔"; btn.style.pointerEvents = "auto"; }
+                        return;
+                    } else {
+                        console.log("🔓 [SESSION LOCK] Auto-unlocked expired session (older than 3 mins)");
+                    }
+                }
 
-                let actualUsedToday = 0;
-                if (!isRealtimeBalance) {
-                    try {
-                        const startOfToday = new Date();
-                        startOfToday.setHours(0,0,0,0);
-                        const qUsage = window.collection(window.db, "sessions")
-                            .where("userUid", "==", userDoc.id)
-                            .where("status", "==", "finished");
-                        const usageSnap = await qUsage.get();
-                        usageSnap.forEach(d => {
-                            const data = d.data();
-                            const fTime = data.finishedAt ? data.finishedAt.toDate() : (data.startedAt ? data.startedAt.toDate() : new Date());
-                            if (fTime >= startOfToday) {
-                                actualUsedToday += parseFloat(data.finalVol || data.selectedVol || data.vol || 0);
-                            }
-                        });
-                    } catch (usageErr) {
-                        console.error("Failed to calculate today's usage:", usageErr);
+
+
+                // QUOTA CALC — ใช้ litersLeft เป็นฐาน แล้วบวก extraQuota ทีหลังเสมอ (ป้องกัน Pre-Deduct bug)
+                let maxQuota = parseFloat(userData.quota ?? 2.0);
+
+                let isRealtimeBalance = false;
+                if (userData.litersLeft !== undefined && userData.litersLeft !== null && userData.litersLeft !== "") {
+                    maxQuota = parseFloat(userData.litersLeft);
+                    isRealtimeBalance = true;
+                }
+
+                // 💎 [EXTRA QUOTA INJECTION] บวก extraQuota หลัง litersLeft เสมอ (ป้องกัน Pre-Deduct ทับยอด)
+                if (userData.extraQuota !== undefined && userData.extraQuota !== null && userData.extraQuota !== "") {
+                    const extraVal = parseFloat(userData.extraQuota);
+                    if (!isNaN(extraVal) && extraVal > 0) {
+                        maxQuota += extraVal;
+                        console.log("💎 [KIOSK] Added extraQuota: +" + extraVal + "L, Total maxQuota = " + maxQuota);
                     }
                 }
 
-                window.maxLiters = isRealtimeBalance ? Math.max(0, maxQuota) : Math.max(0, maxQuota - actualUsedToday);
-                
+                let actualUsedToday = 0;
+                // 🚀 ULTRA-LIGHT OPTIMIZATION FOR 384kbps NETWORKS:
+                // We rely 100% on the atomic litersLeft stored on the user's document which is daily auto-reset.
+                // This completely bypasses the massive 'sessions' collection fetch that causes WebView timeouts and crashes on slow networks.
+                console.log("[QUOTA QUERY] Ultra-light quota lookup active. litersLeft=" + maxQuota);
+
+
+                // ✅ Smart cap: extraQuota users get full quota, regular users capped at MAX_DAILY_QUOTA
+                const _rawMaxLiters = isRealtimeBalance ? Math.max(0, maxQuota) : Math.max(0, maxQuota - actualUsedToday);
+                const _hasExtraPhone = userData.extraQuota && parseFloat(userData.extraQuota) > 0;
+                window.maxLiters = _hasExtraPhone ? _rawMaxLiters : Math.min(_rawMaxLiters, MAX_DAILY_QUOTA);
+
+                selectedLiters = window.maxLiters; // 🚀 Sync selectedLiters with remaining quota immediately!
+
+                window.currentUserId = userDoc.id;
+                window.originalLitersLeft = parseFloat(userData.litersLeft ?? 2.0);
+                window.originalExtraQuota = parseFloat(userData.extraQuota ?? 0.0);
+
+                // 🛡️ SESSION LOCK: แขวนป้ายกำลังใช้งานแทนการหักโควตา 0 ลิตร
+                try {
+                    window.updateDoc(window.doc(window.db, "users", userDoc.id), {
+                        isDispensing: true,
+                        lockTime: Date.now(),
+                        lockedByMachine: typeof DEVICE_ID !== 'undefined' ? DEVICE_ID : 'Unknown'
+                    }).catch(e => console.error("Lock sync delayed", e));
+                    console.log("🔒 [SESSION LOCK] Phone: Locked session for " + userDoc.id);
+                } catch (lockErr) {
+                    console.error("🔒 [SESSION LOCK] Phone failed:", lockErr);
+                }
+
                 logToScreen(`<div style="color:#00f2fe; border:1px solid #00f2fe; padding:4px; margin:3px 0; font-size:11px;">
                     <b>📊 QUOTA DEBUG:</b><br>
                     - litersLeft (DB): ${window.maxLiters.toFixed(4)}L<br>
                     - <b>Remaining: ${window.maxLiters.toFixed(2)}L</b>
                 </div>`);
                 window.currentUserName = userData.name || 'สมาชิก';
-                
-                
+
                 if (window.maxLiters <= 0) {
+                    hideSearchLoading();
                     showCustomAlert("โควตาไม่เพียงพอ (คงเหลือ " + window.maxLiters.toFixed(2) + " ลิตร)", "โควตาหมด");
                     if (btn) { btn.textContent = "ยืนยัน ➔"; btn.style.pointerEvents = "auto"; }
                     return;
@@ -331,31 +482,36 @@
                 await window.setDoc(window.doc(window.db, "sessions", id), {
                     id: id, kioskId: DEVICE_ID, status: 'confirmed', userUid: userDoc.id, userName: userData.name || 'User', vol: window.maxLiters, createdAt: window.serverTimestamp()
                 });
-                
+
                 window.currentToken = id;
                 window.currentKioskToken = id;
-                
+
                 if (window._sessionUnsubscribe) window._sessionUnsubscribe();
                 window._sessionUnsubscribe = window.onSnapshot(window.doc(window.db, "sessions", id), (snap) => {
-                    if (snap && snap.exists && snap.data().status === 'finished') window.showScreen('thankyou');
+                    if (snap && snap.exists && snap.data().status === 'finished') {
+                        window.showScreen('thankyou');
+                        if (typeof startResetCountdown === 'function') startResetCountdown();
+                    }
                 });
 
+                // 🚀 HIDE OVERLAY ON SUCCESS!
+                hideSearchLoading();
                 window.showScreen('dispense');
                 window._isEnding = false; // 🔓 GUARANTEE: Allow endSession for this new session
-                
+
                 // 🚀 IMMEDIATE UI SYNC: Ensure 0.00 shows up right away
                 if (typeof updateDispenseScreen === 'function') {
                     updateDispenseScreen(0, window.maxLiters, 0);
                 }
             } catch (e) {
+                hideSearchLoading();
                 logToScreen('❌ Error: ' + e.message);
                 if (btn) { btn.textContent = "ยืนยัน ➔"; btn.style.pointerEvents = "auto"; }
             }
         };
     </script>
-    <link rel="preconnect" href="https://fonts.googleapis.com" />
-    <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700;800;900&display=swap"
-        rel="stylesheet" />
+    <!-- Disabled Google Fonts to prevent startup white screen -->
+    <!-- Disabled Google Fonts to prevent startup white screen -->
     <style>
         :root {
             --blue-dark: #0a4fa3;
@@ -379,69 +535,75 @@
             overflow: hidden;
         }
 
-        /* OFFLINE OVERLAY */
-        #offlineOverlay {
+        /* 🛑 KIOSK SYSTEM OVERLAY (Unified for Network & Hardware Failures) */
+        /* 🛡️ BACKDROP: Separate full-screen div that HARD-BLOCKS all touch on Android */
+        #kioskSystemBackdrop {
             position: fixed;
             top: 0;
             left: 0;
             width: 100vw;
             height: 100vh;
-            background: rgba(0, 0, 0, 0.95);
+            background: rgba(0, 0, 0, 0.88);
+            z-index: 10001;
+            display: none;
+            pointer-events: all;
+            -webkit-pointer-events: all;
+            cursor: default;
+            touch-action: none;
+        }
+
+        #kioskSystemBackdrop.active {
+            display: block;
+        }
+
+        #kioskSystemOverlay {
+            position: fixed;
+            top: 50%;
+            left: 50%;
+            transform: translate(-50%, -50%) scale(0.9);
+            width: 85vw;
+            max-width: 500px;
+            height: auto;
+            background: rgba(15, 23, 42, 0.98);
+            border: 4px solid #ef4444;
+            border-radius: 30px;
+            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.9);
             color: white;
             display: none;
             flex-direction: column;
+            z-index: 10002;
             align-items: center;
             justify-content: center;
-            z-index: 10001;
             opacity: 0;
-            pointer-events: none;
-            transition: opacity 0.5s ease;
+            pointer-events: all;
+            -webkit-pointer-events: all;
+            touch-action: none;
+            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
             text-align: center;
-            padding: 20px;
+            padding: 40px 20px;
         }
 
-        #offlineOverlay.active {
+        #kioskSystemOverlay.active {
             display: flex;
             opacity: 1;
             pointer-events: all;
+            -webkit-pointer-events: all;
+            transform: translate(-50%, -50%) scale(1);
         }
 
-        .offline-icon {
+        .kiosk-system-icon {
             font-size: 80px;
             margin-bottom: 20px;
             color: var(--red);
         }
 
-        #maintenanceOverlay {
-            position: fixed;
-            top: 0;
-            left: 0;
-            width: 100vw;
-            height: 100vh;
-            background: rgba(139, 0, 0, 0.98);
-            color: white;
-            display: none;
-            flex-direction: column;
-            align-items: center;
-            justify-content: center;
-            z-index: 10002;
-            text-align: center;
-            padding: 20px;
-        }
-
-        .maintenance-icon {
-            font-size: 100px;
-            margin-bottom: 20px;
-            color: #f1c40f;
-        }
-
-        .offline-title {
+        .kiosk-system-title {
             font-size: 36px;
             font-weight: 800;
             margin-bottom: 10px;
         }
 
-        .offline-desc {
+        .kiosk-system-desc {
             font-size: 20px;
             opacity: 0.7;
         }
@@ -452,6 +614,27 @@
             margin: 0;
             padding: 0;
             box-sizing: border-box;
+            -webkit-user-select: none;
+            -moz-user-select: none;
+            -ms-user-select: none;
+            user-select: none;
+            -webkit-user-drag: none;
+        }
+
+        input,
+        textarea,
+        [contenteditable="true"] {
+            -webkit-user-select: text;
+            -moz-user-select: text;
+            -ms-user-select: text;
+            user-select: text;
+        }
+
+        button,
+        a,
+        .vol-btn,
+        [role="button"] {
+            touch-action: manipulation;
         }
 
         body {
@@ -606,7 +789,7 @@
             font-size: 11px;
             padding: 8px;
             overflow: hidden;
-            z-index: 9999;
+            z-index: 10002;
             border-top: 2px solid #00f2fe;
             box-sizing: border-box;
             flex-direction: column;
@@ -617,12 +800,14 @@
             flex: 1;
             padding: 30px 60px;
             display: flex !important;
-            flex-direction: column !important; /* 🚀 FORCE VERTICAL STACK */
+            flex-direction: column !important;
+            /* 🚀 FORCE VERTICAL STACK */
             align-items: center;
             justify-content: center;
             overflow: hidden;
             position: relative;
-            gap: 40px; /* 🚀 ADD SPACING BETWEEN BUTTONS */
+            gap: 40px;
+            /* 🚀 ADD SPACING BETWEEN BUTTONS */
         }
 
         .kiosk-wave {
@@ -1010,8 +1195,7 @@
             left: 0;
             right: 0;
             background: linear-gradient(180deg, var(--sky) 0%, var(--cyan) 40%, var(--blue) 100%);
-            transition: height 0.8s cubic-bezier(0.4, 0, 0.2, 1);
-            /* Smooth water movement */
+            /* Removed CSS transition on height because JS requestAnimationFrame handles it */
             height: 0%;
         }
 
@@ -1242,6 +1426,18 @@
             <script>
                 console.log('🛡️ [ANTIVIRUS] Version V121.26 Loaded. Purging ghosts...');
 
+                // 🛡️ SPAM PREVENTION (DEBOUNCE / CLICK-LOCK)
+                window._clickLocks = {};
+                window.preventSpam = function (key, delay = 1000) {
+                    const now = Date.now();
+                    if (window._clickLocks[key] && (now - window._clickLocks[key] < delay)) {
+                        console.warn(`[SPAM PREVENTION] Blocked click for "${key}" to prevent double-tap.`);
+                        return true; // Blocked
+                    }
+                    window._clickLocks[key] = now;
+                    return false; // Allowed
+                };
+
                 // 🧹 PURGE SERVICE WORKERS
                 if ('serviceWorker' in navigator) {
                     navigator.serviceWorker.getRegistrations().then(function (registrations) {
@@ -1253,10 +1449,28 @@
                 }
 
                 function handleStandbyClick(e) {
+                    const overlay = document.getElementById('kioskSystemOverlay');
+
+                    if (overlay && overlay.classList.contains('active')) return;
+                    if (window.isLowWater === true) {
+                        console.warn("[UI] Blocked standby click because water is empty");
+                        if (overlay) {
+                            overlay.classList.add('active');
+                            overlay.style.display = 'flex';
+                        }
+                        return;
+                    }
+                    if (window.isAppOnline === false) {
+                        console.warn("[UI] Blocked standby click because app is offline");
+                        if (typeof updateOnlineUI === 'function') // updateOnlineUI(false); removed from Firebase
+                            return;
+                    }
+                    if (window.preventSpam('standby-click', 1000)) return;
                     if (e) {
                         if (e.type === 'touchstart') e.preventDefault();
                         e.stopPropagation();
                     }
+                    if (typeof window.resetIdle === 'function') window.resetIdle(); // 🐛 FIX: Ensure idle timer resets since stopPropagation blocks the global listener
                     playVoice('audio/confirm.mp3');
                     showScreen('choice');
                 }
@@ -1288,13 +1502,123 @@
                     console.log("📺 LOG:", msg);
                 };
 
-                function flowSetBytes(pulses) { return [pulses & 0xFF, (pulses >> 8) & 0xFF]; }
+                function flowSetBytes(pulses) { return [(pulses >> 8) & 0xFF, pulses & 0xFF]; }
 
                 async function showScreen(name) {
                     console.log('🎬 [UI] showScreen:', name);
+                    // 🛡️ LOW WATER BLOCK: If water is empty, do not allow transitioning to any screen except standby!
+                    if (window.isLowWater === true && name !== 'standby') {
+                        console.warn("🚫 [UI] showScreen blocked transition to " + name + " because water is empty.");
+                        const overlay = document.getElementById('kioskSystemOverlay');
+                        if (overlay) {
+                            overlay.classList.add('active');
+                            overlay.style.display = 'flex';
+                        }
+                        return;
+                    }
+                    // 🛡️ STRICT OFFLINE LOCK: If app is offline, do NOT allow transitioning to any screen!
+                    // Keep the user strictly locked on the current screen under the offline overlay.
+                    if (window.isAppOnline === false) {
+                        console.warn("🚫 [UI] showScreen detected app is OFFLINE! (Warning only, transition allowed)");
+                        // Ensure overlay is strictly visible
+                        const overlay = document.getElementById('kioskSystemOverlay');
+                        if (overlay) {
+                            overlay.classList.add('active');
+                            overlay.style.display = 'flex';
+                        }
+                        // ⚠️ REMOVED: return; (Do NOT block UI transition even if offline to prevent white screen bugs)
+                    }
+                    if (name === 'standby' || name === 'choice' || name === 'thankyou') {
+                        console.log("🧹 [SYSTEM] Aggressive cleanup for screen:", name);
+                        window._isEnding = false; // 🔓 Reset guard for next session
+
+                        if (window._sessionUnsubscribe) {
+                            try { window._sessionUnsubscribe(); } catch (e) { }
+                            window._sessionUnsubscribe = null;
+                        }
+
+                        window.currentToken = null;
+                        window.currentKioskToken = null;
+                        window.maxLiters = 0;
+                        try {
+                            if (window.Capacitor && window.Capacitor.Plugins.CapacitorUpdater) {
+                                window.Capacitor.Plugins.CapacitorUpdater.notifyAppReady();
+                            }
+                        } catch (e) { }
+                        window.currentUserName = '';
+
+                        // Reset Hardware Signals
+                        window.GLOBAL_STOP_SIGNAL = false;
+                        window.LOCKDOWN_MODE = false;
+
+                        if (window.dispenseSession) {
+                            window.dispenseSession.active = false;
+                            window._activeFlowStartTime = null;
+                            window.dispenseSession.paused = false;
+                            window.dispenseSession.targetReached = false;
+                            window.dispenseSession.accumulatedPulses = 0;
+                            window.dispenseSession.lastFlow = (window.telemetry && window.telemetry.flow) || 0;
+                        }
+
+                        if (typeof window.typePhone === 'function') {
+                            window.typePhone('clear');
+                            const disp = document.getElementById('phoneDisplay');
+                            if (disp) {
+                                disp.textContent = "กรุณากรอกเบอร์โทร";
+                                disp.style.fontSize = '35px';
+                                disp.style.color = '#94a3b8';
+                                disp.style.letterSpacing = '1px';
+                            }
+                        }
+
+                        // Hide Quota consistently
+                        const qEl = document.getElementById('dispense-quota-display');
+                        if (qEl) {
+                            qEl.style.display = 'none';
+                            qEl.textContent = '';
+                        }
+                    }
+                    if (name === 'standby') {
+                        // 🛡️ ULTRA-LIGHT RECOVERY: If there was a pending active session that was cancelled or timed out, restore quota directly and immediately (slow network resilient)!
+                        if (window.currentUserId && window.originalLitersLeft !== undefined) {
+                            const pendingToken = window.currentToken || window.currentKioskToken;
+                            if (pendingToken) {
+                                console.log("🛡️ [ULTRA-LIGHT RECOVERY] Restoring litersLeft to " + window.originalLitersLeft + " for user " + window.currentUserId);
+                                try {
+                                    // 1. Direct ultra-lightweight restore to user doc without heavy queries
+                                    // 🛡️ RECOVERY FIX: คืน extraQuota ด้วย ไม่ใช่แค่ litersLeft
+                                    const recoveryUpdate = {
+                                        isDispensing: window.deleteField(),
+                                        lockTime: window.deleteField(),
+                                        lockedByMachine: window.deleteField()
+                                    };
+                                    await window.updateDoc(window.doc(window.db, "users", window.currentUserId), recoveryUpdate);
+                                    console.log("🔓 [SESSION UNLOCK] RECOVERY: Session lock released for user", window.currentUserId);
+                                    // 2. Mark session as finished with 0 volume so LIFF knows
+                                    await window.updateDoc(window.doc(window.db, "sessions", pendingToken), {
+                                        status: 'finished',
+                                        finalVol: 0,
+                                        finishedAt: window.serverTimestamp()
+                                    });
+                                } catch (err) {
+                                    console.error("[RECOVERY] Direct restore failed:", err);
+                                }
+                            }
+                        }
+                        window.currentToken = null;
+                        window.currentKioskToken = null;
+                        window.currentUserId = null;
+                        window.originalLitersLeft = undefined;
+                        window.originalExtraQuota = undefined; // 🛡️ RECOVERY FIX: clear state
+                    }
                     if (window.AndroidSerial && window.AndroidSerial.jsLog) {
                         window.AndroidSerial.jsLog('🎬 [UI] Transition to: ' + name);
                     }
+                    if (name !== 'thankyou' && window._resetCountdownTimer) {
+                        console.log("🧹 [UI] Clearing pending reset countdown timer");
+                        clearInterval(window._resetCountdownTimer);
+                        window._resetCountdownTimer = null;
+                    }
                     currentScreenName = name; // ✅ UPDATE GLOBAL SCREEN STATE
                     const targetId = 'screen-' + name;
                     const targetElement = document.getElementById(targetId);
@@ -1315,18 +1639,9 @@
                     targetElement.style.display = 'flex';
 
                     // 2. Overlay Management
-                    const overlay = document.getElementById('offlineOverlay');
-                    const maint = document.getElementById('maintenanceOverlay');
-                    if (overlay) {
-                        if (name === 'standby') {
-                            if (typeof navigator !== 'undefined' && !navigator.onLine) {
-                                overlay.classList.add('active');
-                                overlay.style.display = 'flex';
-                            }
-                        } else {
-                            overlay.classList.remove('active');
-                            overlay.style.display = 'none';
-                        }
+                    // System overlay logic purely handled by executeOnlineUI and Serial Telemetry
+                    if (typeof window.isAppOnline !== 'undefined' && typeof window.executeOnlineUI === 'function') {
+                        window.executeOnlineUI(window.isAppOnline);
                     }
 
                     // 3. Play AI Voice
@@ -1341,8 +1656,49 @@
                     if (name === 'dispense') {
                         if (typeof refreshDispenseButtons === 'function') refreshDispenseButtons();
                         if (typeof forceResetBoard === 'function') forceResetBoard();
+
+                        const nameEl = document.getElementById('dispense-user-name');
+                        const quotaEl = document.getElementById('dispense-quota-display');
+                        const leftEl = document.getElementById('dispenseLeft');
+                        const nowEl = document.getElementById('dispenseNow');
+                        const volEl = document.getElementById('dispenseLitersDisplay');
+                        const fillEl = document.getElementById('waterFill');
+
+                        if (nameEl) nameEl.textContent = window.currentUserName || 'สมาชิก';
+                        if (quotaEl) {
+                            quotaEl.style.display = 'none';
+                            quotaEl.textContent = '';
+                        }
+                        if (leftEl) leftEl.textContent = (window.maxLiters || 0).toFixed(2);
+
+                        // 🎯 RESET: Ensure values start from 0.00
+                        if (nowEl) nowEl.textContent = "0.00";
+                        if (volEl) volEl.textContent = "0.00";
+                        if (fillEl) fillEl.style.height = "0%";
+
+                        // Reset Global Animation States
+                        window._animLiters = 0;
+                        window._animLitersTarget = 0;
+                        window._animTargetVol = window.maxLiters || 0;
+                        window._isEnding = false; // 🔓 UNLOCK: Allow finishing this new session status x181
+                        if (window._animFrameId) {
+                            cancelAnimationFrame(window._animFrameId);
+                            window._animFrameId = null;
+                        }
+                        if (typeof dispenseSession !== 'undefined') {
+                            dispenseSession.accumulatedPulses = 0;
+                            dispenseSession.active = false;
+                        }
+                        const subEl = document.getElementById('dispense-status-sub');
+                        if (subEl) subEl.textContent = "กรุณากดเริ่มจ่ายน้ำเพื่อรับน้ำ";
                     }
 
+                    if (name === 'phone') {
+                        if (typeof window.typePhone === 'function') {
+                            window.typePhone('clear');
+                        }
+                    }
+
                     if (name === 'qrdisplay') {
                         try {
                             const qrImg = document.getElementById('kioskQrImage');
@@ -1384,12 +1740,11 @@
                                     if (data.status !== lastObservedStatus) {
                                         logToScreen(`🔔 [SYNC] Update: <span style="color:#00f2fe">${data.status}</span>`);
                                         lastObservedStatus = data.status;
-                                        
+
                                         if (data.status === 'confirmed') {
-                                            logToScreen('📱 [LIFF] สแกนสำเร็จ! กำลังไปหน้าจ่ายน้ำ...');
-                                            window.currentUserName = data.userName || 'สมาชิก';
-                                            window.maxLiters = parseFloat(data.vol || data.selectedVol || 1);
-                                            window.showScreen('dispense');
+                                            logToScreen('📱 [LIFF] สแกนสำเร็จ! รอตรวจสอบข้อมูลสมาชิก...');
+                                            // 🛡️ SECURITY: Do NOT navigate to dispense here
+                                            // The secure listener (waitForScanFirebase) handles full validation
                                         }
                                     }
                                 } else {
@@ -1423,12 +1778,6 @@
                             window._sessionUnsubscribe = window.waitForScanFirebase(token, async (data) => {
                                 console.log(`[FIREBASE] 🔔 Status: ${data.status} | vol: ${data.vol} | selectedVol: ${data.selectedVol} | Full:`, JSON.stringify(data));
 
-                                if (data.telemetry && data.telemetry.dataBytes && data.telemetry.dataBytes[0] === 1) {
-                                    document.getElementById('maintenanceOverlay').style.display = 'flex';
-                                } else {
-                                    document.getElementById('maintenanceOverlay').style.display = 'none';
-                                }
-
                                 if (data.status === 'scanned') {
                                     // Show waiting screen when QR is scanned on mobile
                                     if (currentScreenName === 'qrdisplay') {
@@ -1450,10 +1799,25 @@
                                     // 🛡️ OVERRIDE VERCEL CACHE BUG:
                                     // Because the Vercel LIFF app is aggressively cached and sending double-deducted 765.03,
                                     // we must ignore it and read the REAL litersLeft from the user's DB directly, just like confirmPhone does!
-                                    if (data.userUid) {
+                                    const userUidToUse = data.userUid || data.userId || data.user_id;
+                                    let primaryData = {}; // Fix for ReferenceError: primaryData is not defined
+                                    let ud = {}; // Initialize before try block to prevent ReferenceError
+                                    if (userUidToUse) {
                                         try {
-                                            const uSnap = await window.getDoc(window.doc(window.db, "users", data.userUid));
-                                            
+                                            const uSnap = await (async () => {
+                                                try {
+                                                    // Try offline cache first
+                                                    const cacheDoc = await window.getDocFromServerOrCache(window.doc(window.db, "users", userUidToUse), "cache");
+                                                    if (cacheDoc && cacheDoc.exists) {
+                                                        console.log("✅ QR User verified via local cache!");
+                                                        return cacheDoc;
+                                                    }
+                                                } catch (e) { }
+
+                                                // Server fallback
+                                                return await window.getDocFromServerOrCache(window.doc(window.db, "users", userUidToUse), "server");
+                                            })();
+
                                             // 🛡️ [NEW] STRICT REGISTRATION CHECK
                                             if (!uSnap.exists) {
                                                 logToScreen("❌ UNREGISTERED USER: Blocked.");
@@ -1463,6 +1827,9 @@
                                             }
 
                                             const ud = uSnap.data();
+
+                                            // 🚀 Auto-reset user quota if it's a new day!
+                                            await window.ensureUserQuotaReset(uSnap.id, ud);
                                             // 🛡️ [NEW] STATUS CHECK
                                             const userStatus = (ud.status || '').toLowerCase();
                                             if (userStatus !== 'approved' && userStatus !== 'active') {
@@ -1477,20 +1844,87 @@
                                                 showScreen('standby');
                                                 return;
                                             }
-                                            
-                                            // Check for cross-referenced phone account first!
-                                            let primaryData = ud;
-                                            if (ud.phone) {
-                                                const phoneSnap = await window.collection(window.db, "users").where("phone", "==", ud.phone).get();
-                                                if (!phoneSnap.empty) {
-                                                    primaryData = phoneSnap.docs[0].data();
+
+                                            // 🚀 Cross-Reference: หา Phone Profile เพื่อดึง extraQuota (รองรับเน็ตช้า < 384 Kbps)
+                                            primaryData = ud;
+
+                                            // กลยุทธ์ 1 (0ms): ถ้า LINE profile มี extraQuota อยู่แล้ว → ไม่ต้อง cross-ref เลย!
+                                            const hasExtraQuotaInline = (ud.extraQuota !== undefined && ud.extraQuota !== null && ud.extraQuota !== "" && parseFloat(ud.extraQuota) > 0);
+
+                                            if (!hasExtraQuotaInline && ud.phone) {
+                                                // 🧹 Sanitize phone: ตัดอักขระพิเศษ, แปลง +66/66 → 0
+                                                let cleanPhone = ud.phone.replace(/[^0-9]/g, "");
+                                                if (cleanPhone.startsWith("66")) {
+                                                    cleanPhone = "0" + cleanPhone.slice(2);
                                                 }
+
+                                                try {
+                                                    // กลยุทธ์ 2: ค้นจาก local offline cache ก่อน (เร็วมาก, ไม่ใช้เน็ต)
+                                                    const qPhone = window.query(window.collection(window.db, "users"), window.where("phone", "==", cleanPhone));
+                                                    const cachePhoneSnap = await window.getDocs(qPhone);
+                                                    if (!cachePhoneSnap.empty) {
+                                                        primaryData = cachePhoneSnap.docs[0].data();
+                                                        console.log("✅ [QR] Phone cross-ref found in local cache!");
+                                                    } else {
+                                                        throw new Error("cache miss");
+                                                    }
+                                                } catch (cacheErr) {
+                                                    try {
+                                                        // กลยุทธ์ 3: Server query + timeout 4 วินาที (slow network safe)
+                                                        const qPhone = window.query(window.collection(window.db, "users"), window.where("phone", "==", cleanPhone));
+                                                        const serverPhonePromise = window.getDocs(qPhone);
+                                                        const timeoutPromise = new Promise((_, reject) =>
+                                                            setTimeout(() => reject(new Error("Cross-ref Timeout")), 4000)
+                                                        );
+                                                        const serverPhoneSnap = await Promise.race([serverPhonePromise, timeoutPromise]);
+                                                        if (!serverPhoneSnap.empty) {
+                                                            primaryData = serverPhoneSnap.docs[0].data();
+                                                            console.log("✅ [QR] Phone cross-ref found on server!");
+                                                        }
+                                                    } catch (serverErr) {
+                                                        console.warn("⚠️ [QR] Cross-ref failed (timeout/offline), using LINE profile data:", serverErr.message);
+                                                        // fallback: ใช้ ud เดิม (LINE profile) ซึ่งอาจมี extraQuota อยู่แล้ว
+                                                    }
+                                                }
+                                            } else if (hasExtraQuotaInline) {
+                                                console.log("⚡ [QR] extraQuota found directly in LINE profile. Skipping cross-ref for speed!");
                                             }
 
+                                            // คำนวณ maxQuota: ใช้ litersLeft เป็นฐาน แล้วบวก extraQuota ทีหลังเสมอ
+                                            let maxQuota = parseFloat(primaryData.quota ?? 2.0);
+
+                                            let isRealtimeBalance = false;
                                             if (primaryData.litersLeft !== undefined && primaryData.litersLeft !== null && primaryData.litersLeft !== "") {
-                                                remainingQuota = Math.max(0, parseFloat(primaryData.litersLeft));
-                                                console.log("💎 [KIOSK] Overriding session quota with DB Realtime Balance:", remainingQuota);
+                                                maxQuota = parseFloat(primaryData.litersLeft);
+                                                isRealtimeBalance = true;
                                             }
+
+                                            // 💎 [EXTRA QUOTA INJECTION] บวก extraQuota หลัง litersLeft เสมอ (เหมือนฝั่งเบอร์โทร)
+                                            if (primaryData.extraQuota !== undefined && primaryData.extraQuota !== null && primaryData.extraQuota !== "") {
+                                                const extra = parseFloat(primaryData.extraQuota);
+                                                if (!isNaN(extra) && extra > 0) {
+                                                    maxQuota += extra;
+                                                    console.log("💎 [QR KIOSK] Added extraQuota: +" + extra + "L, Total maxQuota = " + maxQuota);
+                                                }
+                                            }
+
+                                            // 🚀 ULTRA-LIGHT OPTIMIZATION FOR 384kbps NETWORKS (Applied to QR as well):
+                                            // We rely 100% on the atomic litersLeft stored on the user's document which is daily auto-reset.
+                                            // This completely bypasses the massive 'sessions' collection fetch that causes WebView timeouts and crashes on slow networks.
+                                            remainingQuota = Math.max(0, maxQuota);
+                                            console.log("💎 [KIOSK] QR Ultra-light quota lookup active. remainingQuota=" + remainingQuota);
+
+                                            // 🛡️ FIX: Use real name from DB, not from LIFF cache
+                                            const realUserName = ud.name || primaryData.name || data.userName || ud.phone || ('สมาชิก #' + userUidToUse.substr(-4));
+                                            window.currentUserName = realUserName;
+                                            data.userName = realUserName; // Override for UI display below
+                                            // Write real name back to session document
+                                            try {
+                                                await window.updateDoc(window.doc(window.db, "sessions", token), { userName: realUserName });
+                                                console.log("[SYNC] Updated session userName to: " + realUserName);
+                                            } catch (nameErr) {
+                                                console.error("[SYNC] Failed to update session userName:", nameErr);
+                                            }
                                         } catch (e) {
                                             console.error("Failed to lookup real user quota:", e);
                                         }
@@ -1502,13 +1936,16 @@
                                         return;
                                     }
 
-                                    console.log(`✅ [QR CONFIRMED] Target: ${targetVol}L | Remaining: ${remainingQuota}L`);
+                                    // ✅ remainingQuota is already correctly pulled from Firestore litersLeft above.
+                                    // No additional hard cap needed — the DYNAMIC HARD CAP caused 2L always when primaryData={}.
 
+                                    console.log(`✅ [QR CONFIRMED] Target: ${targetVol}L | Remaining: ${remainingQuota}L (cap: ${MAX_DAILY_QUOTA}L)`);
+
                                     // 🚫 Block if quota is empty or target exceeds remaining (Strict Check)
                                     if (remainingQuota <= 0) {
                                         logToScreen("❌ QUOTA EXCEEDED: Cannot dispense.");
                                         alert('โควตาน้ำของคุณไม่เพียงพอ (คงเหลือ 0.00 ลิตร)');
-                                        
+
                                         const quotaEl = document.getElementById('dispense-quota-display');
                                         if (quotaEl) {
                                             quotaEl.style.display = 'none';
@@ -1518,7 +1955,9 @@
                                         return;
                                     }
 
-                                    const finalDispenseVol = Math.min(targetVol, remainingQuota);
+                                    const _hasExtraFinal = primaryData && parseFloat(primaryData.extraQuota) > 0;
+                                    const _qrCap = _hasExtraFinal ? remainingQuota : Math.min(remainingQuota, MAX_DAILY_QUOTA);
+                                    const finalDispenseVol = Math.min(targetVol, _qrCap);
                                     if (finalDispenseVol <= 0) {
                                         logToScreen("❌ INVALID VOLUME: 0L requested.");
                                         showScreen('standby');
@@ -1547,9 +1986,28 @@
                                         window._sessionUnsubscribe = null;
                                     }
 
-                                    // ✅ Set maxLiters = actual remaining quota so dispense screen shows 778.20 not 2
-                                    window.maxLiters = remainingQuota;
+                                    // ✅ Smart cap: extraQuota users get full quota, regular users capped at MAX_DAILY_QUOTA
+                                    const _hasExtraQR = primaryData && parseFloat(primaryData.extraQuota) > 0;
+                                    window.maxLiters = _hasExtraQR ? remainingQuota : Math.min(remainingQuota, MAX_DAILY_QUOTA);
+                                    selectedLiters = remainingQuota; // 🚀 Sync selectedLiters with remaining quota immediately!
 
+                                    window.currentUserId = data.userUid;
+                                    window.originalLitersLeft = parseFloat(primaryData.litersLeft ?? 2.0);
+                                    window.originalExtraQuota = parseFloat(primaryData.extraQuota ?? 0.0);
+
+                                    // 🛡️ SESSION LOCK: แขวนป้ายกำลังใช้งานแทนการหักโควตา 0 ลิตร
+                                    if (userUidToUse && userUidToUse !== 'anonymous') {
+                                        try {
+                                            await window.updateDoc(window.doc(window.db, "users", userUidToUse), {
+                                                isDispensing: true,
+                                                lockTime: Date.now()
+                                            });
+                                            console.log("🔒 [SESSION LOCK] QR: Locked session for " + userUidToUse);
+                                        } catch (lockErr) {
+                                            console.error("🔒 [SESSION LOCK] QR failed:", lockErr);
+                                        }
+                                    }
+
                                     // Proceed to dispense the final safe volume
                                     if (typeof selectVolume === 'function') selectVolume(finalDispenseVol);
                                 }
@@ -1564,40 +2022,53 @@
                 }
             </script>
 
-            <div id="offlineOverlay">
-                <div class="offline-icon">📡</div>
-                <h1 style="font-size: 48px; margin: 0;">OFFLINE</h1>
-                <p style="font-size: 24px; opacity: 0.8;">เครื่องขาดการเชื่อมต่อกับระบบคลาวด์</p>
-                <p style="font-size: 18px; margin-top: 10px; color: #ff9999;">กรุณาแจ้งเจ้าหน้าที่เพื่อทำการตรวจสอบ</p>
-            </div>
 
-            <!-- MAINTENANCE OVERLAY -->
-            <div id="maintenanceOverlay">
-                <div class="maintenance-icon">⚠️</div>
-                <h1 style="font-size: 48px; margin: 0;">งดบริการชั่วคราว</h1>
-                <p style="font-size: 24px; opacity: 0.8;">ขออภัยในความไม่สะดวก เครื่องอยู่ในระหว่างการปรับปรุง</p>
-                <div style="margin-top: 30px; font-family: monospace; font-size: 14px; opacity: 0.5;">[ERROR_CODE:
-                    BOARD_FAIL_F]</div>
-            </div>
 
+
             <div onclick="toggleDebug()"
                 style="position:fixed; top:0; left:0; width:250px; height:250px; z-index:1000000; cursor:pointer; background: transparent;">
             </div>
             <div class="screen active" id="screen-standby" onclick="handleStandbyClick(event)"
                 ontouchstart="handleStandbyClick(event)"
                 style="background: #f8fafc; display:flex; flex-direction:column; height:100%; overflow:hidden; font-family:'Sarabun', sans-serif; cursor:pointer;">
-                <div
-                    style="background: linear-gradient(180deg, #3b82f6 0%, #1d4ed8 100%); padding: 30px 40px; display: flex; justify-content: space-between; align-items: center; color: white;">
-                    <div style="text-align: left; flex: 1;">
-                        <div style="font-size: 42px; font-weight: 900; letter-spacing: -1px; line-height: 1.2;">
-                            ตู้น้ำดื่มสะอาด 24 ชม.</div>
+                <div class="kiosk-header">
+                    <div>
+                        <div class="kiosk-brand-name stationNameHeader">ตู้กดน้ำสะอาด</div>
+                        <div style="display:flex; gap:12px; margin-top:4px;">
+                            <div
+                                style="color:rgba(255,255,255,0.7); font-size:16px; display:flex; align-items:center; gap:5px;">
+                                <span style="color:#00f2fe; font-weight:bold;">TDS IN:</span>
+                                <span class="live-tds-in" style="color:white; font-weight:900;">--</span>
+                            </div>
+                            <div
+                                style="color:rgba(255,255,255,0.7); font-size:16px; display:flex; align-items:center; gap:5px;">
+                                <span style="color:#2ecc71; font-weight:bold;">TDS OUT:</span>
+                                <span class="live-tds-out" style="color:white; font-weight:900;">--</span>
+                            </div>
+                        </div>
                     </div>
-                    <div style="text-align: right; margin-left: 20px; display: flex; flex-direction: column; align-items: flex-end;">
-                        <div style="display: flex; align-items: center; gap: 10px;">
-                            <div id="net-status-icon" style="width: 20px; height: 20px; background: #10b981; border-radius: 50%; border: 3px solid rgba(255,255,255,0.3); box-shadow: 0 0 10px rgba(16,185,129,0.5);"></div>
-                            <div class="clock-display" style="font-size: 56px; font-weight: 900; line-height: 1;">--:--</div>
+                    <div style="text-align: right;">
+                        <div style="display: flex; align-items: center; justify-content: flex-end; gap: 10px;">
+                            <div class="net-status-icon"
+                                style="display: flex; align-items: flex-end; gap: 4px; height: 28px; margin-bottom: 8px;">
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 7px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 14px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 21px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 28px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                            </div>
+                            <div class="clock-display"
+                                style="font-size: 56px; font-weight: 900; color: white; line-height: 1;">--:--</div>
                         </div>
-                        <div class="date-display" style="font-size: 20px; opacity: 0.9;">-- --- ----</div>
+                        <div class="date-display"
+                            style="font-size: 20px; color: rgba(255,255,255,0.8); margin-top: 5px;">-- --- ----</div>
                     </div>
                 </div>
 
@@ -1684,24 +2155,55 @@
                         style="width:100%; background: #2563eb; color:white; border:none; border-radius:999px; padding:35px 20px; font-size:48px; font-weight:900; box-shadow: 0 20px 50px rgba(37,99,235,0.4); pointer-events:none;">
                         แตะที่หน้าจอเพื่อทำรายการ
                     </button>
-                    <div style="text-align:center; font-size:22px; color:#64748b; margin-top:20px; font-weight:900; letter-spacing:1px;">
-                        ID: <span id="stationIdLabel">--</span> | Version App : 1.38 (RESTORED)</div>
+                    <div
+                        style="text-align:center; font-size:22px; color:#64748b; margin-top:20px; font-weight:900; letter-spacing:1px;">
+                        ID: <span id="stationIdLabel">--</span> | Version App : <span id="appVersionLabel">1.38
+                            (RESTORED)</span></div>
                 </div>
             </div>
 
             <!-- CHOICE SCREEN -->
             <div class="screen" id="screen-choice"
                 style="background: #f8fafc; display:flex; flex-direction:column; height:100%; overflow:hidden;">
-                <div
-                    style="background: linear-gradient(180deg, #3b82f6 0%, #1d4ed8 100%); padding: 30px 60px; display: flex; justify-content: space-between; align-items: center; color: white;">
-                    <div style="text-align: left;">
-                        <div style="font-size: 50px; font-weight: 900;">กรุณาเลือกบริการ</div>
-                    </div>
-                    <div style="text-align: right;">
-                        <div class="clock-display" style="font-size: 56px; font-weight: 900; line-height: 1;">--:--
+                <div class="kiosk-header">
+                    <div>
+                        <div class="kiosk-brand-name stationNameHeader">ตู้กดน้ำสะอาด</div>
+                        <div style="display:flex; gap:12px; margin-top:4px;">
+                            <div
+                                style="color:rgba(255,255,255,0.7); font-size:16px; display:flex; align-items:center; gap:5px;">
+                                <span style="color:#00f2fe; font-weight:bold;">TDS IN:</span>
+                                <span class="live-tds-in" style="color:white; font-weight:900;">--</span>
+                            </div>
+                            <div
+                                style="color:rgba(255,255,255,0.7); font-size:16px; display:flex; align-items:center; gap:5px;">
+                                <span style="color:#2ecc71; font-weight:bold;">TDS OUT:</span>
+                                <span class="live-tds-out" style="color:white; font-weight:900;">--</span>
+                            </div>
                         </div>
-                        <div class="date-display" style="font-size: 20px; opacity: 0.9;">-- --- ----</div>
                     </div>
+                    <div style="text-align: right;">
+                        <div style="display: flex; align-items: center; justify-content: flex-end; gap: 10px;">
+                            <div class="net-status-icon"
+                                style="display: flex; align-items: flex-end; gap: 4px; height: 28px; margin-bottom: 8px;">
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 7px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 14px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 21px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 28px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                            </div>
+                            <div class="clock-display"
+                                style="font-size: 56px; font-weight: 900; color: white; line-height: 1;">--:--</div>
+                        </div>
+                        <div class="date-display"
+                            style="font-size: 20px; color: rgba(255,255,255,0.8); margin-top: 5px;">-- --- ----</div>
+                    </div>
                 </div>
                 <div class="kiosk-body" id="choiceBodyContainer">
                     <!-- 🚀 STACKED BUTTONS - FORCED -->
@@ -1713,8 +2215,9 @@
                             <span style="font-size:32px; font-weight:700; opacity:0.9;">เพื่อรับน้ำดื่มฟรี</span>
                         </div>
                     </button>
-                    
-                    <button onclick="if(typeof window.typePhone==='function')window.typePhone('clear'); showScreen('phone');"
+
+                    <button
+                        onclick="if(typeof window.typePhone==='function')window.typePhone('clear'); showScreen('phone');"
                         style="width:90%; background: white; color:#2563eb; border:10px solid #2563eb; border-radius:40px; padding:45px 40px; font-size:54px; font-weight:900; box-shadow: 0 10px 20px rgba(0,0,0,0.05); cursor:pointer; display:flex; align-items:center; justify-content:center; gap:30px; margin-bottom:30px;">
                         <span style="font-size:80px;">☎️</span>
                         <div style="display:flex; flex-direction:column; align-items:flex-start;">
@@ -1730,34 +2233,95 @@
             </div>
 
             <!-- 📱 SCREEN: PHONE ENTRY -->
-            <div class="screen" id="screen-phone" style="background: #f8fafc; flex-direction:column; align-items:center; justify-content:center; padding:40px;">
-                <div style="background:white; width:100%; max-width:700px; padding:50px; border-radius:50px; box-shadow:0 30px 60px rgba(0,0,0,0.15); display:flex; flex-direction:column; align-items:center; gap:30px;">
-                    <h2 style="font-size:46px; font-weight:900; color:var(--blue-dark); margin:0; text-align:center;">กรอกเบอร์โทรศัพท์ที่ท่านลงทะเบียน</h2>
-                    
-                    <!-- Display Area -->
-                    <div id="phoneDisplay" style="background:#f1f5f9; width:100%; height:120px; border-radius:25px; display:flex; align-items:center; justify-content:center; font-size:60px; font-weight:900; color:var(--blue-dark); letter-spacing:8px; border:4px solid #cbd5e1;">
-                        XXX-XXXX-X___
+            <div class="screen" id="screen-phone"
+                style="background: #f8fafc; display:flex; flex-direction:column; height:100%; overflow:hidden;">
+                <div class="kiosk-header">
+                    <div>
+                        <div class="kiosk-brand-name stationNameHeader">ตู้กดน้ำสะอาด</div>
+                        <div style="display:flex; gap:12px; margin-top:4px;">
+                            <div
+                                style="color:rgba(255,255,255,0.7); font-size:16px; display:flex; align-items:center; gap:5px;">
+                                <span style="color:#00f2fe; font-weight:bold;">TDS IN:</span>
+                                <span class="live-tds-in" style="color:white; font-weight:900;">--</span>
+                            </div>
+                            <div
+                                style="color:rgba(255,255,255,0.7); font-size:16px; display:flex; align-items:center; gap:5px;">
+                                <span style="color:#2ecc71; font-weight:bold;">TDS OUT:</span>
+                                <span class="live-tds-out" style="color:white; font-weight:900;">--</span>
+                            </div>
+                        </div>
                     </div>
-
-                    <!-- Keypad -->
-                    <div style="display:grid; grid-template-columns:repeat(3, 1fr); gap:20px; width:100%;">
-                        <button onclick="typePhone('1')" style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">1</button>
-                        <button onclick="typePhone('2')" style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">2</button>
-                        <button onclick="typePhone('3')" style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">3</button>
-                        <button onclick="typePhone('4')" style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">4</button>
-                        <button onclick="typePhone('5')" style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">5</button>
-                        <button onclick="typePhone('6')" style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">6</button>
-                        <button onclick="typePhone('7')" style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">7</button>
-                        <button onclick="typePhone('8')" style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">8</button>
-                        <button onclick="typePhone('9')" style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">9</button>
-                        <button onclick="typePhone('clear')" style="background:#f59e0b; border:none; height:110px; border-radius:25px; font-size:30px; font-weight:900; color:white; cursor:pointer; box-shadow:0 4px 10px rgba(245,158,11,0.2);">ล้าง</button>
-                        <button onclick="typePhone('0')" style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">0</button>
-                        <button onclick="typePhone('back')" style="background:#eab308; border:none; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:white; cursor:pointer; box-shadow:0 4px 10px rgba(234,179,8,0.2);">⌫</button>
+                    <div style="text-align: right;">
+                        <div style="display: flex; align-items: center; justify-content: flex-end; gap: 10px;">
+                            <div class="net-status-icon"
+                                style="display: flex; align-items: flex-end; gap: 4px; height: 28px; margin-bottom: 8px;">
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 7px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 14px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 21px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 28px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                            </div>
+                            <div class="clock-display"
+                                style="font-size: 56px; font-weight: 900; color: white; line-height: 1;">--:--</div>
+                        </div>
+                        <div class="date-display"
+                            style="font-size: 20px; color: rgba(255,255,255,0.8); margin-top: 5px;">-- --- ----</div>
                     </div>
+                </div>
+                <div class="kiosk-body" style="align-items:center; justify-content:center; padding:40px; flex:1;">
+                    <div
+                        style="background:white; width:100%; max-width:700px; padding:50px; border-radius:50px; box-shadow:0 30px 60px rgba(0,0,0,0.15); display:flex; flex-direction:column; align-items:center; gap:30px;">
+                        <h2
+                            style="font-size:46px; font-weight:900; color:var(--blue-dark); margin:0; text-align:center;">
+                            กรอกเบอร์โทรศัพท์ที่ท่านลงทะเบียน</h2>
 
-                    <div style="display:grid; grid-template-columns:1fr 2fr; gap:20px; width:100%;">
-                        <button onclick="showScreen('choice')" style="background:#94a3b8; color:white; border:none; height:100px; border-radius:30px; font-size:32px; font-weight:900; cursor:pointer;">ยกเลิก</button>
-                        <button id="phoneConfirmBtn" onclick="confirmPhone()" style="background:#059669; color:white; border:none; height:100px; border-radius:30px; font-size:36px; font-weight:900; cursor:pointer; opacity:0.5; pointer-events:none; box-shadow:0 6px 15px rgba(5,150,105,0.3);">ยืนยัน ➔</button>
+                        <!-- Display Area -->
+                        <div id="phoneDisplay"
+                            style="background:#f1f5f9; width:100%; height:120px; border-radius:25px; display:flex; align-items:center; justify-content:center; font-size:35px; font-weight:900; color:#94a3b8; letter-spacing:1px; border:4px solid #cbd5e1;">
+                            กรุณากรอกเบอร์โทร</div>
+
+                        <!-- Keypad -->
+                        <div style="display:grid; grid-template-columns:repeat(3, 1fr); gap:20px; width:100%;">
+                            <button onclick="typePhone('1')"
+                                style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">1</button>
+                            <button onclick="typePhone('2')"
+                                style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">2</button>
+                            <button onclick="typePhone('3')"
+                                style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">3</button>
+                            <button onclick="typePhone('4')"
+                                style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">4</button>
+                            <button onclick="typePhone('5')"
+                                style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">5</button>
+                            <button onclick="typePhone('6')"
+                                style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">6</button>
+                            <button onclick="typePhone('7')"
+                                style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">7</button>
+                            <button onclick="typePhone('8')"
+                                style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">8</button>
+                            <button onclick="typePhone('9')"
+                                style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">9</button>
+                            <button onclick="typePhone('clear')"
+                                style="background:#f59e0b; border:none; height:110px; border-radius:25px; font-size:30px; font-weight:900; color:white; cursor:pointer; box-shadow:0 4px 10px rgba(245,158,11,0.2);">ล้าง</button>
+                            <button onclick="typePhone('0')"
+                                style="background:white; border:3px solid #e2e8f0; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:var(--blue-dark); cursor:pointer;">0</button>
+                            <button onclick="typePhone('back')"
+                                style="background:#eab308; border:none; height:110px; border-radius:25px; font-size:40px; font-weight:900; color:white; cursor:pointer; box-shadow:0 4px 10px rgba(234,179,8,0.2);">⌫</button>
+                        </div>
+
+                        <div style="display:grid; grid-template-columns:1fr 2fr; gap:20px; width:100%;">
+                            <button onclick="showScreen('choice')"
+                                style="background:#94a3b8; color:white; border:none; height:100px; border-radius:30px; font-size:32px; font-weight:900; cursor:pointer;">ยกเลิก</button>
+                            <button id="phoneConfirmBtn" onclick="confirmPhone()"
+                                style="background:#059669; color:white; border:none; height:100px; border-radius:30px; font-size:36px; font-weight:900; cursor:pointer; opacity:0.5; pointer-events:none; box-shadow:0 6px 15px rgba(5,150,105,0.3);">ยืนยัน
+                                ➔</button>
+                        </div>
                     </div>
                 </div>
             </div>
@@ -1768,7 +2332,7 @@
                 <div class="kiosk-header">
                     <div>
                         <div class="kiosk-brand-sub">ลงทะเบียนใหม่</div>
-                        <div class="kiosk-brand-name">ตู้น้ำดื่มสะอาด 24 ชม.</div>
+                        <div class="kiosk-brand-name stationNameHeader">ตู้กดน้ำสะอาด</div>
                         <div style="display:flex; gap:12px; margin-top:4px;">
                             <div
                                 style="color:rgba(255,255,255,0.7); font-size:16px; display:flex; align-items:center; gap:5px;">
@@ -1966,14 +2530,6 @@
                         style="display:grid;grid-template-columns:1fr 1fr;gap:24px;width:100%;">
                         <button class="vol-btn" data-vol="1" onclick="selectVolume(1)">1 ลิตร</button>
                         <button class="vol-btn" data-vol="2" onclick="selectVolume(2)">2 ลิตร</button>
-                        <button class="vol-btn" data-vol="3" onclick="selectVolume(3)">3 ลิตร</button>
-                        <button class="vol-btn" data-vol="4" onclick="selectVolume(4)">4 ลิตร</button>
-                        <button class="vol-btn" data-vol="5" onclick="selectVolume(5)">5 ลิตร</button>
-                        <button class="vol-btn" data-vol="6" onclick="selectVolume(6)">6 ลิตร</button>
-                        <button class="vol-btn" data-vol="7" onclick="selectVolume(7)">7 ลิตร</button>
-                        <button class="vol-btn" data-vol="8" onclick="selectVolume(8)">8 ลิตร</button>
-                        <button class="vol-btn" data-vol="9" onclick="selectVolume(9)">9 ลิตร</button>
-                        <button class="vol-btn" data-vol="10" onclick="selectVolume(10)">10 ลิตร</button>
                     </div>
 
                     <button onclick="showScreen('standby')"
@@ -1986,7 +2542,7 @@
             <div class="screen" id="screen-dispense">
                 <div class="kiosk-header">
                     <div>
-                        <div class="kiosk-brand-name" id="stationNameHeader">ตู้กดน้ำสะอาด</div>
+                        <div class="kiosk-brand-name stationNameHeader">ตู้กดน้ำสะอาด</div>
                         <div style="display:flex; gap:12px; margin-top:4px;">
                             <div
                                 style="color:rgba(255,255,255,0.7); font-size:16px; display:flex; align-items:center; gap:5px;">
@@ -2001,8 +2557,25 @@
                         </div>
                     </div>
                     <div style="text-align: right;">
-                        <div class="clock-display"
-                            style="font-size: 56px; font-weight: 900; color: white; line-height: 1;">--:--</div>
+                        <div style="display: flex; align-items: center; justify-content: flex-end; gap: 10px;">
+                            <div class="net-status-icon"
+                                style="display: flex; align-items: flex-end; gap: 4px; height: 28px; margin-bottom: 8px;">
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 7px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 14px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 21px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                                <div class="sig-bar"
+                                    style="width: 5px; height: 28px; background: #10b981; border-radius: 2px; transition: background 0.3s, box-shadow 0.3s; box-shadow: 0 0 5px rgba(16,185,129,0.4);">
+                                </div>
+                            </div>
+                            <div class="clock-display"
+                                style="font-size: 56px; font-weight: 900; color: white; line-height: 1;">--:--</div>
+                        </div>
                         <div class="date-display"
                             style="font-size: 20px; color: rgba(255,255,255,0.8); margin-top: 5px;">-- --- ----</div>
                     </div>
@@ -2010,12 +2583,15 @@
 
                 <div class="kiosk-body" style="justify-content:center;padding-top:25px;gap:17px;">
                     <div class="dispense-title" id="dispense-user-name"
-                        style="color:var(--green); font-size:42px; font-weight:900; margin-bottom:10px; display:none;">กำลังโหลด...</div>
+                        style="color:var(--green); font-size:42px; font-weight:900; margin-bottom:10px; display:none;">
+                        กำลังโหลด...</div>
                     <div class="dispense-title" id="dispense-quota-display"
                         style="font-size:26px; opacity:0.8; margin-bottom:20px; display:none;">โควตา -- L</div>
-                    
+
                     <div id="dispenseMainTitle" style="display:none;"></div> <!-- Hide redundant title -->
-                    <div class="dispense-sub" id="dispense-status-sub" style="font-size:48px; color:var(--blue-dark); font-weight:900; margin: 15px 0;">กรุณากดเริ่มจ่ายน้ำเพื่อรับน้ำ</div>
+                    <div class="dispense-sub" id="dispense-status-sub"
+                        style="font-size:48px; color:var(--blue-dark); font-weight:900; margin: 15px 0;">
+                        กรุณากดเริ่มจ่ายน้ำเพื่อรับน้ำ</div>
 
                     <div class="water-container">
                         <div class="water-fill" id="waterFill">
@@ -2036,6 +2612,7 @@
                             <div class="dispense-stat-val" id="dispenseLeft">0.75</div>
                             <div class="dispense-stat-unit">ลิตร</div>
                         </div>
+
                     </div>
 
                     <div id="dispenseBtnContainer" style="margin-top:20px; width:100%;">
@@ -2043,6 +2620,9 @@
                         <button id="startDispenseBtn" onclick="startDispensing()"
                             style="width:100%; background:#059669; color:white; border:none; padding:25px; border-radius:20px; font-size:32px; font-weight:900; cursor:pointer; box-shadow:0 6px 20px rgba(5,150,105,0.3);">▶️
                             เริ่มจ่ายน้ำ</button>
+                        <button onclick="showScreen('standby')"
+                            style="width:100%; margin-top:15px; background:#ef4444; color:white; border:none; padding:20px; border-radius:20px; font-size:26px; font-weight:900; cursor:pointer; box-shadow:0 6px 15px rgba(239,68,68,0.3);">❌
+                            ยกเลิก</button>
                     </div>
                 </div>
             </div>
@@ -2103,11 +2683,23 @@
         </div>
     </div>
 
-    <!-- 🌐 OFFLINE OVERLAY -->
-    <div id="offlineOverlay">
-        <div class="offline-icon">⚠️</div>
-        <div class="offline-title">เครื่องไม่พร้อมใช้งาน</div>
-        <div class="offline-desc">กำลังตรวจสอบการเชื่อมต่ออินเทอร์เน็ต...</div>
+    <!-- 🛡️ BACKDROP: Hard-blocks ALL touch/click events on Android when overlay is active -->
+    <div id="kioskSystemBackdrop" class="active"
+        onclick="event.stopPropagation(); event.preventDefault(); return false;"
+        ontouchstart="event.stopPropagation(); event.preventDefault(); return false;"
+        ontouchmove="event.stopPropagation(); event.preventDefault(); return false;"
+        ontouchend="event.stopPropagation(); event.preventDefault(); return false;"></div>
+
+    <!-- 🛑 KIOSK SYSTEM OVERLAY (Unified for Network & Hardware Failures) -->
+    <div id="kioskSystemOverlay" class="active" style="display: flex;"
+        onclick="event.stopPropagation(); event.preventDefault();"
+        ontouchstart="event.stopPropagation(); event.preventDefault();">
+        <div id="kioskSystemIcon" class="kiosk-system-icon">⚠️</div>
+        <div id="kioskSystemTitle" class="kiosk-system-title">เครื่องไม่พร้อมใช้งาน</div>
+        <div id="kioskSystemDesc" class="kiosk-system-desc">กำลังตรวจสอบการเชื่อมต่ออินเทอร์เน็ต...</div>
+        <div
+            style="margin-top: 20px; font-size: 24px; color: rgba(255,255,255,0.6); font-family: monospace; font-weight: bold; padding: 10px 20px; background: rgba(0,0,0,0.5); border-radius: 10px; border: 1px solid rgba(255,255,255,0.2); margin-bottom: 25px;">
+            Machine ID: <span id="offlineMachineIdDisplay" style="color: #f1c40f;">Loading...</span></div>
     </div>
 
     <!-- 🛠️ DEBUG CONSOLE -->
@@ -2229,6 +2821,18 @@
             <button onclick="setupMachineName()"
                 style="background:#e67e22; color:white; border:none; padding:5px 12px; border-radius:4px; cursor:pointer; font-size:10px; font-weight:bold;">🆔
                 SETUP ID</button>
+            <button onclick="runAutoStressTest()" id="btnStressTest"
+                style="background:#d35400; color:white; border:none; padding:5px 12px; border-radius:4px; cursor:pointer; font-size:10px; font-weight:bold;">🧪
+                STRESS TEST 100x</button>
+            <button onclick="runSpamStressTest()" id="btnSpamTest"
+                style="background:#8e44ad; color:white; border:none; padding:5px 12px; border-radius:4px; cursor:pointer; font-size:10px; font-weight:bold;">💥
+                SPAM TEST 20x</button>
+            <button onclick="runQuotaLimitStressTest()" id="btnLimitTest"
+                style="background:#16a085; color:white; border:none; padding:5px 12px; border-radius:4px; cursor:pointer; font-size:10px; font-weight:bold;">🛡️
+                LIMIT TEST 20x</button>
+            <button onclick="runRealHardwareStressTest()" id="btnRealHwTest"
+                style="background:#00f2fe; color:black; border:none; padding:5px 12px; border-radius:4px; cursor:pointer; font-size:10px; font-weight:bold; margin-left:2px;">🧪
+                REAL HW TEST</button>
         </div>
 
         <!-- Row: Manual Flow Input -->
@@ -2312,6 +2916,8 @@
     <script>
         // 💡 Cabinet Light Control
         var lastLightState = true; // Default to true (ON)
+        window._lightOnHour = 18;  // Default, overridden by Firestore
+        window._lightOffHour = 6;  // Default, overridden by Firestore
         function controlLight(isOn) {
             const label = isOn ? 'ON' : 'OFF';
             logToScreen(`💡 [COMMAND] LIGHT ${label}`);
@@ -2349,6 +2955,15 @@
         }
 
         // Unified call for the premium PIN overlay
+        window.closePinOverlay = function () {
+            const overlay = document.getElementById("pinOverlay");
+            if (overlay) overlay.style.display = "none";
+            if (window._pinTimeoutTimer) {
+                clearTimeout(window._pinTimeoutTimer);
+                window._pinTimeoutTimer = null;
+            }
+        };
+
         window.toggleDebug = () => {
             const overlay = document.getElementById("pinOverlay");
             const input = document.getElementById("pinInput");
@@ -2358,12 +2973,37 @@
             input.value = "";
             overlay.style.display = "flex";
 
+            // Start 30s inactivity timer
+            window.resetPinTimeout = () => {
+                if (window._pinTimeoutTimer) {
+                    clearTimeout(window._pinTimeoutTimer);
+                }
+                window._pinTimeoutTimer = setTimeout(() => {
+                    window.closePinOverlay();
+                    logToScreen("<span style='color:#e74c3c'>⏱️ PIN Pad Timeout: Closed after 30 seconds of inactivity</span>");
+                }, 30000); // 30 seconds
+            };
+
+            window.resetPinTimeout();
+
+            // Intercept interactions to reset the 30s inactivity timer
+            if (!overlay._hasTimeoutListener) {
+                overlay._hasTimeoutListener = true;
+                const resetOnInteraction = () => {
+                    if (overlay.style.display === "flex" && typeof window.resetPinTimeout === "function") {
+                        window.resetPinTimeout();
+                    }
+                };
+                overlay.addEventListener('click', resetOnInteraction);
+                overlay.addEventListener('touchstart', resetOnInteraction, { passive: true });
+            }
+
             submit.onclick = () => {
                 let inputPin = input.value;
                 const errLabel = document.getElementById("pinErrorMsg");
 
                 if (inputPin === window.MASTER_PIN || inputPin === "7777" || inputPin === "0670") {
-                    overlay.style.display = "none";
+                    window.closePinOverlay();
                     const c = document.getElementById("debugConsole");
                     const d = document.getElementById("deviceIdOverlay");
                     if (c) c.style.display = (c.style.display === "flex") ? "none" : "flex";
@@ -2377,6 +3017,7 @@
                         setTimeout(() => { if (errLabel) errLabel.style.display = "none"; }, 2000);
                     }
                     input.value = "";
+                    window.resetPinTimeout(); // Reset timer on wrong code too!
                     // Add a small shake effect
                     overlay.firstElementChild.animate([
                         { transform: 'translateX(-10px)' },
@@ -2393,36 +3034,122 @@
         document.addEventListener('DOMContentLoaded', () => {
             console.log("🚀 [STATION] DOM Loaded, running syncIdToUI...");
             syncIdToUI();
-            
-            // 🌐 Internet Status Monitoring
-            function updateNetworkStatus() {
-                const isOnline = navigator.onLine;
-                const icon = document.getElementById('net-status-icon');
-                if (icon) {
-                    icon.style.background = isOnline ? '#10b981' : '#ef4444';
-                    icon.style.boxShadow = isOnline ? '0 0 10px rgba(16,185,129,0.5)' : '0 0 10px rgba(239,68,68,0.5)';
+
+            // 🌐 Robust Network & Internet Verification (Combines navigator.onLine + fast HTTP ping)
+            let isCheckingNetwork = false;
+            async function updateNetworkStatus(forceInstant = false) {
+                if (isCheckingNetwork && !forceInstant) return;
+                isCheckingNetwork = true;
+
+                let isOnline = navigator.onLine;
+
+                // If navigator.onLine says we are offline, trust it immediately
+                if (!isOnline) {
+                    applyNetworkState(false, forceInstant);
+                    isCheckingNetwork = false;
+                    return;
                 }
-                console.log("🌐 [NETWORK] Online status:", isOnline);
+
+                // If navigator.onLine says we are online, perform a strict fetch to verify real internet
+                // We use a CORS-enforced fetch instead of Image ping to prevent Captive Portals from giving false positives.
+                try {
+                    const controller = new AbortController();
+                    const timeoutId = setTimeout(() => controller.abort(), 1500);
+                    const res = await fetch("https://www.gstatic.com/generate_204", {
+                        method: "GET",
+                        mode: "no-cors", // no-cors is reliable on Android WebView, avoids dns.google CORS false failures
+                        cache: "no-store",
+                        signal: controller.signal
+                    });
+                    clearTimeout(timeoutId);
+                    isOnline = true; // no-cors response is always opaque (type=opaque), treat any response as online
+                    if (!isOnline) {
+                        console.warn("⚠️ [NETWORK] Internet check fetch failed, assuming OFFLINE.");
+                    }
+                } catch (err) {
+                    console.warn("⚠️ [NETWORK] Internet check fetch threw error:", err.message || err);
+                    isOnline = false; // Ping failed (timeout or no connection)
+                }
+
+                applyNetworkState(isOnline, forceInstant);
+                isCheckingNetwork = false;
             }
-            window.addEventListener('online', updateNetworkStatus);
-            window.addEventListener('offline', updateNetworkStatus);
-            updateNetworkStatus(); // Initial check
+
+            function applyNetworkState(isOnline, forceInstant) {
+                const icons = document.querySelectorAll('.net-status-icon');
+                icons.forEach(icon => {
+                    const bars = icon.getElementsByClassName('sig-bar');
+                    for (let bar of bars) {
+                        bar.style.background = isOnline ? '#10b981' : '#ff4b2b';
+                        bar.style.boxShadow = isOnline ? '0 0 8px rgba(16,185,129,0.5)' : '0 0 8px rgba(255,75,43,0.5)';
+                    }
+                });
+                window.isAppOnline = isOnline;
+                console.log("🌐 [NETWORK] Real Internet Status:", isOnline, "(forceInstant:", forceInstant + ")");
+                if (typeof updateOnlineUI === 'function') updateOnlineUI(isOnline, forceInstant);
+            }
+
+            // React instantly to OS network events (with 2 seconds stabilization delay for online event to let DNS & IP routing establish)
+            let onlineEventTimeout = null;
+            window.addEventListener('online', () => {
+                console.log("🟢 [NET-EVENT] OS reported online. Stabilizing connection for 2s...");
+                if (onlineEventTimeout) clearTimeout(onlineEventTimeout);
+                onlineEventTimeout = setTimeout(() => {
+                    updateNetworkStatus(true);
+                }, 2000);
+            });
+            window.addEventListener('offline', () => {
+                console.log("🔴 [NET-EVENT] offline");
+                if (onlineEventTimeout) clearTimeout(onlineEventTimeout);
+                updateNetworkStatus(true);
+            });
+            // Initial check: 100ms delay — fast check with Image ping
+            setTimeout(() => updateNetworkStatus(true), 100);
+            // Periodic safety check every 5 seconds (faster detection when internet is lost)
+            setInterval(() => updateNetworkStatus(false), 5000);
+
+
         });
 
         function syncIdToUI() {
             let id = localStorage.getItem('station_id');
-            if (!id || id === 'null' || id === 'undefined') {
+            // 💡 PRE-POPULATE Machine ID instantly so it's ready before offline check fires
+            const midDisp = document.getElementById('offlineMachineIdDisplay');
+            if (midDisp) {
+                const sid = localStorage.getItem('KIOSK_NAME') || localStorage.getItem('station_id') || (typeof DEVICE_ID !== 'undefined' ? DEVICE_ID : 'Unknown');
+                midDisp.innerText = sid;
+                console.log('[STATION] Machine ID pre-populated:', sid);
+            }
+            if (window.AndroidSerial && typeof window.AndroidSerial.getDeviceId === 'function') {
+                id = window.AndroidSerial.getDeviceId().toLowerCase();
+                localStorage.setItem('station_id', id);
+            } else if (!id || id === 'null' || id === 'undefined') {
                 id = '660000000002'; // FORCE ID FOR DEBUGGING
                 localStorage.setItem('station_id', id);
                 console.log("🛠️ [DEBUG] ID was missing, forcing to:", id);
             }
             window.deviceId = id;
+
+            const displayId = localStorage.getItem('KIOSK_NAME') || id;
+
             const el = document.getElementById('stationIdDisplay');
-            if (el) el.textContent = id;
-            
+            if (el) el.textContent = displayId;
+
             const labelEl = document.getElementById('stationIdLabel');
-            if (labelEl) labelEl.textContent = id;
+            if (labelEl) labelEl.textContent = displayId;
 
+            const verEl = document.getElementById('appVersionLabel');
+            if (verEl) {
+                let currentVer = "1.38 (RESTORED)";
+                if (window.AndroidSerial && typeof window.AndroidSerial.getAppVersion === 'function') {
+                    currentVer = window.AndroidSerial.getAppVersion();
+                }
+                const kIn = typeof TDS_CALIBRATION_IN !== 'undefined' ? TDS_CALIBRATION_IN.toFixed(2) : "1.00";
+                const rIn = window.debugRawIn || 0;
+                const rOut = window.debugRawOut || 0;
+                verEl.textContent = currentVer + " (K:" + kIn + " | R:" + rIn + "," + rOut + ")";
+            }
+
             console.log("🆔 [STATION] Current Device ID:", window.deviceId);
             logToScreen(`🆔 STATION ID: ${window.deviceId}`);
 
@@ -2438,24 +3165,26 @@
                 return;
             }
 
+            const cleanId = id.trim().toLowerCase();
+
             // Avoid duplicate listeners
-            if (window.activeRemoteListener === id) {
-                console.log("🛰️ [REMOTE] Listener already active for:", id);
+            if (window.activeRemoteListener === cleanId) {
+                console.log("🛰️ [REMOTE] Listener already active for:", cleanId);
                 return;
             }
-            window.activeRemoteListener = id;
+            window.activeRemoteListener = cleanId;
 
-            logToScreen(`🛰️ [COMMAND] Listening for remote commands: ${id}`);
-            console.log(`🛰️ [REMOTE] Now listening on Firestore: commands/${id}`);
+            logToScreen(`🛰️ [COMMAND] Listening for remote commands: ${cleanId}`);
+            console.log(`🛰️ [REMOTE] Now listening on Firestore: commands/${cleanId}`);
 
             let isFirstSnapshot = true;
             let lastExecutedTime = 0;
 
-            onSnapshot(doc(db, "commands", id), (snap) => {
+            onSnapshot(doc(db, "commands", cleanId), (snap) => {
                 if (snap.exists) {
                     const cmd = snap.data();
                     console.log("🛰️ [REMOTE] Document updated:", cmd);
-                    const cmdTime = cmd.timestamp ? (cmd.timestamp.toMillis ? cmd.timestamp.toMillis() : cmd.timestamp) : 0;
+                    const cmdTime = cmd.timestamp ? (cmd.timestamp.toMillis ? cmd.timestamp.toMillis() : cmd.timestamp) : Date.now();
 
                     if (isFirstSnapshot) {
                         isFirstSnapshot = false;
@@ -2470,12 +3199,52 @@
                         console.log(`🛰️ [REMOTE] EXECUTE: type=${cmd.type}, value=${cmd.value}`);
                         if (cmd.type === 'light') {
                             controlLight(cmd.value);
+                        } else if (cmd.type === 'console_stream') {
+                            window.isConsoleStreaming = cmd.value === true || cmd.value === 'true';
+                            console.log("🛰️ [REMOTE] Console streaming set to:", window.isConsoleStreaming);
+                            if (window.isConsoleStreaming) {
+                                if (typeof triggerConsoleUpload === 'function') triggerConsoleUpload();
+                            }
+                        } else if (cmd.type === 'reboot' || cmd.type === 'restart') {
+                            logToScreen("🛰️ [REMOTE] Executing remote app reboot...");
+                            localStorage.setItem('restart_reason', 'remote_reboot');
+
+                            // Log the remote reboot execution to the database log using v10 ESM syntax
+                            try {
+                                const machineName = document.getElementById('hotlineMachineName')?.innerText || cleanId;
+                                addDoc(collection(db, "machine_logs"), {
+                                    machineId: cleanId,
+                                    machineName: machineName,
+                                    status: 'app_reboot_command',
+                                    timestamp: serverTimestamp(),
+                                    details: 'ตู้บริการได้รับคำสั่งรีสตาร์ทแอปพลิเคชันจากระยะไกลสำเร็จ (Remote Reboot Command Executed Successfully)'
+                                });
+                            } catch (e) {
+                                console.error("Failed to log remote reboot command:", e);
+                            }
+
+                            setTimeout(() => {
+                                if (window.AndroidSerial && typeof window.AndroidSerial.restartApp === 'function') {
+                                    window.AndroidSerial.restartApp();
+                                } else {
+                                    console.log("🔄 Simulator Reboot: Reloading page...");
+                                    window.location.reload();
+                                }
+                            }, 800); // 800ms delay to let Firestore write complete
+                        } else if (cmd.type === 'simulate_freeze') {
+                            logToScreen("🛰️ [REMOTE] Executing remote simulate freeze (Test)...");
+                            try {
+                                window.AndroidSerial.simulateJavaFreeze();
+                            } catch (e) {
+                                console.log("🔄 Simulator Freeze: Freezing JS instead...");
+                                while (true) { } // Freeze browser tab if simulator
+                            }
                         }
                     } else {
                         console.log(`🛰️ [REMOTE] SKIP: Command not new (CmdTime: ${cmdTime}, LastExecuted: ${lastExecutedTime})`);
                     }
                 } else {
-                    console.log("🛰️ [REMOTE] Document does not exist yet at commands/" + id);
+                    console.log("🛰️ [REMOTE] Document does not exist yet at commands/" + cleanId);
                 }
             }, (err) => {
                 console.error("🛰️ [REMOTE] Snapshot error:", err);
@@ -2588,6 +3357,9 @@
             return Math.max(0, Math.round(tdsValue));
         }
         var K_FACTOR = 570;
+        // 🔬 TDS Calibration Factor (โหลดจาก Firebase แบบ Dynamic)
+        var TDS_CALIBRATION_IN = 1.0;   // Default = ไม่ปรับ (จะถูกแทนที่เมื่อโหลด Firebase)
+        var TDS_CALIBRATION_OUT = 1.0;  // Default = ไม่ปรับ (จะถูกแทนที่เมื่อโหลด Firebase)
         var dispenseSession = {
             active: false, paused: false, targetLiters: 0, targetPulses: 0, targetReached: false,
             startFlow: 0, startTime: null, wasRunning: false, startFlowCaptured: false, maxFlow: 0,
@@ -2639,6 +3411,18 @@
 
             // 🚀 UI OPTIMIZATION: Limit log history to 50 lines to prevent lag
             if (area.childNodes.length > 50) area.removeChild(area.lastChild);
+
+            // 🚀 Real-time Console Log Streaming
+            if (!window.consoleLogsList) window.consoleLogsList = [];
+            const cleanMsg = msg.replace(/<[^>]*>/g, '');
+            const timeStr = new Date().toLocaleTimeString();
+            window.consoleLogsList.push(`[${timeStr}] ${cleanMsg}`);
+            if (window.consoleLogsList.length > 25) window.consoleLogsList.shift();
+
+            // 🚀 Stream to database ONLY if admin is actively watching (console_stream is active)
+            if (window.isConsoleStreaming) {
+                if (typeof triggerConsoleUpload === 'function') triggerConsoleUpload();
+            }
         }
 
         logToScreen("<span style='color:#00f2fe; font-weight:bold;'>🚀 SCRIPT LOADED & READY (V1.1-C)</span>");
@@ -2668,6 +3452,7 @@
         // ── 3. SERIAL LOGIC (Strict 24-byte Buffer) ──
         window.onSerialReceiveHex = function (hexStr) {
             console.log("📥 SERIAL_RX: " + hexStr);
+            // Always show RX logs on screen for technician transparency
             logToScreen('<span style="color:#2ecc71">📥 RX: ' + hexStr + '</span>');
 
             // 🎯 REPORT SUCCESS IF SCANNING
@@ -2682,6 +3467,10 @@
             }
             processRxBuffer();
             updateHwStatus('active');
+
+            // 🚀 SERIAL WATCHDOG: If no data received for 5 seconds, mark hardware as offline
+            if (window.hwStatusTimeout) clearTimeout(window.hwStatusTimeout);
+            window.hwStatusTimeout = setTimeout(() => { updateHwStatus('error'); }, 5000);
         };
 
         function processRxBuffer() {
@@ -2731,50 +3520,147 @@
                     const trace = dataBytes.slice(0, 16).map((b, i) => `[${i}]:${b.toString(16).toUpperCase()}`).join(' ');
                     console.log("🔬 DATA TRACE: " + trace);
 
-                    telemetry.level = dataBytes[0] | (dataBytes[1] << 8);
-                    telemetry.flow = dataBytes[2] | (dataBytes[3] << 8);
-                    telemetry.lowPress = dataBytes[12];
+                    if (boardAddr === 0xC1) {
+                        telemetry.level = dataBytes[0] | (dataBytes[1] << 8);
+                        telemetry.flow = dataBytes[2] | (dataBytes[3] << 8);
+                        telemetry.lowPress = dataBytes[12];
 
+                        // 🔴 No Tap Water Check (Byte 12 is 'F' = 0x46, 'N' = 0x4E)
+                        let statusChanged = false;
 
-                    telemetry.lowPress = dataBytes[12];
+                        // 💧 Raw Tap Water Sensor (Level = F = 0x46)
+                        const isTapWaterMissing = (dataBytes[12] === 0x46);
 
-                    // 📊 REAL-TIME CALCULATION: Redundant block removed (See logic below)
+                        if (isTapWaterMissing) {
+                            if (telemetry.no_tap_water !== true) { telemetry.no_tap_water = true; statusChanged = true; }
+                        } else {
+                            if (telemetry.no_tap_water !== false) { telemetry.no_tap_water = false; statusChanged = true; }
+                        }
 
-                    if (window.lastFlowVal !== undefined && telemetry.flow !== window.lastFlowVal) { logToScreen("<span style=\"background:#f1c40f; color:black; font-size:10px;\">🌬️ SENSOR DETECTED: " + telemetry.flow + " pulses</span>"); } window.lastFlowVal = telemetry.flow;
+                        // 🔴 Water Empty Check (Level L = F)
+                        if (dataBytes[0] === 0x46) {
+                            if (telemetry.water_empty !== true) { telemetry.water_empty = true; statusChanged = true; }
+                        } else if (dataBytes[0] === 0x4E) {
+                            if (telemetry.water_empty !== false) { telemetry.water_empty = false; statusChanged = true; }
+                        }
 
-                    // 📥 TDS IN (Always show 100-200)
-                    let rawTdsIn = dataBytes[4] | (dataBytes[5] << 8);
-                    telemetry.tdsIn = 120 + (Math.abs(rawTdsIn) % 50);
+                        // ⚙️ Producing Water Check (Level H = F and Tap Water = N)
+                        const isProducingWater = (dataBytes[1] === 0x46 && dataBytes[12] === 0x4E);
+                        if (isProducingWater) {
+                            if (telemetry.producing_water !== true) { telemetry.producing_water = true; statusChanged = true; }
+                        } else {
+                            if (telemetry.producing_water !== false) { telemetry.producing_water = false; statusChanged = true; }
+                        }
 
-                    // 📤 TDS OUT (Always show 5-8)
-                    let rawTdsOut = dataBytes[6] | (dataBytes[7] << 8);
-                    telemetry.tdsOut = 6 + (Math.abs(rawTdsOut) % 3);
+                        // 🔵 Water Full Check (Level H = N = 0x4E)
+                        const isWaterFull = (dataBytes[1] === 0x4E);
+                        if (isWaterFull) {
+                            if (telemetry.water_full !== true) { telemetry.water_full = true; statusChanged = true; }
+                        } else {
+                            if (telemetry.water_full !== false) { telemetry.water_full = false; statusChanged = true; }
+                        }
 
+                        // Banner removed based on user request.
+
+                        if (statusChanged) {
+                            if (typeof updateTestConsoleDisplay === 'function') {
+                                updateTestConsoleDisplay({ ...telemetry, raw: rawHex });
+                            }
+                            if (typeof sendHeartbeat === 'function') {
+                                sendHeartbeat(); // ⚡ อัปเดตขึ้น Firestore ทันทีที่สถานะเปลี่ยน
+                            }
+                        }
+
+                        // 🛡️ Debug Flow display update in telemetry RX
+                        let debugFlowEl = document.getElementById('dispenseFlowVal');
+                        if (debugFlowEl) {
+                            let acc = (window.dispenseSession && window.dispenseSession.accumulatedPulses) ? window.dispenseSession.accumulatedPulses : 0;
+                            let flowVal = (typeof telemetry !== 'undefined' && telemetry.flow !== undefined) ? telemetry.flow : 0;
+                            debugFlowEl.textContent = flowVal + " / " + acc;
+                        }
+
+                        if (!dispenseSession || !dispenseSession.active) {
+                            if (window.lastFlowVal !== undefined && telemetry.flow !== window.lastFlowVal) {
+                                logToScreen("<span style=\"background:#f1c40f; color:black; font-size:10px;\">🌬️ SENSOR DETECTED: " + telemetry.flow + " pulses</span>");
+                            }
+                        }
+                        window.lastFlowVal = telemetry.flow;
+                    }
+
+                    // 📥 TDS IN (Linear Mapping + Dynamic Calibration จาก Firebase)
+                    let currentRawIn = dataBytes[4] | (dataBytes[5] << 8);
+                    tdsInHistory.push(Math.abs(currentRawIn));
+                    if (tdsInHistory.length > FILTER_LEN) tdsInHistory.shift();
+                    let medianRawIn = getMedianNum(tdsInHistory, tdsInHistory.length);
+                    window.debugRawIn = medianRawIn; // ส่งออก Global ให้ Debug UI เห็น
+                    telemetry.tdsIn = Math.max(0, Math.round(medianRawIn * TDS_CALIBRATION_IN));
+
+                    // 📤 TDS OUT (Linear Mapping + Dynamic Calibration จาก Firebase)
+                    let currentRawOut = dataBytes[6] | (dataBytes[7] << 8);
+                    tdsOutHistory.push(Math.abs(currentRawOut));
+                    if (tdsOutHistory.length > FILTER_LEN) tdsOutHistory.shift();
+                    let medianRawOut = getMedianNum(tdsOutHistory, tdsOutHistory.length);
+                    window.debugRawOut = medianRawOut; // ส่งออก Global ให้ Debug UI เห็น
+                    telemetry.tdsOut = Math.max(0, Math.round(medianRawOut * TDS_CALIBRATION_OUT));
+
                     telemetry.status = dataBytes[8] || 0;
                     telemetry.fw = dataBytes[9] || 0;
                     telemetry.lowPress = dataBytes[10] || 0;
 
                     const stChar = String.fromCharCode(telemetry.status || 82);
-                    const isBoardFail = (dataBytes[0] === 0x46); const mOverlay = document.getElementById("maintenanceOverlay"); if (mOverlay) mOverlay.style.display = isBoardFail ? "flex" : "none"; logToScreen(`<div style="color:#f1c40f; font-size:9px; border-bottom:1px solid #333; word-break:break-all;">RAW: ${rawHex}</div><span style="color:${isBoardFail ? "#e74c3c" : "#2ecc71"}; font-size:10px;">${isBoardFail ? "❌ FAIL" : "✅ RX OK"} [0x${boardAddr.toString(16).toUpperCase()}] | TDS IN:${telemetry.tdsIn} | TDS OUT:${telemetry.tdsOut} | DATA:${trace}</span>`);
+                    const isBoardFail = (dataBytes[0] === 0x46);
+                    if (isBoardFail) {
+                        if (boardAddr === 0xC1) {
+                            window.isLowWater = true;
+                            window.lastLowWaterTime = Date.now(); // 🔒 LOCK LOW WATER STATE
+                            if (window.dispenseSession && window.dispenseSession.active) {
+                                logToScreen("<span style='color:red;'>🛑 LOW WATER DETECTED! Stopping dispense...</span>");
+                                if (typeof stopDispense === 'function') stopDispense();
+                                setTimeout(() => { if (typeof endSession === 'function') endSession(); }, 1500);
+                            }
+                        } else {
+                            updateHwStatus('error');
+                        }
+                    } else {
+                        if (boardAddr === 0xC1) {
+                            // ⏱️ DEBOUNCE: Only clear Low Water if we haven't seen a FAIL for 5 seconds.
+                            if (window.isLowWater && window.lastLowWaterTime && (Date.now() - window.lastLowWaterTime > 5000)) {
+                                window.isLowWater = false;
+                                console.log("💧 [WATER LEVEL RESTORED] Cleared Low Water State");
+                            }
+                        }
+                    }
 
+                    if (typeof window.syncSystemOverlay === 'function') {
+                        window.syncSystemOverlay();
+                    }
+
+                    // Always show trace logs on screen so user can see flow values update in real-time
+                    logToScreen(`<div style="color:#f1c40f; font-size:9px; border-bottom:1px solid #333; word-break:break-all;">RAW: ${rawHex}</div><span style="color:${isBoardFail ? "#e74c3c" : "#2ecc71"}; font-size:10px;">${isBoardFail ? "❌ FAIL" : "✅ RX OK"} [0x${boardAddr.toString(16).toUpperCase()}] | TDS IN:${telemetry.tdsIn} | TDS OUT:${telemetry.tdsOut} | DATA:${trace}</span>`);
+
                     updateTestConsoleDisplay({ ...telemetry, raw: rawHex });
                     window.lastHwData = { ...telemetry, raw: rawHex };
 
                     // 📊 REAL-TIME CALCULATION & UI UPDATE (SINGLE POINT OF TRUTH)
-                    if (dispenseSession.active && !dispenseSession.paused) {
+                    if (dispenseSession.active && !dispenseSession.paused && boardAddr === 0xC1) {
                         let currentFlow = telemetry.flow || 0;
-                        
-                        // 🛡️ CRITICAL FIX: Prevent delta jump from previous session
-                        // If lastFlow is 0 (fresh session) or significantly behind, sync it first
-                        if (!dispenseSession.lastFlow || dispenseSession.lastFlow === 0) {
-                            console.log("🔄 [SESSION] Syncing lastFlow to current:", currentFlow);
+
+                        // 🛡️ CRITICAL FIX: Establish baseline flow value exactly ONCE at start/resume
+                        if (dispenseSession.baselineInitialized === false || dispenseSession.lastFlow === undefined) {
                             dispenseSession.lastFlow = currentFlow;
+                            dispenseSession.baselineInitialized = true;
+                            console.log("🚀 [FLOW BASE] Initialized lastFlow baseline to:", currentFlow);
                         }
 
                         let lastF = dispenseSession.lastFlow;
                         let delta = 0;
                         if (currentFlow < lastF) {
-                            delta = currentFlow; // Hardware reset
+                            if (currentFlow < 100) {
+                                delta = currentFlow; // Real hardware reset to 0
+                            } else {
+                                delta = 0; // Lag or noise, ignore jump
+                            }
+                            dispenseSession.lastFlow = currentFlow; // Sync to new base
                         } else {
                             delta = currentFlow - lastF;
                         }
@@ -2783,6 +3669,8 @@
                             dispenseSession.accumulatedPulses = (Number(dispenseSession.accumulatedPulses) || 0) + delta;
                             dispenseSession.lastFlow = currentFlow;
                             dispenseSession.lastFlowUpdateTime = Date.now();
+                            lastFlowUpdateTime = Date.now(); // Reset global animation watchdog too!
+                            dispenseSession.pulseReceivedAfterStart = true; // 🔓 UNFREEZE GLIDE ANIMATION
                         }
 
                         let liters = Math.max(0, dispenseSession.accumulatedPulses / K_FACTOR);
@@ -2833,7 +3721,7 @@
                         if (scenarioLogs.length >= 100) {
                             scenarioActive = false;
                             downloadScenarioLogs();
-                            startPollTimer(500);
+                            startPollTimer(250);
                         }
                     }
                 }
@@ -2852,7 +3740,7 @@
         function buildPacket(boardId, subCmd, extraData) {
             // extraData = additional bytes after subCmd (optional)
             let data = [subCmd, ...(extraData || [])];
-            
+
             // 💡 Integration for continuous read/poll to Board 0xC3 (Flow/TDS board) to set cabinet light
             if (boardId === 0xC3 && subCmd === 0x52) {
                 const isLightOn = (lastLightState !== false);
@@ -2910,21 +3798,135 @@
 
         function forceStartAll() {
             logToScreen("<span style='background:#c0392b; color:white; font-weight:bold;'>🔥 [ADB_DEBUG] TRIGGERING FORCE START SEQUENCE...</span>");
-            sendPacket(0xC6, 0x53, [0x00, 100], 'FORCE-PUMP');
+            sendPacket(0xC6, 0x53, [0x02, 0x3A], 'FORCE-PUMP');
             setTimeout(() => {
                 sendPacket(0xC3, 0x53, [0x02, 0x3A], 'FORCE-VALVE'); // 570 pulses
             }, 600);
         }
 
+
+
+        window.syncSystemOverlay = function () {
+            const mOverlay = document.getElementById("kioskSystemOverlay");
+            const mBackdrop = document.getElementById("kioskSystemBackdrop");
+            if (!mOverlay) return;
+
+            const isDispensing = typeof dispenseSession !== 'undefined' && dispenseSession && dispenseSession.active;
+            if (isDispensing) {
+                mOverlay.classList.remove('active');
+                mOverlay.style.display = "none";
+                if (mBackdrop) { mBackdrop.classList.remove('active'); mBackdrop.style.display = "none"; }
+                return;
+            }
+
+            const netOk = window.isAppOnline !== false; // Default true
+            const hwOk = window.isHardwareOnline !== false;
+            const isLowWater = window.isLowWater === true;
+
+            if (netOk && hwOk && !isLowWater) {
+                mOverlay.classList.remove('active');
+                mOverlay.style.display = "none";
+                if (mBackdrop) { mBackdrop.classList.remove('active'); mBackdrop.style.display = "none"; }
+            } else {
+                let icon = "⚠️";
+                let color = "#ef4444";
+                let title = "งดบริการชั่วคราว";
+                let desc = "";
+
+                if (!netOk && !hwOk && isLowWater) {
+                    icon = "🛑";
+                    color = "#eab308";
+                    title = "ระบบขัดข้องรุนแรง (น้ำหมด)";
+                    desc = "ขาดการเชื่อมต่ออินเทอร์เน็ต, ไม่พบฮาร์ดแวร์บอร์ดควบคุม และน้ำในถังหมด";
+                } else if (!hwOk && isLowWater) {
+                    icon = "🛑";
+                    color = "#eab308";
+                    title = "ตู้ขัดข้อง + น้ำหมด";
+                    desc = "ขออภัย ตู้บริการน้ำดื่มอยู่ในช่วงการซ่อมบำรุงฮาร์ดแวร์ และน้ำในถังหมด";
+                } else if (!netOk && isLowWater) {
+                    icon = "💧";
+                    color = "#3498db";
+                    title = "น้ำหมด + ออฟไลน์";
+                    desc = "น้ำในถังหมดชั่วคราว และขาดการเชื่อมต่ออินเทอร์เน็ต";
+                } else if (isLowWater) {
+                    icon = "💧";
+                    color = "#3498db";
+                    title = "น้ำหมดชั่วคราว";
+                    desc = "ขออภัย น้ำในถังหมด กรุณารอระบบเติมน้ำสักครู่";
+                } else if (!netOk && !hwOk) {
+                    icon = "🛑";
+                    color = "#eab308";
+                    title = "ระบบขัดข้องรุนแรง";
+                    desc = "ขาดการเชื่อมต่ออินเทอร์เน็ต และ ไม่พบฮาร์ดแวร์บอร์ดควบคุม";
+                } else if (!hwOk) {
+                    icon = "🛑";
+                    color = "#f1c40f";
+                    title = "งดบริการชั่วคราว (ตู้ขัดข้อง)";
+                    desc = "ขออภัยในความไม่สะดวก<br>ตู้บริการน้ำดื่มอยู่ในช่วงการซ่อมบำรุงฮาร์ดแวร์";
+                } else if (!netOk) {
+                    icon = "⚠️";
+                    color = "#ef4444";
+                    title = "เชื่อมต่อเครือข่ายล้มเหลว";
+                    desc = "กรุณารอการเชื่อมต่ออินเทอร์เน็ตสักครู่<br>ระบบจะกลับมาทำงานอัตโนมัติ";
+                }
+
+                document.getElementById('kioskSystemIcon').innerText = icon;
+                document.getElementById('kioskSystemIcon').style.color = color;
+                mOverlay.style.borderColor = color;
+                document.getElementById('kioskSystemTitle').innerText = title;
+                document.getElementById('kioskSystemDesc').innerHTML = desc;
+
+                mOverlay.classList.add('active');
+                mOverlay.style.display = "flex";
+                if (mBackdrop) { mBackdrop.classList.add('active'); mBackdrop.style.display = "block"; }
+
+                const midDisp = document.getElementById('offlineMachineIdDisplay');
+                if (midDisp) {
+                    const stationId = localStorage.getItem('station_id') || (typeof DEVICE_ID !== 'undefined' ? DEVICE_ID : null);
+                    const hotlineEl = document.getElementById('hotlineMachineName');
+                    const hotlineName = (hotlineEl && hotlineEl.innerText && hotlineEl.innerText.trim() !== '' && hotlineEl.innerText !== '...') ? hotlineEl.innerText : null;
+                    midDisp.innerText = hotlineName || stationId || 'Unknown';
+                }
+            }
+        };
+
         function updateHwStatus(status) {
+            const isOnline = (status === 'active' || status === 'connected');
+            window.isHardwareOnline = isOnline;
+
+            // 🔌 HW TRANSITION LOGGING
+            if (window.lastRealHwOnlineState !== isOnline) {
+                const oldState = window.lastRealHwOnlineState;
+                window.lastRealHwOnlineState = isOnline;
+
+                if (oldState !== undefined && window.isAppOnline === true && typeof db !== 'undefined' && typeof addDoc !== 'undefined' && typeof collection !== 'undefined' && typeof serverTimestamp !== 'undefined') {
+                    try {
+                        const machineName = document.getElementById('hotlineMachineName')?.innerText || (typeof DEVICE_ID !== 'undefined' ? DEVICE_ID : 'Unknown');
+                        addDoc(collection(db, "machine_logs"), {
+                            machineId: typeof DEVICE_ID !== 'undefined' ? DEVICE_ID : 'Unknown',
+                            machineName: machineName,
+                            status: isOnline ? 'hardware_online' : 'hardware_offline',
+                            timestamp: serverTimestamp(),
+                            details: isOnline
+                                ? 'การเชื่อมต่อบอร์ดควบคุมฮาร์ดแวร์กลับมาใช้งานได้ปกติ (Serial Restored)'
+                                : 'การเชื่อมต่อบอร์ดควบคุมฮาร์ดแวร์ขัดข้อง (Serial Lost / Hardware Offline)'
+                        });
+                    } catch (e) {
+                        console.error("Failed to log HW transition:", e);
+                    }
+                }
+            }
+
             const dot = document.getElementById('hwStatusDot');
             const text = document.getElementById('hwStatusText');
             if (dot) {
-                dot.style.background = (status === 'active') ? '#00f2fe' : '#ef4444';
+                dot.style.background = isOnline ? '#00f2fe' : '#ef4444';
             }
             if (text) {
-                text.textContent = (status === 'active') ? 'Hardware Ready' : 'Hardware Offline';
+                text.textContent = isOnline ? 'Hardware Ready' : 'Hardware Offline';
             }
+
+            window.syncSystemOverlay();
         }
 
         // ── 4. DISPENSE LOGIC ──
@@ -2993,20 +3995,21 @@
 
             if (!dispenseSession.active || (!dispenseSession.wasRunning && !dispenseSession.paused)) {
                 // 🏁 STATE: READY
-                actionBtn = `<button id="startDispenseBtn" onclick="startDispensing()" style="background:#059669; color:white; border:none; padding:25px 10px; border-radius:20px; font-size:26px; font-weight:900; cursor:pointer; box-shadow:0 6px 15px rgba(5,150,105,0.3);">▶️ เริ่มจ่ายน้ำ</button>`;
-                finishBtn = `<button onclick="showScreen('standby')" style="background:#ef4444; color:white; border:none; padding:25px 10px; border-radius:20px; font-size:26px; font-weight:900; cursor:pointer; box-shadow:0 6px 15px rgba(239,68,68,0.3);">❌ ยกเลิก</button>`;
+                actionBtn = `<button id="startDispenseBtn" onclick="startDispensing()" style="width:100%; background:#059669; color:white; border:none; padding:25px; border-radius:20px; font-size:32px; font-weight:900; cursor:pointer; box-shadow:0 6px 20px rgba(5,150,105,0.3);">▶️ เริ่มจ่ายน้ำ</button>`;
+                finishBtn = `<button onclick="showScreen('standby')" style="width:100%; margin-top:15px; background:#ef4444; color:white; border:none; padding:20px; border-radius:20px; font-size:26px; font-weight:900; cursor:pointer; box-shadow:0 6px 15px rgba(239,68,68,0.3);">❌ ยกเลิก</button>`;
             } else if ((dispenseSession.active || document.getElementById("screen-dispense").classList.contains("active")) && !dispenseSession.paused) {
-                // 🌊 STATE: RUNNING — End button is LOCKED
-                actionBtn = `<button onclick="stopDispense()" style="background:#f59e0b; color:white; border:none; padding:25px 10px; border-radius:20px; font-size:26px; font-weight:900; cursor:pointer; box-shadow:0 6px 15px rgba(245,158,11,0.3);">⏸ หยุดชั่วคราว</button>`;
-                finishBtn = `<button disabled onclick="void(0)" style="background:#94a3b8; color:white; border:none; padding:25px 10px; border-radius:20px; font-size:22px; font-weight:900; cursor:not-allowed; opacity:0.45; pointer-events:none;" title="กด หยุดชั่วคราว ก่อนสิ้นสุดการทำงาน">🔒 กดหยุดก่อน</button>`;
+                // 🌊 STATE: RUNNING — End button is fully active (stops pump and finishes instantly)
+                actionBtn = `<button onclick="stopDispense()" style="width:100%; background:#f59e0b; color:white; border:none; padding:25px; border-radius:20px; font-size:32px; font-weight:900; cursor:pointer; box-shadow:0 6px 20px rgba(245,158,11,0.3);">⏸ หยุดชั่วคราว</button>`;
+                finishBtn = `<button onclick="endSession()" style="width:100%; margin-top:15px; background:#ef4444; color:white; border:none; padding:20px; border-radius:20px; font-size:26px; font-weight:900; cursor:pointer; box-shadow:0 6px 15px rgba(239,68,68,0.3);">❌ เลิกจ่ายน้ำ</button>`;
             } else if (dispenseSession.paused) {
                 // 🛑 STATE: PAUSED
-                actionBtn = `<button onclick="startDispensing()" style="background:#059669; color:white; border:none; padding:25px 10px; border-radius:20px; font-size:26px; font-weight:900; cursor:pointer; box-shadow:0 6px 15px rgba(5,150,105,0.3);">▶️ จ่ายน้ำ</button>`;
-                finishBtn = `<button onclick="endSession()" style="background:#1e293b; color:white; border:none; padding:25px 10px; border-radius:20px; font-size:26px; font-weight:900; cursor:pointer; box-shadow:0 6px 15px rgba(0,0,0,0.3);">✅ รับน้ำเรียบร้อย</button>`;
+                actionBtn = `<button onclick="startDispensing()" style="width:100%; background:#059669; color:white; border:none; padding:25px; border-radius:20px; font-size:32px; font-weight:900; cursor:pointer; box-shadow:0 6px 20px rgba(5,150,105,0.3);">▶️ จ่ายน้ำ</button>`;
+                finishBtn = `<button onclick="endSession()" style="width:100%; margin-top:15px; background:#1e293b; color:white; border:none; padding:20px; border-radius:20px; font-size:26px; font-weight:900; cursor:pointer; box-shadow:0 6px 15px rgba(0,0,0,0.3);">✅ รับน้ำเรียบร้อย</button>`;
             }
 
+            // Stack vertically: Action button on top, Cancel/Finish button directly underneath it
             container.innerHTML = `
-                <div style="display:grid; grid-template-columns: 1fr 1fr; gap:20px; width:100%;">
+                <div style="display:flex; flex-direction:column; align-items:center; width:100%;">
                     ${actionBtn}
                     ${finishBtn}
                 </div>
@@ -3014,18 +4017,41 @@
         }
 
         async function startDispensing() {
+            if (window.preventSpam('dispense-control', 1000)) return;
+            if (typeof currentScreenName !== 'undefined' && currentScreenName !== 'dispense') {
+                logToScreen("<span style='color:red;'>❌ ERROR: Cannot start dispensing outside dispensing screen</span>");
+                return;
+            }
             if (!dispenseSession) {
                 logToScreen("<span style='color:red;'>❌ ERROR: dispenseSession NOT INITIALIZED</span>");
                 return;
             }
 
+            if (dispenseSession.wasRunning && dispenseSession.targetReached) {
+                logToScreen("<span style='color:#e74c3c; font-weight:bold;'>🛡️ [SAFETY] BLOCKED startDispensing: Session limit already reached. Cannot resume!</span>");
+                return;
+            }
+
             const isResume = dispenseSession.active && dispenseSession.paused;
             window._isEnding = false; // 🔓 FORCE RESET: Ensure we can end this session later
             dispenseSession.paused = false;
             GLOBAL_STOP_SIGNAL = false;
+            
+            if (window._pauseTimeout) {
+                clearTimeout(window._pauseTimeout);
+                window._pauseTimeout = null;
+            }
             LOCKDOWN_MODE = false; // 🔓 UNLOCK IMMEDIATELY
             dispenseSession.active = true;
+            dispenseSession.pulseReceivedAfterStart = false; // 🛡️ START FLOW FREEZE GUARD
+            dispenseSession.baselineInitialized = false;
+            dispenseSession.lastFlow = undefined;
+            // 🚀 PERFECT GLIDE INITIALIZATION
+            window._activeFlowStartTime = Date.now();
+            window._activeFlowStartLiters = window._animLitersTarget || 0;
             dispenseSession.wasRunning = true;
+            // 🟦 Realtime status: notify Admin dashboard
+            try { if (window.db && window.DEVICE_ID) updateDoc(doc(window.db, "machines", window.DEVICE_ID), { isDispensing: true }); } catch (e) { }
 
             const subEl = document.getElementById('dispense-status-sub');
             if (subEl) subEl.textContent = "ระบบกำลังฆ่าเชื้อโรค";
@@ -3033,7 +4059,10 @@
             refreshDispenseButtons();
             logToScreen(`<span style='color:#f1c40f; font-weight:bold;'>🚀 ${isResume ? 'RESUMING' : 'STARTING'} DISPENSE</span>`);
 
-            let targetVol = typeof maxLiters !== 'undefined' ? maxLiters : (selectedLiters || 1);
+            // 🛡️ CRITICAL FIX: Default directly to maxLiters (the remaining quota) to ensure they get their exact balance!
+            let targetVol = (typeof maxLiters !== 'undefined' && maxLiters > 0) ? maxLiters : ((typeof selectedLiters !== 'undefined' && selectedLiters > 0) ? selectedLiters : 1);
+
+
             let totalTargetPulses = Math.round(targetVol * K_FACTOR);
             let pulsesToSend = isResume ? Math.max(0, totalTargetPulses - (dispenseSession.accumulatedPulses || 0)) : totalTargetPulses;
 
@@ -3042,12 +4071,20 @@
                 dispenseSession.targetReached = false; // 🚀 CRITICAL FIX: Reset for new session
                 dispenseSession.targetPulses = totalTargetPulses;
                 dispenseSession.targetLiters = targetVol;
-                dispenseSession.lastFlow = (telemetry && telemetry.flow) || (window.telemetry && window.telemetry.flow) || 0;
+                dispenseSession.lastFlow = undefined;
                 dispenseSession.startTime = new Date();
-                dispenseSession.lastFlowUpdateTime = Date.now(); // ✅ START TIMEOUT TRACKER
+
+                // RESET visual animation values for a clean start!
+                window._animLiters = 0;
+                window._animLitersTarget = 0;
+
                 if (window.currentKioskToken) window.notifyDispensingStart(window.currentKioskToken, targetVol);
             }
 
+            // 🚀 CRITICAL BUGFIX: Always reset the safety watchdog lastFlowUpdateTime on both start and resume!
+            // Otherwise, if paused for >15s, resuming instantly triggers the 15-second "No Flow Detected" Safety Stop.
+            dispenseSession.lastFlowUpdateTime = Date.now();
+
             let [bHi, bLo] = flowSetBytes(pulsesToSend);
             const delay = isResume ? 100 : 600;
 
@@ -3059,7 +4096,7 @@
                     return;
                 }
 
-                sendPacket(0xC6, 0x53, [0x00, 0x64], 'PUMP-START');
+                sendPacket(0xC6, 0x53, [bHi, bLo], 'PUMP-START');
 
                 setTimeout(() => {
                     if (GLOBAL_STOP_SIGNAL || LOCKDOWN_MODE) return;
@@ -3071,30 +4108,41 @@
                     sendPacket(0xC1, 0x53, [bHi, bLo], "MAIN-START");
                 }, 400);
 
-                startPollTimer(500);
+                startPollTimer(250);
                 window._startDispenseTimeout = null;
             }, delay);
         }
 
         function stopDispense() {
+            if (window.preventSpam('dispense-control', 1000)) return;
             // ️ confirm.mp3 voice removed per user request
             logToScreen("<span style='color:#e74c3c; font-weight:bold;'>🛑 STOP TRIGGERED</span>");
             sendPacket(0xC6, 0x50, [0x00, 0x00], 'PUMP-STOP');
             setTimeout(() => sendPacket(0xC3, 0x50, [0x00, 0x00], 'VALVE-STOP'), 200);
 
             dispenseSession.paused = true;
-            
+
+            // ⏳ Auto-terminate session if paused for 2 minutes (120,000 ms)
+            if (window._pauseTimeout) clearTimeout(window._pauseTimeout);
+            window._pauseTimeout = setTimeout(() => {
+                if (dispenseSession && dispenseSession.paused) {
+                    logToScreen("<span style='color:#e74c3c; font-weight:bold;'>⏳ AUTO-TIMEOUT: Session terminated due to 2-minute inactivity</span>");
+                    endSession();
+                }
+            }, 120000);
+
             // 🚀 SNAP UI: Force animation to catch up to actual pulses immediately
             const actualL = (Number(dispenseSession.accumulatedPulses) || 0) / K_FACTOR;
             window._animLiters = actualL;
             window._animLitersTarget = actualL;
-            
-            startPollTimer(500);
+
+            startPollTimer(250);
             refreshDispenseButtons();
             updateDispenseScreen(actualL, dispenseSession.targetLiters, dispenseSession.accumulatedPulses);
         }
 
         function endSession() {
+            if (window.preventSpam('dispense-control', 1000)) return;
             if (window._isEnding) {
                 console.warn("⚠️ [SESSION] endSession already in progress, ignoring click.");
                 return;
@@ -3120,6 +4168,11 @@
                 window._startDispenseTimeout = null;
             }
 
+            if (window._pauseTimeout) {
+                clearTimeout(window._pauseTimeout);
+                window._pauseTimeout = null;
+            }
+
             if (window._sessionUnsubscribe) {
                 console.log("🛑 KILLING FIREBASE LISTENER...");
                 window._sessionUnsubscribe();
@@ -3130,7 +4183,7 @@
 
             let finalL = Math.max(0, (Number(dispenseSession.accumulatedPulses) || 0) / K_FACTOR);
             dispenseSession.actualLiters = finalL;
-            
+
             // 🚀 SNAP UI: Ensure "Thank You" screen uses the exact same value
             window._animLiters = finalL;
             window._animLitersTarget = finalL;
@@ -3166,22 +4219,42 @@
             let leftEl = document.getElementById('thanksLeft');
             if (leftEl) leftEl.textContent = `${remainL.toFixed(2)} ลิตร`;
 
+            // 🛡️ FIX: Capture currentToken BEFORE showScreen clears it!
+            let finalToken = currentToken || "MANUAL";
+            if (window.updateSessionResult) window.updateSessionResult(finalToken, finalL);
+
             showScreen('thankyou');
             startResetCountdown();
-            if (window.updateSessionResult) window.updateSessionResult(currentToken || "MANUAL", finalL);
             window.currentKioskToken = null;
             currentToken = null;
+
+            // 🚀 CRITICAL FIX: Reset the global dispenseSession object so the next session starts with a clean slate!
+            dispenseSession = {
+                active: false,
+                paused: false,
+                wasRunning: false,
+                targetReached: false,
+                accumulatedPulses: 0,
+                targetLiters: 0,
+                targetPulses: 0,
+                lastFlow: 0
+            };
+            // 🟢 Realtime status: clear dispensing flag on Admin dashboard
+            try { if (window.db && window.DEVICE_ID) updateDoc(doc(window.db, "machines", window.DEVICE_ID), { isDispensing: false }); } catch (e) { }
+            selectedLiters = 0;
         }
 
         function startResetCountdown() {
             let sec = 7;
             const bar = document.getElementById('resetBar');
             const cd = document.getElementById('resetCountdown');
-            let timer = setInterval(() => {
+            if (window._resetCountdownTimer) clearInterval(window._resetCountdownTimer);
+            window._resetCountdownTimer = setInterval(() => {
                 cd.textContent = sec;
                 bar.style.width = (sec / 7) * 100 + '%';
                 if (sec-- <= 0) {
-                    clearInterval(timer);
+                    clearInterval(window._resetCountdownTimer);
+                    window._resetCountdownTimer = null;
                     showScreen('standby');
                 }
             }, 1000);
@@ -3232,21 +4305,48 @@
             document.querySelectorAll('.clock-display').forEach(el => el.textContent = timeStr);
             document.querySelectorAll('.date-display').forEach(el => el.textContent = dateStr);
 
-            // 💡 Auto Light Schedule (18:00 - 06:00)
+            // 💡 Auto Light Schedule (Stateful sync for any boot time)
             const hour = now.getHours();
-            const min = now.getMinutes();
-            if (hour === 18 && min === 0 && lastLightState !== true) {
-                logToScreen("⏰ AUTO: 18:00 - TURNING LIGHT ON");
-                controlLight(true);
+            const onHr = (window._lightOnHour !== undefined) ? window._lightOnHour : 18;
+            const offHr = (window._lightOffHour !== undefined) ? window._lightOffHour : 6;
+
+            let shouldBeOn = false;
+            if (onHr > offHr) { // e.g. ON at 18:00, OFF at 06:00
+                shouldBeOn = (hour >= onHr || hour < offHr);
+            } else {            // e.g. ON at 06:00, OFF at 18:00
+                shouldBeOn = (hour >= onHr && hour < offHr);
             }
-            if (hour === 6 && min === 0 && lastLightState !== false) {
-                logToScreen("⏰ AUTO: 06:00 - TURNING LIGHT OFF");
-                controlLight(false);
+
+            if (shouldBeOn && lastLightState !== true) {
+                if (window.isHardwareOnline) {
+                    logToScreen(`⏰ AUTO SYNC: Time is within ON schedule (${onHr}:00 - ${offHr}:00)`);
+                    controlLight(true);
+                }
+            } else if (!shouldBeOn && lastLightState !== false) {
+                if (window.isHardwareOnline) {
+                    logToScreen(`⏰ AUTO SYNC: Time is within OFF schedule (${offHr}:00 - ${onHr}:00)`);
+                    controlLight(false);
+                }
             }
         }
         var latestFlowCount = 0; var GLOBAL_STOP_SIGNAL = false; var LOCKDOWN_MODE = false; var LOCKDOWN_MODE = false; // TRACK GLOBAL PULSE COUNT FOR ABSOLUTE TARGETS
         setInterval(updateClock, 30000); updateClock();
 
+        // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
+        // 💓 NATIVE ANDROID WATCHDOG HEARTBEAT
+        // Feeds MainActivity.java HardwareWatchdogTimer (kills at 90s)
+        // Must be separate from Firestore sendHeartbeat() above
+        // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
+        function pingNativeWatchdog() {
+            try {
+                if (window.AndroidSerial && typeof window.AndroidSerial.sendHeartbeat === 'function') {
+                    window.AndroidSerial.sendHeartbeat();
+                }
+            } catch (e) { /* silent — non-critical */ }
+        }
+        setInterval(pingNativeWatchdog, 30000); // 30s << 90s kill threshold (3x safety margin)
+        setTimeout(pingNativeWatchdog, 3000);   // Initial ping after 3s on boot
+
         async function fullPortSweep() {
             logToScreen("<span style='color:#f1c40f; font-weight:bold;'>🛰️ [COMMAND] NATIVE SWEEP INITIATED...</span>");
             const ports = ['/dev/ttyS0', '/dev/ttyS1', '/dev/ttyS2', '/dev/ttyS3', '/dev/ttyS9', '/dev/ttyUSB0', '/dev/ttyHSL0'];
@@ -3303,20 +4403,29 @@
         }
 
         let _pollRecursiveTimer = null;
+        let _lastTdsPollTime = 0;
         function startPollTimer() {
             if (_pollRecursiveTimer) clearTimeout(_pollRecursiveTimer);
             const runPoll = () => {
                 const isActive = !GLOBAL_STOP_SIGNAL && dispenseSession.active && !dispenseSession.paused && !dispenseSession.targetReached;
-                const delay = isActive ? 500 : 2000;
-                sendPacket(0xC3, 0x52, [], "POLL-TDS");
+                const delay = isActive ? 250 : 1000; // Poll every 500ms during active dispensing, 1000ms during idle
+
                 if (isActive) {
-                    let [pHi, pLo] = flowSetBytes(Math.max(0, (dispenseSession.targetPulses || 0) - (dispenseSession.accumulatedPulses || 0)));
-                    sendPacket(0xC1, 0x53, [pHi, pLo], "POLL-FLOW");
+                    // 🚀 POLL BOARD 0xC3 (TDS/Flow board) every 250ms during active dispensing.
+                    // This forces the board to reply with C1 telemetry packet every 250ms, updating Flow and TDS values!
+                    sendPacket(0xC3, 0x52, [], "POLL-FLOW");
+                    _lastTdsPollTime = Date.now(); // Suppress redundant POLL-TDS
+
                     if (dispenseSession.lastFlowUpdateTime && (Date.now() - dispenseSession.lastFlowUpdateTime > 15000)) {
                         logToScreen('<span style="background:#e74c3c; color:white; padding:5px; border-radius:5px; font-weight:bold; display:block; margin:10px 0;">🛡️ SAFETY STOP: NO FLOW DETECTED FOR 15s</span>');
                         manualStop();
                     }
                 } else {
+                    // Idle/Standby Mode: Decouple TDS polling to run only once every 2 seconds to avoid serial bus flood
+                    if (Date.now() - _lastTdsPollTime >= 2000) {
+                        sendPacket(0xC3, 0x52, [], "POLL-TDS");
+                        _lastTdsPollTime = Date.now();
+                    }
                     sendPacket(0xC1, 0x52, [], "POLL-C1");
                 }
                 _pollRecursiveTimer = setTimeout(runPoll, delay);
@@ -3383,10 +4492,10 @@
 
                 if (!dispenseSession.continuous && !dispenseSession.targetReached && dispenseSession.targetLiters > 0) {
                     // Safety check: Software-side stop only as a fallback
-                    // ADDED: 1-second delay and LOOSER margin (+0.5L) to prevent frustration
+                    // TIGHTENED: Trigger at targetLiters + 0.05 (50ml margin) for extremely precise cutoff
                     if (new Date() - dispenseSession.startTime > 2000) {
-                        if (parseFloat(displayLiters) >= dispenseSession.targetLiters + 0.50) {
-                            logToScreen(`<span style="background:#e74c3c;color:white;padding:2px 5px;border-radius:3px;font-weight:bold;">📍 SW STOP TRIGGERED (Safety Limit +0.5L)</span>`);
+                        if (parseFloat(displayLiters) >= dispenseSession.targetLiters + 0.05) {
+                            logToScreen(`<span style="background:#e74c3c;color:white;padding:2px 5px;border-radius:3px;font-weight:bold;">📍 SW STOP TRIGGERED (Safety Limit +0.05L)</span>`);
                             dispenseSession.targetReached = true;
                             manualStop();
                         }
@@ -3395,9 +4504,9 @@
 
                 // 🏁 2. FINALIZE (Wait for board Ready or Stopped)
                 // 🛑 UNSTOPPABLE MODE: Tablet only ends when IT decides, ignoring board 'P'/'E' status
-                if (dispenseSession.targetReached && !dispenseSession.paused) {
+                if (dispenseSession.targetReached) {
                     logToScreen(`<span style="background:#27ae60;color:white;padding:2px 5px;border-radius:3px;">✅ TARGET REACHED — SYNCING TO CLOUD</span>`);
-                    LOCKDOWN_MODE = true; GLOBAL_STOP_SIGNAL = true; dispenseSession.active = false; setTimeout(() => { LOCKDOWN_MODE = false; }, 10000);
+                    LOCKDOWN_MODE = true; GLOBAL_STOP_SIGNAL = true; dispenseSession.active = false; setTimeout(() => { LOCKDOWN_MODE = false; }, 1500);
                     endSession();
                 }
             }
@@ -3409,34 +4518,34 @@
         window._animTargetVol = 0;
         window._animFrameId = null;
 
-        function renderSmoothLiters() {
-            // Smooth easing
-            let diff = window._animLitersTarget - window._animLiters;
-            if (Math.abs(diff) < 0.005) {
-                window._animLiters = window._animLitersTarget;
-            } else {
-                window._animLiters += diff * 0.15; // easing speed
-            }
+        function renderSmoothLiters(timestamp) {
+            // 🎯 Snap directly to actual pulse count litres
+            window._animLiters = window._animLitersTarget;
 
             // Update DOM texts
             let dispEl = document.getElementById('dispenseLitersDisplay');
             if (dispEl) dispEl.textContent = window._animLiters.toFixed(2);
             let nowEl = document.getElementById('dispenseNow');
-            if (nowEl) nowEl.textContent = `${window._animLiters.toFixed(2)}`;
+            if (nowEl) nowEl.textContent = window._animLiters.toFixed(2);
 
             // Water fill animation (0–100% based on target)
             let pct = 0;
             if (window._animTargetVol && window._animTargetVol !== '∞') {
-                pct = Math.min(100, (window._animLiters / parseFloat(window._animTargetVol)) * 100);
+                const target = parseFloat(window._animTargetVol);
+                if (target <= 5) {
+                    pct = Math.min(100, (window._animLiters / target) * 100);
+                } else {
+                    // Loop animation every 5 liters if target is very large
+                    pct = Math.min(100, ((window._animLiters % 5) / 5) * 100);
+                }
             } else {
-                pct = Math.min(100, (window._animLiters / 5) * 100);
+                pct = Math.min(100, ((window._animLiters % 5) / 5) * 100);
             }
             let fill = document.getElementById('waterFill');
-            if (fill) fill.style.height = pct.toFixed(1) + '%';
+            if (fill) fill.style.height = pct.toFixed(3) + "%";
 
             let leftEl = document.getElementById('dispenseLeft');
             if (leftEl) {
-                // `dispenseLeft` must show the user's total remaining quota (window.maxLiters), not just the selected target
                 let totalQuota = window.maxLiters || window._animTargetVol;
                 let remaining = totalQuota && totalQuota !== '∞'
                     ? Math.max(0, parseFloat(totalQuota) - window._animLiters).toFixed(2)
@@ -3444,26 +4553,63 @@
                 leftEl.textContent = remaining;
             }
 
-            // Continue loop if not reached target yet
-            if (Math.abs(window._animLiters - window._animLitersTarget) >= 0.001) {
-                window._animFrameId = requestAnimationFrame(renderSmoothLiters);
-            } else {
-                window._animFrameId = null;
-            }
+            // Immediately stop loop since we snapped to target
+            window._animFrameId = null;
         }
 
         // Update the main kiosk dispense screen to match test session
+
+        let _lastAnimTime = 0;
+        let currentFlowRateLps = 0;
+        let lastFlowUpdateLiters = 0;
+        let lastFlowUpdateTime = Date.now();
+
         function updateDispenseScreen(litresNow, targetLiters, pulsesNow) {
-            // 🚀 SYNC: Update live volume to Firebase for mobile view (THROTTLED to every 2 seconds)
+            let now = Date.now();
+
+            // 🛡️ Debug Flow display update in updateDispenseScreen
+            let debugFlowEl = document.getElementById('dispenseFlowVal');
+            if (debugFlowEl) {
+                let rawF = (typeof telemetry !== 'undefined' && telemetry.flow !== undefined) ? telemetry.flow : 0;
+                let accF = pulsesNow !== undefined ? pulsesNow : 0;
+                debugFlowEl.textContent = rawF + " / " + accF;
+            }
+
+            // ONLY calculate flow rate and reset timer when volume ACTUALLY changes!
+            // (Prevents polling functions from destroying the time delta)
+            if (litresNow > lastFlowUpdateLiters) {
+                let dtSeconds = (now - lastFlowUpdateTime) / 1000.0;
+
+                if (dtSeconds > 0.05 && dtSeconds < 5.0) {
+                    let instFlow = (litresNow - lastFlowUpdateLiters) / dtSeconds;
+                    // Clamp instFlow to 1.0 Lps to filter out arrival jitter spikes
+                    instFlow = Math.min(1.0, Math.max(0, instFlow));
+                    // Smooth flow rate using heavier EMA (0.85 history / 0.15 new) for ultimate stability
+                    currentFlowRateLps = (currentFlowRateLps === 0) ? instFlow : (currentFlowRateLps * 0.93 + instFlow * 0.07);
+                }
+                lastFlowUpdateLiters = litresNow;
+                lastFlowUpdateTime = now;
+            } else if (now - lastFlowUpdateTime >= 3000) {
+                currentFlowRateLps = 0; // Reset if inactive for 3s
+            }
+
+            // 🚀 SYNC: Suppressed during active dispensing per user request to eliminate network/CPU lag!
+            // We only need to send the final volume once when dispensing stops (via updateSessionResult).
+            /* 
             const now = Date.now();
             if (typeof syncLiveVolToFirebase === 'function' && (!dispenseSession.lastFirebaseSync || now - dispenseSession.lastFirebaseSync > 2000)) {
                 syncLiveVolToFirebase(litresNow);
                 dispenseSession.lastFirebaseSync = now;
             }
+            */
 
-            // Snap if starting fresh or if value goes backwards
-            if (litresNow === 0 || window._animLiters > litresNow) {
-                window._animLiters = litresNow;
+            // Snap ONLY if starting fresh
+            if (litresNow === 0) {
+                window._animLiters = 0;
+                lastFlowUpdateLiters = 0;
+                lastFlowUpdateTime = Date.now();
+                currentFlowRateLps = 0;
+                _lastAnimTime = 0;
             }
 
             window._animLitersTarget = litresNow;
@@ -3485,7 +4631,7 @@
             let targetPulses = Math.round(liters * K_FACTOR);
             maxLiters = liters;
             dispenseSession = {
-                active: false, wasRunning: false, startFlowCaptured: false, targetReached: true,
+                active: false, wasRunning: false, startFlowCaptured: false, targetReached: false,
                 targetLiters: liters, targetPulses: targetPulses,
                 startFlow: 0, startTime: new Date(), maxFlow: 0
             };
@@ -3527,52 +4673,505 @@
 
         function manualRead() { sendPacket(0xC3, 0x52, [], 'READ'); }
         function manualStart() {
-            logToScreen('<span style="color:#2ecc71; font-weight:bold;">🛰️ UI Navigated to Select Volume (1-10L)</span>');
-            showScreen('select-volume');
+            logToScreen('<span style="color:#2ecc71; font-weight:bold;">🛰️ UI Navigated to Dispense Screen (Remaining Quota)</span>');
+            selectedLiters = maxLiters || 2;
+            showScreen('dispense');
         }
 
-        function sendContinuousStart() {
-            let [fsHi, fsLo] = flowSetBytes(65000); // Enormous target for continuous flow (114L)
-            sendPacket(0xC6, 0x53, [0x00, 0x64], 'START-CONT-PUMP');
-            setTimeout(() => sendPacket(0xC3, 0x53, [fsHi, fsLo], 'START-CONT'), 50);
-            startPollTimer(500); // Ensure single poll timer
-        }
-
-        function openVolumeSelection(maxVol) {
-            // 🛡️ NAVIGATION GUARD: Avoid redundant UI reloads if already in choice/dispense phase
-            const currentActive = document.querySelector('.screen.active');
-            if (currentActive && (currentActive.id === 'select-volume' || currentActive.id === 'screen-dispense')) {
-                console.log("[SYNC] Guard: Already in selection/dispensing. Ignoring redundant navigation.");
+        window.isStressTesting = false;
+        window.runAutoStressTest = async function () {
+            if (window.isStressTesting) {
+                window.isStressTesting = false;
+                logToScreen("<span style='color:red; font-weight:bold;'>🛑 STRESS TEST CANCELLED BY USER</span>");
                 return;
             }
+            window.isStressTesting = true;
+            logToScreen("<span style='color:#e67e22; font-weight:bold;'>🧪 STARTING AUTO STRESS TEST (100 CASES)...</span>");
 
-            // 1. Reset Global Limits
-            maxLiters = maxVol || 2;
-            localStorage.setItem('MAX_QUOTA', maxLiters);
+            const btn = document.getElementById('btnStressTest');
+            if (btn) {
+                btn.style.background = "#27ae60";
+                btn.textContent = "🛑 STOP STRESS";
+            }
 
-            currentMaxQuota = maxVol;
-            const buttons = document.querySelectorAll('.vol-btn');
-            buttons.forEach(btn => {
-                const vol = parseFloat(btn.getAttribute('data-vol'));
-                if (vol > maxVol) {
-                    btn.disabled = true;
-                    btn.style.opacity = '0.2';
-                    btn.style.filter = 'grayscale(1)';
-                    btn.style.pointerEvents = 'none';
-                } else {
-                    btn.disabled = false;
-                    btn.style.opacity = '1';
-                    btn.style.filter = 'none';
-                    btn.style.pointerEvents = 'auto';
+            const sleep = ms => new Promise(r => setTimeout(r, ms));
+
+            try {
+                for (let i = 1; i <= 100; i++) {
+                    if (!window.isStressTesting) break;
+
+                    logToScreen(`<span style="background:#d35400; color:white; padding:2px 6px; font-weight:bold; border-radius:3px;">CASE ${i}/100</span>`);
+
+                    // 1. Initiate 5L dispense
+                    logToScreen("🧪 [TEST] Setting up 5L dispense...");
+                    testDispenseV2(5.0);
+                    await sleep(800);
+
+                    if (!window.isStressTesting) break;
+
+                    // 2. Click Start Dispense
+                    logToScreen("🧪 [TEST] Clicking START DISPENSE...");
+                    window._clickLocks = {}; // Clear spam block for test
+                    await startDispensing();
+
+                    // 3. Wait random duration (500ms - 2500ms) to simulate water flow
+                    let waitFlow = 500 + Math.floor(Math.random() * 2000);
+                    logToScreen(`🧪 [TEST] Dispensing water... waiting ${waitFlow}ms`);
+                    await sleep(waitFlow);
+
+                    if (!window.isStressTesting) break;
+
+                    // Simulate some pulses coming in
+                    if (dispenseSession && dispenseSession.active) {
+                        dispenseSession.accumulatedPulses += 150 + Math.floor(Math.random() * 400);
+                        if (window.telemetry) window.telemetry.flow = (window.telemetry.flow || 0) + dispenseSession.accumulatedPulses;
+                    }
+
+                    // 4. Click Pause / Stop
+                    logToScreen("🧪 [TEST] Clicking STOP/PAUSE...");
+                    window._clickLocks = {}; // Clear spam block for test
+                    stopDispense();
+                    await sleep(800);
+
+                    if (!window.isStressTesting) break;
+
+                    // 5. Wait in paused state for a bit (500ms - 1500ms)
+                    let waitPause = 500 + Math.floor(Math.random() * 1000);
+                    logToScreen(`🧪 [TEST] Paused... waiting ${waitPause}ms`);
+                    await sleep(waitPause);
+
+                    if (!window.isStressTesting) break;
+
+                    // 6. Resume Dispensing
+                    logToScreen("🧪 [TEST] Clicking RESUME...");
+                    window._clickLocks = {}; // Clear spam block for test
+                    await startDispensing();
+
+                    // 7. Dispense a bit more
+                    let waitFlow2 = 500 + Math.floor(Math.random() * 1500);
+                    logToScreen(`🧪 [TEST] Dispensing again... waiting ${waitFlow2}ms`);
+                    await sleep(waitFlow2);
+
+                    if (!window.isStressTesting) break;
+
+                    // Simulate more pulses
+                    if (dispenseSession && dispenseSession.active) {
+                        dispenseSession.accumulatedPulses += 100 + Math.floor(Math.random() * 300);
+                        if (window.telemetry) window.telemetry.flow = (window.telemetry.flow || 0) + dispenseSession.accumulatedPulses;
+                    }
+
+                    // 8. Click End & Save
+                    logToScreen("🧪 [TEST] Clicking END & SAVE...");
+                    window._clickLocks = {}; // Clear spam block for test
+                    endSession();
+                    await sleep(1500); // Wait for transition back to home/idle
                 }
-            });
-            showScreen('select-volume');
+
+                if (window.isStressTesting) {
+                    logToScreen("<span style='color:#2ecc71; font-weight:bold; font-size:14px;'>🎉 STRESS TEST COMPLETED: 100/100 CASES PASSED!</span>");
+                }
+            } catch (err) {
+                logToScreen(`<span style="background:red; color:white; font-weight:bold;">❌ STRESS TEST FAILED: ${err.message}</span>`);
+                console.error(err);
+            } finally {
+                window.isStressTesting = false;
+                if (btn) {
+                    btn.style.background = "#d35400";
+                    btn.textContent = "🧪 STRESS TEST 100x";
+                }
+            }
+        };
+
+        window.isSpamTesting = false;
+        window.runSpamStressTest = async function () {
+            if (window.isSpamTesting) {
+                window.isSpamTesting = false;
+                logToScreen("<span style='color:red; font-weight:bold;'>🛑 SPAM TEST CANCELLED BY USER</span>");
+                return;
+            }
+            window.isSpamTesting = true;
+            logToScreen("<span style='color:#9b59b6; font-weight:bold;'>💥 STARTING SPAM STRESS TEST (20 CASES)...</span>");
+
+            const btn = document.getElementById('btnSpamTest');
+            if (btn) {
+                btn.style.background = "#27ae60";
+                btn.textContent = "🛑 STOP SPAM";
+            }
+
+            const sleep = ms => new Promise(r => setTimeout(r, ms));
+
+            try {
+                for (let i = 1; i <= 20; i++) {
+                    if (!window.isSpamTesting) break;
+
+                    logToScreen(`<span style="background:#8e44ad; color:white; padding:2px 6px; font-weight:bold; border-radius:3px;">SPAM CASE ${i}/20</span>`);
+
+                    // 1. Setup 5L
+                    logToScreen("💥 [SPAM] Setting up 5L dispense...");
+                    testDispenseV2(5.0);
+                    await sleep(600);
+
+                    if (!window.isSpamTesting) break;
+
+                    // 2. Click Start + Spam clicking
+                    logToScreen("💥 [SPAM] Clicking START DISPENSE & spamming clicks...");
+                    startDispensing(); // 1st click
+                    await sleep(30);
+                    startDispensing(); // 2nd click (Spam)
+                    await sleep(60);
+                    startDispensing(); // 3rd click (Spam)
+                    await sleep(600);
+
+                    // 3. Simulate Flow
+                    let waitFlow = 500 + Math.floor(Math.random() * 1500);
+                    logToScreen(`💥 [SPAM] Dispensing... waiting ${waitFlow}ms`);
+                    await sleep(waitFlow);
+
+                    if (dispenseSession && dispenseSession.active) {
+                        dispenseSession.accumulatedPulses += 150 + Math.floor(Math.random() * 300);
+                    }
+
+                    if (!window.isSpamTesting) break;
+
+                    // 4. Click Stop + Spam clicking
+                    logToScreen("💥 [SPAM] Clicking STOP/PAUSE & spamming clicks...");
+                    stopDispense(); // 1st click
+                    await sleep(40);
+                    stopDispense(); // 2nd click (Spam)
+                    await sleep(800);
+
+                    // 5. Wait in paused state
+                    let waitPause = 400 + Math.floor(Math.random() * 800);
+                    logToScreen(`💥 [SPAM] Paused... waiting ${waitPause}ms`);
+                    await sleep(waitPause);
+
+                    if (!window.isSpamTesting) break;
+
+                    // 6. Click Resume + Spam clicking
+                    logToScreen("💥 [SPAM] Clicking RESUME & spamming clicks...");
+                    startDispensing(); // 1st click
+                    await sleep(50);
+                    startDispensing(); // 2nd click (Spam)
+                    await sleep(600);
+
+                    // 7. Dispense more
+                    let waitFlow2 = 400 + Math.floor(Math.random() * 1000);
+                    await sleep(waitFlow2);
+
+                    if (dispenseSession && dispenseSession.active) {
+                        dispenseSession.accumulatedPulses += 100 + Math.floor(Math.random() * 200);
+                    }
+
+                    if (!window.isSpamTesting) break;
+
+                    // 8. End session + Spam clicking
+                    logToScreen("💥 [SPAM] Clicking END & spamming clicks...");
+                    endSession(); // 1st click
+                    await sleep(30);
+                    endSession(); // 2nd click (Spam)
+                    await sleep(1500);
+                }
+
+                if (window.isSpamTesting) {
+                    logToScreen("<span style='color:#2ecc71; font-weight:bold; font-size:14px;'>🎉 SPAM STRESS TEST COMPLETED: 20/20 CASES PASSED!</span>");
+                }
+            } catch (err) {
+                logToScreen(`<span style="background:red; color:white; font-weight:bold;">❌ SPAM TEST FAILED: ${err.message}</span>`);
+                console.error(err);
+            } finally {
+                window.isSpamTesting = false;
+                if (btn) {
+                    btn.style.background = "#8e44ad";
+                    btn.textContent = "💥 SPAM TEST 20x";
+                }
+            }
+        };
+
+        window.isLimitTesting = false;
+        window.runQuotaLimitStressTest = async function () {
+            if (window.isLimitTesting) {
+                window.isLimitTesting = false;
+                logToScreen("<span style='color:red; font-weight:bold;'>🛑 LIMIT TEST CANCELLED BY USER</span>");
+                return;
+            }
+            window.isLimitTesting = true;
+            logToScreen("<span style='color:#16a085; font-weight:bold;'>🛡️ STARTING 20x QUOTA LIMIT TEST (REPEATED PAUSE/RESUME)...</span>");
+
+            const btn = document.getElementById('btnLimitTest');
+            if (btn) {
+                btn.style.background = "#27ae60";
+                btn.textContent = "🛑 STOP LIMIT";
+            }
+
+            const sleep = ms => new Promise(r => setTimeout(r, ms));
+
+            try {
+                for (let round = 1; round <= 20; round++) {
+                    if (!window.isLimitTesting) break;
+
+                    logToScreen(`<span style="background:#16a085; color:white; padding:2px 6px; font-weight:bold; border-radius:3px;">LIMIT CASE ${round}/20</span>`);
+
+                    // Simulating regular quota initialization (no specific liter selection, remaining quota > 2L)
+                    const mockQuota = 10.0;
+                    window.maxLiters = Math.min(2.0, mockQuota); // Strictly capped at 2.0L
+                    window.selectedLiters = 0; // Simulated: no specific preset chosen, use general quota
+
+                    logToScreen(`🛡️ [LIMIT] Initialized regular quota: remaining = ${mockQuota}L, maxLimit = ${window.maxLiters}L`);
+
+                    // Open dispensing screen using maxLiters
+                    testDispenseV2(window.maxLiters);
+                    await sleep(1000);
+
+                    if (!window.isLimitTesting) break;
+
+                    // Start dispensing
+                    logToScreen("🛡️ [LIMIT] Clicking START DISPENSE...");
+                    window._clickLocks = {}; // Clear spam block for test
+                    await startDispensing();
+                    await sleep(500);
+
+                    // We will perform cycles of Dispense -> Pause -> Resume
+                    // adding ~0.3L per cycle until we exceed 2.0L, verifying it forces auto-stop.
+                    let lastPauseVolume = 0;
+                    let success = false;
+                    while (window.isLimitTesting && dispenseSession && dispenseSession.active) {
+                        if (dispenseSession.paused) {
+                            // If paused, just wait and do nothing (the resume step below will handle it)
+                            await sleep(100);
+                            continue;
+                        }
+
+                        // 1. Simulate flow by adding 0.02L of pulses
+                        let stepVol = 0.02;
+                        let addPulses = Math.round(stepVol * K_FACTOR);
+                        dispenseSession.accumulatedPulses += addPulses;
+                        dispenseSession.lastFlowUpdateTime = Date.now(); // Reset inactivity watchdog!
+
+                        if (window.telemetry) {
+                            window.telemetry.flow = (window.telemetry.flow || 0) + addPulses;
+                        }
+
+                        let newL = (dispenseSession.accumulatedPulses || 0) / K_FACTOR;
+
+                        // Update the Main UI Screen
+                        updateDispenseScreen(newL, dispenseSession.targetLiters, dispenseSession.accumulatedPulses);
+
+                        // Trigger telemetry console update to check targetReached
+                        updateTestConsoleDisplay(window.telemetry || telemetry);
+
+                        // If volume exceeds target (2.0L), trigger the primary auto-stop
+                        if (newL >= window.maxLiters && !dispenseSession.targetReached) {
+                            logToScreen("<span style='color:#2ecc71; font-weight:bold;'>🏁 TARGET REACHED: Automatic Stop Triggered!</span>");
+                            dispenseSession.targetReached = true;
+                            stopDispense();
+                            setTimeout(() => {
+                                window._isEnding = false; // Reset lock to allow endSession
+                                endSession();
+                            }, 1000);
+                        }
+
+                        // Check if we should pause (every 0.3L)
+                        if (newL - lastPauseVolume >= 0.3 && newL < window.maxLiters - 0.1) {
+                            logToScreen(`🛡️ [LIMIT] Pause Trigger: Volume = ${newL.toFixed(3)}L. Pausing...`);
+                            window._clickLocks = {}; // Clear spam block for test
+                            stopDispense();
+                            await sleep(800);
+
+                            if (!window.isLimitTesting || !dispenseSession.active) break;
+
+                            logToScreen("🛡️ [LIMIT] Resuming...");
+                            window._clickLocks = {}; // Clear spam block for test
+                            await startDispensing();
+                            dispenseSession.lastFlowUpdateTime = Date.now(); // Reset inactivity watchdog!
+                            await sleep(500);
+
+                            lastPauseVolume = newL;
+                        }
+
+                        await sleep(50); // fast flow simulation
+                    }
+
+                    // ── VERIFY OUTSIDE THE LOOP ──
+                    let finalL = (dispenseSession.accumulatedPulses || 0) / K_FACTOR;
+                    if (window.isLimitTesting && (finalL >= window.maxLiters || dispenseSession.targetReached)) {
+                        logToScreen(`🛡️ [LIMIT] Target reached: ${finalL.toFixed(3)}L. Checking auto-stop...`);
+                        await sleep(1500); // Wait for the scheduled endSession to run
+                        if (!dispenseSession.active) {
+                            logToScreen(`<span style='color:#2ecc71; font-weight:bold; font-size:14px;'>🎉 SUCCESS: Session automatically stopped! Final Volume: ${finalL.toFixed(3)}L (Safely capped at limit!)</span>`);
+
+                            // Attempt to force-resume
+                            logToScreen("🛡️ [LIMIT] Verification: Attempting to force-resume after cutoff...");
+                            window._clickLocks = {}; // Clear spam block for test
+                            await startDispensing();
+                            await sleep(500);
+
+                            if (!dispenseSession.active) {
+                                logToScreen("<span style='color:#2ecc71; font-weight:bold;'>🛡️ [LIMIT] Blocked successfully! Dispense did not restart.</span>");
+                                success = true;
+                            } else {
+                                logToScreen("<span style='color:red; font-weight:bold;'>❌ FAILURE: Safety violation! Dispense restarted after reaching limit!</span>");
+                                throw new Error(`Safety failure: Resume allowed after target limit reached at round ${round}`);
+                            }
+                        } else {
+                            logToScreen("<span style='color:red; font-weight:bold; font-size:14px;'>❌ FAILURE: Session failed to auto-stop above 2.0L!</span>");
+                            throw new Error(`Safety limit auto-stop failed to trigger at round ${round}`);
+                        }
+                    }
+
+                    if (!success && window.isLimitTesting) {
+                        throw new Error(`Round ${round} did not complete successfully`);
+                    }
+
+                    await sleep(1500); // Wait for transition back to idle
+                }
+
+                if (window.isLimitTesting) {
+                    logToScreen("<span style='color:#2ecc71; font-weight:bold; font-size:14px;'>🎉 ALL 20 LIMIT CASES PASSED! Water never exceeded quota cap!</span>");
+                }
+
+            } catch (err) {
+                logToScreen(`<span style="background:red; color:white; font-weight:bold;">❌ LIMIT TEST FAILED: ${err.message}</span>`);
+                console.error(err);
+            } finally {
+                window.isLimitTesting = false;
+                if (btn) {
+                    btn.style.background = "#16a085";
+                    btn.textContent = "🛡️ LIMIT TEST 20x";
+                }
+            }
+        };
+
+        window.isRealHwTesting = false;
+        window.runRealHardwareStressTest = async function () {
+            if (window.isRealHwTesting) {
+                window.isRealHwTesting = false;
+                logToScreen("<span style='color:red; font-weight:bold;'>🛑 REAL HW TEST CANCELLED BY USER</span>");
+                return;
+            }
+            window.isRealHwTesting = true;
+            logToScreen("<span style='color:#00f2fe; font-weight:bold;'>🧪 STARTING REAL HARDWARE CYCLE TEST (REAL WATER)...</span>");
+
+            const btn = document.getElementById('btnRealHwTest');
+            if (btn) {
+                btn.style.background = "#27ae60";
+                btn.textContent = "🛑 STOP TEST";
+            }
+
+            const sleep = ms => new Promise(r => setTimeout(r, ms));
+
+            try {
+                for (let round = 1; round <= 5; round++) {
+                    if (!window.isRealHwTesting) break;
+
+                    logToScreen(`<span style="background:#00f2fe; color:black; padding:2px 6px; font-weight:bold; border-radius:3px;">REAL HW ROUND ${round}/5</span>`);
+
+                    window.maxLiters = 2.0;
+                    window.selectedLiters = 0;
+
+                    logToScreen(`🧪 [REAL HW] Initializing 2.0L session...`);
+                    testDispenseV2(window.maxLiters);
+                    await sleep(1500);
+
+                    if (!window.isRealHwTesting) break;
+
+                    logToScreen("🧪 [REAL HW] Clicking START DISPENSE...");
+                    window._clickLocks = {};
+                    await startDispensing();
+                    await sleep(500);
+
+                    let pausedAt05 = false;
+                    let pausedAt12 = false;
+
+                    while (window.isRealHwTesting && dispenseSession && dispenseSession.active) {
+                        let newL = (dispenseSession.accumulatedPulses || 0) / K_FACTOR;
+
+                        // 1. Auto Pause at 0.5L
+                        if (newL >= 0.5 && newL < 0.8 && !pausedAt05) {
+                            pausedAt05 = true;
+                            logToScreen(`🧪 [REAL HW] Auto Pausing at ${newL.toFixed(3)}L...`);
+                            window._clickLocks = {};
+                            stopDispense();
+                            await sleep(3000);
+
+                            if (!window.isRealHwTesting || !dispenseSession.active) break;
+
+                            logToScreen("🧪 [REAL HW] Auto Resuming...");
+                            window._clickLocks = {};
+                            await startDispensing();
+                            await sleep(500);
+                        }
+
+                        // 2. Auto Pause at 1.2L
+                        if (newL >= 1.2 && newL < 1.5 && !pausedAt12) {
+                            pausedAt12 = true;
+                            logToScreen(`🧪 [REAL HW] Auto Pausing at ${newL.toFixed(3)}L...`);
+                            window._clickLocks = {};
+                            stopDispense();
+                            await sleep(3000);
+
+                            if (!window.isRealHwTesting || !dispenseSession.active) break;
+
+                            logToScreen("🧪 [REAL HW] Auto Resuming...");
+                            window._clickLocks = {};
+                            await startDispensing();
+                            await sleep(500);
+                        }
+
+                        if (!dispenseSession.active || dispenseSession.targetReached) {
+                            break;
+                        }
+
+                        await sleep(100);
+                    }
+
+                    logToScreen(`🧪 [REAL HW] Waiting for thank you screen transition...`);
+                    await sleep(9000); // 9 seconds (7s countdown + buffer)
+
+                    if (!window.isRealHwTesting) break;
+
+                    logToScreen(`<span style='color:#2ecc71; font-weight:bold;'>🎉 Round ${round} Completed Successfully!</span>`);
+                    await sleep(2000);
+                }
+
+                if (window.isRealHwTesting) {
+                    logToScreen("<span style='color:#2ecc71; font-weight:bold; font-size:14px;'>🎉 ALL 5 REAL HW CYCLES PASSED PERFECTLY!</span>");
+                }
+
+            } catch (err) {
+                logToScreen(`<span style="background:red; color:white; font-weight:bold;">❌ REAL HW TEST FAILED: ${err.message}</span>`);
+                console.error(err);
+            } finally {
+                window.isRealHwTesting = false;
+                if (btn) {
+                    btn.style.background = "#00f2fe";
+                    btn.textContent = "🧪 REAL HW TEST";
+                }
+            }
+        };
+
+        function sendContinuousStart() {
+            let [fsHi, fsLo] = flowSetBytes(65000); // Enormous target for continuous flow (114L)
+            sendPacket(0xC6, 0x53, [fsHi, fsLo], 'START-CONT-PUMP');
+            setTimeout(() => sendPacket(0xC3, 0x53, [fsHi, fsLo], 'START-CONT'), 50);
+            startPollTimer(250); // Ensure single poll timer
+        }
+
+        function openVolumeSelection(maxVol) {
+            console.log(`[FLOW] Skipping select-volume screen. Automatically selecting max quota: ${maxVol}L`);
+            // Ensure both variables are synchronized to the maximum remaining quota
+            maxLiters = maxVol || 2;
+            selectedLiters = maxLiters;
+            localStorage.setItem('MAX_QUOTA', maxLiters);
+            currentMaxQuota = maxVol;
+            selectVolume(maxLiters);
         }
 
         function manualStop() {
             // CRITICAL FIX: We MUST keep dispenseSession.active = true
             // so the background poll can observe the 'R' (Ready) state and trigger endSession.
-            dispenseSession.targetReached = true;
+            if (dispenseSession) {
+                dispenseSession.targetReached = true;
+            }
 
             // Pump Board expects STOP (0x50) array [0x00, 0x64]
             sendPacket(0xC6, 0x50, [0x00, 0x64], 'STOP-PUMP');
@@ -3584,7 +5183,7 @@
             // Old code sent 0x53 (Start) to C3 to "reset flowset" which briefly opened the valve
             setTimeout(() => sendPacket(0xC3, 0x50, [0x00, 0x00], 'RESET-FLOWSET-SAFE'), 300);
             logToScreen('<span style="color:#f39c12">⏹ STOP COMMANDS SENT — awaiting board ready...</span>');
-            console.log(`[DEBUG] V107.7: manualStop() called. targetLiters=${dispenseSession.targetLiters}`);
+            console.log(`[DEBUG] V107.7: manualStop() called. targetLiters=${dispenseSession ? dispenseSession.targetLiters : 'undefined'}`);
         }
 
         function forceReset() {
@@ -3652,7 +5251,7 @@
             const pC1 = buildPacket(0xC1, 0x53, pulseData);
             const pC3 = buildPacket(0xC3, 0x53, pulseData);
             // 2. Start Pump on C6
-            const pC6 = buildPacket(0xC6, 0x53, [0x00, 0x64]); // 100 pulses
+            const pC6 = buildPacket(0xC6, 0x53, pulseData); // 100 pulses
 
             if (window.AndroidSerial && window.AndroidSerial.sendHex) {
                 window.AndroidSerial.sendHex(pC1.map(b => b.toString(16).padStart(2, '0')).join('').toUpperCase());
@@ -3705,7 +5304,7 @@
         // Select volume from kiosk UI
         function selectVolume(liters) {
             // FORCE RESET session state cleanly before starting
-            LOCKDOWN_MODE = true; GLOBAL_STOP_SIGNAL = true; dispenseSession.active = false; setTimeout(() => { LOCKDOWN_MODE = false; }, 10000);
+            LOCKDOWN_MODE = true; GLOBAL_STOP_SIGNAL = true; dispenseSession.active = false; setTimeout(() => { LOCKDOWN_MODE = false; }, 1500);
             dispenseSession.paused = false;
             dispenseSession.targetReached = false; // ✅ FIX: Must be false!
             dispenseSession.wasRunning = false;
@@ -3843,8 +5442,8 @@
             window.AndroidSerial.openPort('/dev/ttyS9', 115200);
 
             // 🛰️ START CONTINUOUS POLLING (Keep-alive + TDS updates)
-            logToScreen("<span style='color:#2ecc71'>🛰️ STARTING CONTINUOUS HARDWARE POLLING (500ms)...</span>");
-            startPollTimer(500);
+            logToScreen("<span style='color:#2ecc71'>🛰️ STARTING CONTINUOUS HARDWARE POLLING (250ms)...</span>");
+            startPollTimer(250);
         }
     </script>
 
@@ -4036,13 +5635,18 @@
                 await setDoc(doc(db, "machines", DEVICE_ID), {
                     lastSeen: serverTimestamp(), // Standardized key
                     last_seen: serverTimestamp(), // Legacy key
-                    status: 'online',
+                    status: (window.isHardwareOnline === false) ? 'hardware_offline' : 'online',
                     id: DEVICE_ID,
                     apk_version: apkVer,
                     android_os_version: osVer,
                     tds_in: (window.telemetry && window.telemetry.tdsIn) || 0,
                     tds_out: (window.telemetry && window.telemetry.tdsOut) || 0,
+                    no_tap_water: (window.telemetry && window.telemetry.no_tap_water) || false,
+                    water_empty: (window.telemetry && window.telemetry.water_empty) || false,
+                    producing_water: (window.telemetry && window.telemetry.producing_water) || false,
+                    water_full: (window.telemetry && window.telemetry.water_full) || false,
                     android_temp: 38 + Math.floor(Math.random() * 5),
+                    // Real-time logs are streamed on-demand under console_streams to save internet/database writes
                     ...locationData
                 }, { merge: true });
                 console.log("[HEARTBEAT] TelemetrySynced (5m): v" + apkVer + " | GPS: " + (locationData.lat ? 'OK' : 'NA'));
@@ -4051,10 +5655,33 @@
             }
         }
 
-        // Start Heartbeat loop (every 5 minutes to save quota)
-        setInterval(sendHeartbeat, 5 * 60 * 1000);
+        // Start Heartbeat loop (every 1 minute to feed the watchdog and keep dashboard online)
+        setInterval(sendHeartbeat, 60 * 1000);
         setTimeout(sendHeartbeat, 10000); // Initial beat after load (10s)
+
+        // 🛡️ LOCAL NETWORK WATCHDOG
+        // If the app fails to successfully write/read Firestore for more than 2 minutes (120,000ms),
+        // and is not actively dispensing water, we reload/reboot the app to heal any frozen connection.
+        setInterval(() => {
+            const timeSinceLastActive = Date.now() - (window.lastSuccessfulWriteTime || 0);
+            const isDispensing = typeof dispenseSession !== 'undefined' && dispenseSession.active;
 
+            // Only log if silence exceeds 30 seconds to avoid flooding debug console
+            if (timeSinceLastActive > 30000) {
+                console.log(`[WATCHDOG] Time since last server sync: ${Math.round(timeSinceLastActive / 1000)}s | Dispensing: ${isDispensing}`);
+            }
+
+            if (timeSinceLastActive > 600000 && !isDispensing) { // 🚀 10 minutes tolerance (relaxing from 2m reboot loop bug)
+                console.error(`🚨 [WATCHDOG] Firestore silence of ${Math.round(timeSinceLastActive / 1000)}s exceeded 600s! Triggering app reboot.`);
+                if (window.AndroidSerial && typeof window.AndroidSerial.restartApp === 'function') {
+                    window.AndroidSerial.restartApp();
+                } else {
+                    console.warn("[WATCHDOG] AndroidSerial.restartApp not available. Simulating reload.");
+                    window.location.reload();
+                }
+            }
+        }, 10000); // Run watchdog check every 10 seconds
+
         window.updateSessionResult = async function (token, liters) {
             // liters <= 0 is allowed to mark session as finished with 0 volume status x181
             const safeLiters = Math.max(0, liters);
@@ -4068,6 +5695,10 @@
                 let userUid = 'anonymous';
                 let userName = isManual ? 'กดหน้าตู้ (Manual)' : 'สมาชิกทั่วไป';
 
+                // Get current TDS values safely from telemetry
+                const currentTdsIn = (window.telemetry && window.telemetry.tdsIn !== undefined) ? window.telemetry.tdsIn : 135;
+                const currentTdsOut = (window.telemetry && window.telemetry.tdsOut !== undefined) ? window.telemetry.tdsOut : 6;
+
                 // 1. Fetch Session Data if not manual
                 if (!isManual) {
                     const sessionRef = doc(db, "sessions", sessionId);
@@ -4077,12 +5708,36 @@
                         userUid = sData.userUid || 'anonymous';
                         userName = sData.userName || 'สมาชิกทั่วไป';
 
+                        // 🛡️ FIX: If userName is still default, look up real name/phone from users collection
+                        if ((userName === 'สมาชิกทั่วไป' || userName === 'User') && userUid && userUid !== 'anonymous') {
+                            try {
+                                const uRef = doc(db, "users", userUid);
+                                const uSnap = await getDoc(uRef);
+                                if (uSnap.exists) {
+                                    const uData = uSnap.data();
+                                    if (uData.name) {
+                                        userName = uData.name;
+                                        console.log("[REPORT] Resolved real userName from DB: " + userName);
+                                    } else if (uData.phone) {
+                                        userName = uData.phone;
+                                        console.log("[REPORT] Using phone as userName: " + userName);
+                                    }
+                                }
+                            } catch (nameErr) {
+                                console.error("[REPORT] Failed to lookup user name:", nameErr);
+                            }
+                        }
+
                         // 2. Update session to finished so LIFF shows receipt
                         logToScreen(`<span style="color:#2ecc71;">💾 SYNC: Saving ${safeLiters.toFixed(2)}L to Session ${sessionId.substr(-6)}</span>`);
                         await updateDoc(sessionRef, {
                             status: 'finished',
                             finalVol: safeLiters,
-                            finishedAt: serverTimestamp()
+                            finishedAt: serverTimestamp(),
+                            tdsIn: currentTdsIn,
+                            tdsOut: currentTdsOut,
+                            tds_in: currentTdsIn,
+                            tds_out: currentTdsOut
                         });
                         console.log(`[SYNC] Notified LIFF: Session ${sessionId} finished with ${safeLiters}L`);
                     }
@@ -4096,7 +5751,11 @@
                     method: methodLabel,
                     sessionId: sessionId,
                     userUid: userUid,
-                    userName: userName
+                    userName: userName,
+                    tdsIn: currentTdsIn,
+                    tdsOut: currentTdsOut,
+                    tds_in: currentTdsIn,
+                    tds_out: currentTdsOut
                 });
 
                 // 4. Update machine totals
@@ -4105,27 +5764,40 @@
                     lastSeen: serverTimestamp()
                 });
 
-                // 5. 💰 DEDUCT QUOTA: Update User Profile
+                // 5. 💰 DEDUCT QUOTA: Update User Profile + Reconcile litersLeft
                 if (userUid && userUid !== 'anonymous') {
                     try {
                         const userRef = doc(db, "users", userUid);
                         const userSnap = await getDoc(userRef);
                         if (userSnap.exists) {
                             const ud = userSnap.data();
+                            const currentLeft = parseFloat(ud.litersLeft ?? 0);
+
+                            // 🛡️ POST-RECONCILE (FIXED): หักโควตารายวันก่อน แล้วค่อยหักโควตาพิเศษ
+                            // ใช้ค่าที่ Capture ไว้ตอนเริ่ม session (ตั้งค่าใน confirmPhone / QR scan)
+                            const originalLiters = parseFloat(window.originalLitersLeft ?? 0);
+                            const originalExtra = parseFloat(window.originalExtraQuota ?? 0);
+                            const totalOriginal = originalLiters + originalExtra;
+
+                            // Remaining quota after dispensing
+                            const remainingTotal = Math.max(0, totalOriginal - safeLiters);
+
+                            // Distribute back (FIXED): To deduct Daily Quota (litersLeft) first, we must preserve extraQuota first!
+                            const newExtra = Math.min(originalExtra, remainingTotal);
+                            const newLeft = remainingTotal - newExtra;
+
                             let updates = {
                                 totalVol: increment(liters),
-                                updatedAt: serverTimestamp()
+                                updatedAt: serverTimestamp(),
+                                litersLeft: newLeft,
+                                extraQuota: newExtra > 0 ? newExtra : 0,
+                                isDispensing: window.deleteField(),
+                                lockTime: window.deleteField(),
+                                lockedByMachine: window.deleteField()
                             };
 
-                            // 💰 DEDUCT: Update user's remaining liters
-                            if (ud.litersLeft !== undefined) {
-                                updates.litersLeft = Math.max(0, ud.litersLeft - liters);
-                            } else if (ud.quota !== undefined) {
-                                updates.litersLeft = Math.max(0, ud.quota - liters);
-                            }
-                            
                             await updateDoc(userRef, updates);
-                            console.log(`[QUOTA] Deducted ${liters}L from User ${userUid}. New Balance: ${updates.litersLeft}`);
+                            console.log(`[QUOTA] User ${userUid}: dispensed ${safeLiters}L, litersLeft=${newLeft.toFixed(2)}, extraQuota=${newExtra.toFixed(2)}`);
                         }
                     } catch (quotaErr) {
                         console.error("[QUOTA] Failed to deduct:", quotaErr);
@@ -4154,30 +5826,53 @@
         window.updateHeartbeat = sendHeartbeat;
 
         // 🌐 CONNECTION STATUS UI
-        function updateOnlineUI(isOnline) {
+        let offlineDebounceTimer = null;
+        function updateOnlineUI(isOnline, forceInstant = false) {
+            if (isOnline) {
+                if (offlineDebounceTimer) { clearTimeout(offlineDebounceTimer); offlineDebounceTimer = null; }
+                window.executeOnlineUI(true);
+            } else {
+                if (forceInstant) {
+                    if (offlineDebounceTimer) { clearTimeout(offlineDebounceTimer); offlineDebounceTimer = null; }
+                    window.executeOnlineUI(false);
+                    return;
+                }
+                if (!offlineDebounceTimer) {
+                    offlineDebounceTimer = setTimeout(() => {
+                        offlineDebounceTimer = null;
+                        window.executeOnlineUI(false);
+                    }, 500); // Reduce debounce from 2000ms to 500ms for faster offline response
+                }
+            }
+        }
+
+        window.executeOnlineUI = function (isOnline) {
+            if (!isOnline && typeof dispenseSession !== 'undefined' && dispenseSession && dispenseSession.active) {
+                console.warn("[NETWORK] Offline detected, but user is dispensing. Suppressing offline overlay.");
+                return;
+            }
             const dot = document.getElementById('hwStatusDot');
             const txt = document.getElementById('hwStatusText');
-            const overlay = document.getElementById('offlineOverlay');
 
-            // 🔌 HW TRANSITION LOGGING
-            if (window.lastHwOnlineState !== isOnline) {
-                const oldState = window.lastHwOnlineState;
-                window.lastHwOnlineState = isOnline;
-                
-                if (oldState !== undefined) {
+            // 🌐 NETWORK TRANSITION LOGGING
+            if (window.lastNetOnlineState !== isOnline) {
+                const oldState = window.lastNetOnlineState;
+                window.lastNetOnlineState = isOnline;
+
+                if (oldState !== undefined && typeof db !== 'undefined' && typeof addDoc !== 'undefined' && typeof collection !== 'undefined' && typeof serverTimestamp !== 'undefined') {
                     try {
-                        const machineName = document.getElementById('hotlineMachineName')?.innerText || DEVICE_ID;
+                        const machineName = document.getElementById('hotlineMachineName')?.innerText || (typeof DEVICE_ID !== 'undefined' ? DEVICE_ID : 'Unknown');
                         addDoc(collection(db, "machine_logs"), {
-                            machineId: DEVICE_ID,
+                            machineId: typeof DEVICE_ID !== 'undefined' ? DEVICE_ID : 'Unknown',
                             machineName: machineName,
-                            status: isOnline ? 'hardware_online' : 'hardware_offline',
+                            status: isOnline ? 'network_online' : 'network_offline',
                             timestamp: serverTimestamp(),
-                            details: isOnline 
-                                ? 'การเชื่อมต่อบอร์ดควบคุมฮาร์ดแวร์กลับมาใช้งานได้ปกติ (Serial Restored)' 
-                                : 'การเชื่อมต่อบอร์ดควบคุมฮาร์ดแวร์ขัดข้อง (Serial Lost / Hardware Offline)'
+                            details: isOnline
+                                ? 'การเชื่อมต่ออินเทอร์เน็ตกลับมาใช้งานได้ปกติ'
+                                : 'การเชื่อมต่ออินเทอร์เน็ตล้มเหลว'
                         });
                     } catch (e) {
-                        console.error("Failed to log HW transition:", e);
+                        console.error("Failed to log Net transition:", e);
                     }
                 }
             }
@@ -4188,42 +5883,54 @@
                     txt.innerText = "Online";
                     txt.style.color = "white";
                 }
-                // ✅ ALWAYS HIDE OVERLAY WHEN ONLINE
-                if (overlay) {
-                    overlay.classList.remove('active');
-                    overlay.style.display = 'none';
-                }
             } else {
                 if (dot) dot.style.background = "#ff4b2b"; // Red
                 if (txt) {
-                    txt.innerText = "Hardware Offline";
+                    txt.innerText = "Network Offline";
                     txt.style.color = "rgba(255,255,255,0.6)";
                 }
-                // ⚠️ ONLY SHOW OFFLINE OVERLAY WHEN ON STANDBY SCREEN
-                // Otherwise it blocks the QR screen and other screens!
-                if (overlay && currentScreenName === 'standby') {
-                    overlay.classList.add('active');
-                    overlay.style.display = 'flex';
-                }
             }
+
+            window.syncSystemOverlay();
         }
 
         // 🎧 LISTEN FOR NAME CHANGES FROM ADMIN (Robust Version)
         function listenToConfig() {
+            // 🛡️ [LOW-SPEED SAFETY NET] Prevent Firebase listener from locking up the application boot on slow 0.10 Mbps connection!
+            let listenerResolved = false;
+            setTimeout(() => {
+                if (!listenerResolved) {
+                    console.warn("⚠️ [LISTEN-WATCHDOG] Config listener took too long. Forcing app to unlock!");
+                    // hideAppLoader(); // REMOVED: This function is undefined and causes ReferenceError crash → white screen
+                    showScreen('standby');
+                }
+            }, 3000);
             logToScreen("<span style='color:#3498db'>📻 STARTING CONFIG LISTENER...</span>");
             onSnapshot(doc(db, "machines", DEVICE_ID), (snap) => {
-                updateOnlineUI(true);
+                listenerResolved = true; // Unlock watchdog
+                // updateOnlineUI(true); removed from Firebase
 
                 if (snap.exists) {
                     const data = snap.data();
                     const machineName = data.name || DEVICE_ID;
+                    localStorage.setItem('KIOSK_NAME', machineName);
 
                     // 🎯 UPDATE BRAND NAME (TITLE)
-                    const brandEls = document.querySelectorAll('.kiosk-brand-name');
+                    const brandEls = document.querySelectorAll('.stationNameHeader');
                     if (brandEls.length > 0) {
                         brandEls.forEach(el => el.innerText = machineName);
                     }
 
+                    // 🎯 UPDATE ID LABELS
+                    const labelEl = document.getElementById('stationIdLabel');
+                    if (labelEl) labelEl.textContent = machineName;
+
+                    const el = document.getElementById('stationIdDisplay');
+                    if (el) el.textContent = machineName;
+
+                    const midDisp = document.getElementById('offlineMachineIdDisplay');
+                    if (midDisp) midDisp.innerText = machineName;
+
                     // 🎯 EXTRACT FILTER LIFE
                     const filterCap = data.filterCapacity || 3000;
                     const volAtReset = data.filterVolAtReset || 0;
@@ -4258,12 +5965,21 @@
                         }
                     }
 
+                    // 🔬 LOAD TDS CALIBRATION FACTOR (Dynamic จาก Firebase)
+                    if (data.tdsCalibration) {
+                        TDS_CALIBRATION_IN  = parseFloat(data.tdsCalibration.in)  || 1.0;
+                        TDS_CALIBRATION_OUT = parseFloat(data.tdsCalibration.out) || 1.0;
+                        console.log("[TDS-CAL] Loaded from Firebase: IN=", TDS_CALIBRATION_IN, "OUT=", TDS_CALIBRATION_OUT);
+                    } else {
+                        console.log("[TDS-CAL] No calibration found, using default 1.0");
+                    }
+
                     console.log("[SYNC] Machine Sync updated:", machineName, "Ads:", window.adImages.length, "TDS:", data.tds_in, data.tds_out);
                 } else {
                     logToScreen("<span style='color:#e67e22'>⚠️ CONFIG: Document not found in cloud</span>");
                 }
             }, (err) => {
-                updateOnlineUI(false);
+                // updateOnlineUI(false); removed from Firebase
                 logToScreen(`<span style='color:red;'>❌ CONFIG ERROR: ${err.message}</span>`);
             });
 
@@ -4273,31 +5989,121 @@
                     const data = snap.data();
                     if (data.master_pin) window.MASTER_PIN = data.master_pin;
 
-                    // 🚀 AUTO-UPDATE LOGIC
-                    const latestApkVer = data.latest_apk_version;
-                    const apkUrl = data.apk_url;
 
-                    if (latestApkVer && apkUrl) {
-                        let currentVer = "1.0.0";
-                        if (window.AndroidSerial && typeof window.AndroidSerial.getAppVersion === 'function') {
-                            currentVer = window.AndroidSerial.getAppVersion();
+
+                    // 🚀 CANARY OTA LOGIC (CAPACITOR HOT CODE PUSH)
+                    async function handleHotUpdate(webVersion, zipUrl) {
+                        return; // 🛑 Temporarily disabled to prevent APK downgrades
+                        if (!webVersion || !zipUrl) return;
+
+                        try {
+                            const { CapacitorUpdater } = window.Capacitor.Plugins;
+                            if (!CapacitorUpdater) return;
+
+                            // Local cached version tracking
+                            const currentWebVer = localStorage.getItem('CURRENT_WEB_VERSION') || "1.0.0";
+                            if (currentWebVer === webVersion) return; // Already updated
+
+                            logToScreen(`<span style="color:#f1c40f; font-weight:bold;">✨ 📥 OTA: Downloading v${webVersion}...</span>`);
+
+                            const res = await CapacitorUpdater.download({
+                                url: zipUrl,
+                                version: webVersion
+                            });
+
+                            logToScreen(`<span style="color:#2ecc71; font-weight:bold;">✨ 🔄 OTA: Refreshing to v${webVersion}</span>`);
+                            localStorage.setItem('CURRENT_WEB_VERSION', webVersion);
+                            await CapacitorUpdater.set({ id: webVersion });
+                        } catch (e) {
+                            console.warn("OTA Hot Push Error:", e);
                         }
+                    }
 
-                        if (currentVer !== latestApkVer) {
-                            logToScreen(`<span style="color:#f1c40f; font-weight:bold;">✨ NEW UPDATE DETECTED: v${latestApkVer}</span>`);
-                            if (window.AndroidSerial && typeof window.AndroidSerial.downloadAndInstallUpdate === 'function') {
-                                logToScreen("📥 Initiating background update...");
-                                window.AndroidSerial.downloadAndInstallUpdate(apkUrl);
+                    // Priority 1: Check individual Machine config (Canary)
+                    let targetWebVer = data.latest_web_version;
+                    let targetZipUrl = data.web_zip_url;
+
+                    let targetApkVer = data.ota_target_version;
+                    let targetApkUrl = data.ota_apk_url;
+                    let forceApkUpdate = data.ota_force_update;
+
+                    async function handleAndroidOTA(targetVer, apkUrl, forceUpdate) {
+                        if (!targetVer || !apkUrl) return;
+                        try {
+                            let currentVer = "0.0.0";
+                            if (window.AndroidSerial && typeof window.AndroidSerial.getAppVersion === 'function') {
+                                currentVer = window.AndroidSerial.getAppVersion();
+                            }
+                            
+                            if (currentVer && currentVer !== "0.0.0") {
+                                if (currentVer !== targetVer || forceUpdate) {
+                                    if (window.lastTriggeredOtaVersion !== targetVer) {
+                                        window.lastTriggeredOtaVersion = targetVer;
+                                        logToScreen(`<span style="color:#f1c40f; font-weight:bold;">📲 OTA: Triggering Android App Update to v${targetVer}...</span>`);
+                                        
+                                        // 🛑 CLEAR THE FLAG IN FIREBASE FIRST TO PREVENT INFINITE LOOP!
+                                        if (forceUpdate && window.DEVICE_ID) {
+                                            try {
+                                                await window.updateDoc(window.doc(window.db, "machines", window.DEVICE_ID), {
+                                                    ota_force_update: window.deleteField(),
+                                                    force_apk_update: window.deleteField()
+                                                });
+                                                console.log("🧹 Cleared force update flags in Firebase");
+                                            } catch (e) {
+                                                console.error("Failed to clear force update flags:", e);
+                                            }
+                                        }
+
+                                        if (window.AndroidSerial && typeof window.AndroidSerial.downloadAndInstallUpdate === 'function') {
+                                            window.AndroidSerial.downloadAndInstallUpdate(apkUrl);
+                                        }
+                                    }
+                                }
                             }
+                        } catch (e) {
+                            console.warn("Android OTA Error:", e);
                         }
                     }
+
+                    if (window.DEVICE_ID) {
+                        const machineRef = window.doc(window.db, "machines", window.DEVICE_ID);
+                        window.getDoc(machineRef).then(mSnap => {
+                            if (mSnap.exists() && mSnap.data().target_web_version && mSnap.data().target_web_zip_url) {
+                                targetWebVer = mSnap.data().target_web_version;
+                                targetZipUrl = mSnap.data().target_web_zip_url;
+                            }
+                            if (mSnap.exists() && mSnap.data().ota_target_version && mSnap.data().ota_apk_url) {
+                                targetApkVer = mSnap.data().ota_target_version;
+                                targetApkUrl = mSnap.data().ota_apk_url;
+                                forceApkUpdate = mSnap.data().ota_force_update;
+                            }
+                            handleHotUpdate(targetWebVer, targetZipUrl);
+                            handleAndroidOTA(targetApkVer, targetApkUrl, forceApkUpdate);
+                        }).catch(e => {
+                            // Fallback to global
+                            handleHotUpdate(targetWebVer, targetZipUrl);
+                            handleAndroidOTA(targetApkVer, targetApkUrl, forceApkUpdate);
+                        });
+                    } else {
+                        handleHotUpdate(targetWebVer, targetZipUrl);
+                        handleAndroidOTA(targetApkVer, targetApkUrl, forceApkUpdate);
+                    }
+                    // 💡 LIGHT SCHEDULE: load from Admin settings
+                    if (data.lightOnHour !== undefined) {
+                        window._lightOnHour = parseInt(data.lightOnHour);
+                        logToScreen(`<span style="color:#f39c12;">💡 LIGHT SCHEDULE: ON=${window._lightOnHour}:00, OFF=${window._lightOffHour}:00</span>`);
+                    }
+                    if (data.lightOffHour !== undefined) {
+                        window._lightOffHour = parseInt(data.lightOffHour);
+                    }
+
                     logToScreen(`<span style="color:#3498db; font-style:italic;">📡 GLOBAL: Tech Config synced</span>`);
                 }
             });
         }
 
         // Push ID to UI elements & Add Custom ID Prompt
-        const syncIdToUI = () => {
+        const syncIdToUI_2 = () => {
             const overlay = document.getElementById('deviceIdOverlay');
             if (overlay) {
                 overlay.innerText = "ID: " + DEVICE_ID;
@@ -4365,8 +6171,8 @@
         };
 
         // Network event listeners
-        window.addEventListener('online', () => updateOnlineUI(true));
-        window.addEventListener('offline', () => updateOnlineUI(false));
+        // window.addEventListener('online', () => updateOnlineUI(true)); // Delegated to updateNetworkStatus
+        // window.addEventListener('offline', () => updateOnlineUI(false));
 
         // ── 📺 ADS & TIMEOUT SYSTEM LOGIC ──
         let idleSeconds = 0;
@@ -4410,9 +6216,14 @@
             if (typeof showScreen === 'function') showScreen('standby');
         }
 
+        // 🚀 KIOSK STABILITY WATCHDOGS (Daily Refresh & Auto-Recovery)
+        const appStartTime = Date.now();
+
         // Global activity listeners
         window.addEventListener('touchstart', resetIdle);
         window.addEventListener('mousedown', resetIdle);
+        window.addEventListener('click', resetIdle);
+        window.addEventListener('pointerdown', resetIdle);
 
         setInterval(() => {
             // 🚀 ONLY INCREMENT IDLE IF NOT DISPENSING
@@ -4422,13 +6233,20 @@
                 idleSeconds = 0; // Reset while water is flowing
             }
 
+            // ⏰ Send Heartbeat to Android Watchdog every 5 seconds
+            if (idleSeconds % 5 === 0) {
+                if (window.AndroidSerial && typeof window.AndroidSerial.sendHeartbeat === 'function') {
+                    window.AndroidSerial.sendHeartbeat();
+                }
+            }
+
             if (idleSeconds >= IDLE_LIMIT) {
                 const adsActive = document.getElementById('adsOverlay').style.display === 'block';
-                
+
                 // 🚀 CHECK IF WE ARE ON ANY INTERACTIVE SCREEN (NOT STANDBY)
                 const currentScreen = document.querySelector('.screen.active');
                 const isStandby = currentScreen && currentScreen.id === 'screen-standby';
-                
+
                 if (currentScreen && !isStandby) {
                     logToScreen('⏳ [TIMEOUT] Inactive for 120s. Returning to standby...');
                     if (typeof manualStop === 'function') manualStop();
@@ -4438,12 +6256,110 @@
                     showAds();
                 }
             }
+
+            // ⏰ WATCHDOG 1: Nightly Scheduled Refresh at 00:00 AM (Midnight)
+            const now = new Date();
+            if (now.getHours() === 0 && now.getMinutes() === 0 && now.getSeconds() === 0) {
+                const currentScreen = document.querySelector('.screen.active');
+                const isStandby = currentScreen && currentScreen.id === 'screen-standby';
+                const adsActive = document.getElementById('adsOverlay') && document.getElementById('adsOverlay').style.display === 'block';
+
+                // Refresh only if the machine is completely idle (standby or ads screen) to avoid interrupting a user session
+                if (isStandby || adsActive) {
+                    console.log("⏰ [WATCHDOG] Nightly stability refresh triggered at 12:00 AM (Midnight). Purging memory heap...");
+                    location.reload(true);
+                }
+            }
+
+            // ⏰ WATCHDOG 2: 4-Hour Continuous Run Auto-Recovery Fail-safe (Highly active memory cleanup)
+            const runningDuration = Date.now() - appStartTime;
+            if (runningDuration >= 4 * 60 * 60 * 1000) { // 4 hours
+                const currentScreen = document.querySelector('.screen.active');
+                const isStandby = currentScreen && currentScreen.id === 'screen-standby';
+                const adsActive = document.getElementById('adsOverlay') && document.getElementById('adsOverlay').style.display === 'block';
+
+                if (isStandby || adsActive) {
+                    console.log("🛡️ [WATCHDOG] Continuous run limit (4h) reached. Performing scheduled refresh for stability...");
+                    location.reload(true);
+                }
+            }
         }, 1000);
 
+        // 🚀 ON-DEMAND THROTTLED LOG STREAMING (Saves Internet & Database writes)
+        window.isConsoleStreaming = false;
+        window.lastConsoleUploadTime = 0;
+        window.consoleUploadTimeout = null;
+
+        window.triggerConsoleUpload = function () {
+            if (!window.isConsoleStreaming) return;
+            const now = Date.now();
+            const timeSinceLastUpload = now - window.lastConsoleUploadTime;
+
+            // Limit writes to at most once every 3 seconds
+            if (timeSinceLastUpload >= 3000) {
+                uploadConsoleLogsNow();
+            } else {
+                if (window.consoleUploadTimeout) clearTimeout(window.consoleUploadTimeout);
+                window.consoleUploadTimeout = setTimeout(() => {
+                    uploadConsoleLogsNow();
+                }, 3000 - timeSinceLastUpload);
+            }
+        };
+
+        async function uploadConsoleLogsNow() {
+            if (!window.isConsoleStreaming) return;
+            window.lastConsoleUploadTime = Date.now();
+            if (window.consoleUploadTimeout) {
+                clearTimeout(window.consoleUploadTimeout);
+                window.consoleUploadTimeout = null;
+            }
+            try {
+                // Write directly to dedicated lightweight document
+                await setDoc(doc(db, "console_streams", DEVICE_ID), {
+                    logs: window.consoleLogsList || [],
+                    timestamp: serverTimestamp()
+                }, { merge: true });
+                console.log("[STREAM] Logs uploaded to console_streams");
+            } catch (e) {
+                console.warn("[STREAM] Upload fail", e);
+            }
+        }
+
         listenToConfig();
         updateHeartbeat();
         setInterval(updateHeartbeat, 5 * 60 * 1000);
-        updateOnlineUI(navigator.onLine);
+        // updateOnlineUI(navigator.onLine); // Removed to prevent false positive on offline boot
+
+        // 🛡️ SAFETY: Prevent water flow if app is closed or put to background
+        document.addEventListener('visibilitychange', function () {
+            if (document.hidden) {
+                if (typeof dispenseSession !== 'undefined' && dispenseSession.active && !dispenseSession.paused) {
+                    console.warn('⚠️ App went to background while dispensing! Stopping flow for safety.');
+                    // Force pause command immediately
+                    sendPacket(0xC6, 0x50, [0x00, 0x00], 'EMERGENCY-PUMP-STOP');
+                    setTimeout(() => sendPacket(0xC3, 0x50, [0x00, 0x00], 'EMERGENCY-VALVE-STOP'), 100);
+                    dispenseSession.paused = true;
+                    window._activeFlowStartTime = null;
+                    // Snap UI
+                    if (typeof updateDispenseScreen === 'function') {
+                        let actualL = (Number(dispenseSession.accumulatedPulses) || 0) / K_FACTOR;
+                        updateDispenseScreen(actualL, dispenseSession.targetLiters, dispenseSession.accumulatedPulses);
+                    }
+                    if (typeof refreshDispenseButtons === 'function') refreshDispenseButtons();
+                }
+            }
+        });
+
+        window.addEventListener('beforeunload', function () {
+            if (typeof dispenseSession !== 'undefined' && dispenseSession.active) {
+                // Synchronous/fire-and-forget stop before JS context is destroyed
+                if (window.AndroidSerial && window.AndroidSerial.sendHex) {
+                    window.AndroidSerial.sendHex('C65000000000'); // PUMP STOP
+                }
+                sendPacket(0xC6, 0x50, [0x00, 0x00], 'UNLOAD-PUMP-STOP');
+                sendPacket(0xC3, 0x50, [0x00, 0x00], 'UNLOAD-VALVE-STOP');
+            }
+        });
     </script>
 
     <!-- 🎨 Version Verification Tag -->
@@ -4510,19 +6426,65 @@
                 <button id="pinSubmitBtn"
                     style="padding:15px; background:#00ff88; border:none; color:black; border-radius:10px; font-size:20px; font-weight:bold;">OK</button>
             </div>
-            <button onclick="document.getElementById('pinOverlay').style.display='none'"
+            <button onclick="window.closePinOverlay()"
                 style="margin-top:20px; background:none; border:none; color:#666; font-size:12px; cursor:pointer;">CANCEL
                 / EXIT</button>
         </div>
     </div>
 
+    <!-- 🔍 Searching Loading Modal Overlay -->
+    <div id="searchLoadingOverlay"
+        style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.85); z-index:9999999; flex-direction:column; justify-content:center; align-items:center; color:white; font-family:'Sarabun', sans-serif; text-align:center; padding:20px;">
+        <div class="search-spinner"
+            style="width:120px; height:120px; border:12px solid rgba(255,255,255,0.1); border-top:12px solid #00f2fe; border-radius:50%; animation:search-spin 1.2s cubic-bezier(0.5, 0, 0.5, 1) infinite; box-shadow: 0 0 30px rgba(0,242,254,0.4);">
+        </div>
+        <h2
+            style="margin-top:40px; font-size:52px; font-weight:900; letter-spacing:1px; text-shadow: 0 4px 20px rgba(0,0,0,0.8); color:#00f2fe; margin-bottom: 10px;">
+            กำลังค้นหาข้อมูลสมาชิก...</h2>
+        <p style="margin-top:10px; font-size:32px; color:#e2e8f0; font-weight:bold; max-width:800px; line-height:1.4;">
+            โปรดรอสักครู่ ระบบกำลังตรวจสอบสิทธิ์และดึงข้อมูลโควตา</p>
+    </div>
+    <style>
+        @keyframes search-spin {
+            0% {
+                transform: rotate(0deg);
+            }
+
+            100% {
+                transform: rotate(360deg);
+            }
+        }
+
+        .btn-custom-alert-ok {
+            background: #ff4b2b;
+            color: white;
+            border: none;
+            padding: 20px 80px;
+            font-size: 40px;
+            font-weight: bold;
+            border-radius: 20px;
+            cursor: pointer;
+            box-shadow: 0 10px 0 #b3341e;
+            transition: all 0.1s;
+        }
+
+        .btn-custom-alert-ok:active {
+            transform: translateY(5px);
+            box-shadow: 0 5px 0 #b3341e;
+        }
+    </style>
+
     <!-- 🛸 Custom Large Alert Modal -->
-    <div id="customAlertModal" style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.9); z-index:10000000; justify-content:center; align-items:center; flex-direction:column; color:white; font-family:sans-serif; text-align:center; padding:20px;">
-        <div style="background:#222; border:5px solid #ff4b2b; padding:40px; border-radius:30px; max-width:90%; box-shadow:0 0 50px rgba(255,75,43,0.5);">
+    <div id="customAlertModal"
+        style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.9); z-index:10000000; justify-content:center; align-items:center; flex-direction:column; color:white; font-family:sans-serif; text-align:center; padding:20px;">
+        <div
+            style="background:#222; border:5px solid #ff4b2b; padding:40px; border-radius:30px; max-width:90%; box-shadow:0 0 50px rgba(255,75,43,0.5);">
             <div style="font-size:80px; margin-bottom:20px;">⚠️</div>
-            <h2 id="customAlertTitle" style="font-size:48px; color:#ff4b2b; margin-bottom:20px; font-weight:900;">แจ้งเตือน</h2>
-            <p id="customAlertMessage" style="font-size:40px; line-height:1.4; margin-bottom:40px; font-weight:bold;">ข้อความ</p>
-            <button onclick="closeCustomAlert()" style="background:#ff4b2b; color:white; border:none; padding:20px 80px; font-size:40px; font-weight:bold; border-radius:20px; cursor:pointer; box-shadow:0 10px 0 #b3341e; active:{transform:translateY(5px); box-shadow:0 5px 0 #b3341e;}">ตกลง</button>
+            <h2 id="customAlertTitle" style="font-size:48px; color:#ff4b2b; margin-bottom:20px; font-weight:900;">
+                แจ้งเตือน</h2>
+            <p id="customAlertMessage" style="font-size:40px; line-height:1.4; margin-bottom:40px; font-weight:bold;">
+                ข้อความ</p>
+            <button class="btn-custom-alert-ok" onclick="closeCustomAlert()">ตกลง</button>
         </div>
     </div>
 
@@ -4535,6 +6497,14 @@
         function closeCustomAlert() {
             document.getElementById('customAlertModal').style.display = 'none';
         }
+        function showSearchLoading() {
+            const overlay = document.getElementById('searchLoadingOverlay');
+            if (overlay) overlay.style.display = 'flex';
+        }
+        function hideSearchLoading() {
+            const overlay = document.getElementById('searchLoadingOverlay');
+            if (overlay) overlay.style.display = 'none';
+        }
     </script>
 </body>
 
